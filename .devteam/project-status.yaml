project:
  name: "Speaker search by nickname & secretary full permissions with review notifications"
  type: feature_request
  created_at: "2026-03-07"
  user_request: |
    1. CR-275: Nos campos de search dos discursantes, procurar pelo apelido (informal_name) também.
    2. CR-276: Secretário com mesmas permissões que bispado (speech:assign, speech:unassign, home:next_assignments).
       Quando secretário designar discursante ou tema para posições 1-3, enviar push notification imediata ao bispado.
       Novo tipo de notificação via trigger SQL (padrão do app). Expandir trigger para detectar mudanças em topic.
       Textos i18n (pt-BR, en-US, es-LA). Orações (pos 0,4) não notificam. Bispado não gera notificação extra.

phases:
  total: 1
  current: 1
  summary:
    - phase: 1
      artifact_id: "013"
      description: "Speaker search by nickname (CR-275) & secretary full permissions with review notifications (CR-276)"
      status: completed
      crs: [275, 276]

current_phase:
  id: 1
  started_at: "2026-03-07"
  features: [F065, F066]
  change_requests: [275, 276]
  current_agent: "orchestrator"
  current_step: "Project completed - all phases done"

artifacts:
  spec_consolidated: "docs/specs/SPEC_CONSOLIDATED.yaml"
  arch_consolidated: "docs/arch/ARCH_CONSOLIDATED.yaml"
  plan_consolidated: "docs/plans/PLAN_CONSOLIDATED.yaml"
  code_ledger: "docs/code/CODE_LEDGER.yaml"
  test_consolidated: "docs/tests/TEST_CONSOLIDATED.yaml"
  review_consolidated: "docs/reviews/REVIEW_CONSOLIDATED.yaml"
  qa_consolidated: "docs/qa/QA_CONSOLIDATED.yaml"

pending_deploys:
  - type: migration
    description: "031_secretary_review_notification.sql via SQL Editor or Management API"
    order: 1
  - type: edge_function
    description: "supabase functions deploy process-notifications"
    order: 2
  - type: eas_build
    description: "Client-side changes: permissions, assigned_by_role, informal_name search, notification text"
    order: 3

last_updated: "2026-03-08"

artifact_counter:
  current: 13
