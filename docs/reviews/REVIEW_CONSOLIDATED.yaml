# =============================================================================
# REVIEW_CONSOLIDATED.yaml
# Consolidated code review records
# =============================================================================

metadata:
  project: Sacrament Meeting Planner
  last_updated: "2026-03-05"
  version: 12
  last_updated_by: "phase-010"

reviews:
  - id: REV-001
    scope: "CR-248 - Project Cleanup & Consolidation"
    date: "2026-03-03"
    reviewer: reviewer-agent
    commits:
      - hash: e1a0bb8
        message: "refactor(cleanup): consolidate project removing all CR/batch history (CR-248 phase 1)"
      - hash: 12e0d92
        message: "test(cleanup): remove CR/SPEC/ARCH references from tests and populate TEST_CONSOLIDATED (CR-248 phase 3)"
    verdict: changes_required

    summary: |
      The cleanup was largely successful: 245k+ lines of historical docs removed,
      batch/CR/phase tests deleted, SPEC_CONSOLIDATED rewritten from scratch with
      38 features, TEST_CONSOLIDATED populated with 577 tests, process docs zeroed,
      archive created. No functional code was lost. However, two categories of
      residual references remain that must be addressed before this can be approved.

    issues:
      - id: P1-001
        severity: P1
        title: "13 CR-XXX references remain in source code comments"
        description: |
          The user explicitly requested "remover comentarios CR do codigo-fonte".
          Phase 1 commit (e1a0bb8) removed some CR comments but missed 13 references
          across 7 source files. These are all in code comments (not functional code).
        files:
          - path: src/components/DebouncedTextInput.tsx
            line: 4
            content: "Prevents excessive mutation calls during typing (CR-27)."
          - path: src/hooks/useSundayTypes.ts
            line: 25
            content: "Per F007/CR-56: 'speeches' is stored in sunday_exceptions table"
          - path: src/contexts/AuthContext.tsx
            line: 104
            content: "F116 (CR-178): App language from user_metadata"
          - path: src/components/AgendaForm.tsx
            line: 509
            content: "Special meeting: dynamic section header based on type (CR-29)"
          - path: src/components/SundayCard.tsx
            lines: [47, 103, 461, 533, 590]
            content: "Multiple CR-221 references in JSDoc and JSX comments"
          - path: src/components/SpeechSlot.tsx
            line: 55
            content: "CR-221: Whether this slot is a prayer"
          - path: src/app/(tabs)/speeches.tsx
            lines: [436, 489]
            content: "CR-221 references in JSX comments"
          - path: src/app/(tabs)/settings/whatsapp.tsx
            line: 415
            content: "CR-231: Segmented control always shown"
        fix: |
          Remove the CR-XXX portion of each comment while preserving the useful
          descriptive text. For example:
          - "CR-221: Whether managed prayers is enabled" -> "Whether managed prayers is enabled"
          - "Per F007/CR-56: speeches is stored..." -> "speeches is stored..."
          - "Special meeting: dynamic section header based on type (CR-29)" -> "Special meeting: dynamic section header based on type"

      - id: P1-002
        severity: P1
        title: "ARCH_CONSOLIDATED spec_features reference 98 old-style feature IDs not in new SPEC"
        description: |
          SPEC_CONSOLIDATED was rewritten from scratch with new sequential IDs F001-F038.
          However, ARCH_CONSOLIDATED still references the old feature IDs (F044-F173)
          in its spec_features arrays for each module. These 98 old IDs do not exist
          in the new SPEC, making the cross-references broken and meaningless.
        files:
          - path: docs/arch/ARCH_CONSOLIDATED.yaml
            lines: [124, 179, 239, 306, 380, 403, 426, 449]
            content: "spec_features arrays contain old IDs like F050, F093-F173"
        fix: |
          Re-map each module's spec_features to reference only the new F001-F038 IDs
          from SPEC_CONSOLIDATED.yaml. Each module should list which of the 38 new
          features it implements.

      - id: P2-001
        severity: P2
        title: "TEST_CONSOLIDATED header references CR-248"
        description: |
          Line 3 of TEST_CONSOLIDATED.yaml contains:
          "Consolidated test inventory - all surviving tests after CR-248 cleanup"
          This is a minor cosmetic issue in a comment header.
        files:
          - path: docs/tests/TEST_CONSOLIDATED.yaml
            line: 3
            content: "# Consolidated test inventory - all surviving tests after CR-248 cleanup"
        fix: |
          Change to: "# Consolidated test inventory for all project tests"

    verified_items:
      - item: "Individual doc files deleted (SPEC_F*, ARCH_M*, PLAN_P*, QA_PLAN*)"
        status: pass
      - item: "history/ directories deleted"
        status: pass
      - item: ".devteam/phases/ deleted"
        status: pass
      - item: "Batch/CR/phase/bug/qa test files deleted"
        status: pass
      - item: "SPEC_CONSOLIDATED.yaml rewritten from scratch (38 features, no CR refs)"
        status: pass
      - item: "ARCH_CONSOLIDATED.yaml free of CR-XXX text references"
        status: pass
      - item: "CODE_LEDGER.yaml zeroed with structure"
        status: pass
      - item: "REVIEW_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "QA_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "PLAN_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "TEST_CONSOLIDATED.yaml populated (30 files, 577 tests)"
        status: pass
      - item: "CHANGE_REQUESTS_ARCHIVE.yaml exists with warning header"
        status: pass
      - item: "No source code files deleted (only docs and tests)"
        status: pass
      - item: "No batch/BATCH/phase-/PHASE_ references in docs or src"
        status: pass
      - item: "Tests passing (577 confirmed by tester)"
        status: pass
      - item: "Test files clean of CR references"
        status: pass

    pending_deploys: []

  - id: REV-002
    scope: "CR-248 - Re-review after P1/P2 fixes"
    date: "2026-03-03"
    reviewer: reviewer-agent
    commits_reviewed:
      - hash: 93b0c76
        description: "Remove 13 CR-XXX references from source code + fix TEST_CONSOLIDATED header"
      - description: "ARCH_CONSOLIDATED.yaml rewritten from scratch by architect (aligned with new SPEC F001-F038)"
    verdict: approved

    summary: |
      All three issues from REV-001 have been resolved. The project cleanup
      is now complete.

    fixes_verified:
      - issue: P1-001
        title: "CR-XXX references in source code"
        status: fixed
        verification: |
          grep for CR-\d+ in src/ returns zero matches. All 13 references
          across 7 files have been removed.

      - issue: P1-002
        title: "ARCH spec_features referencing old feature IDs"
        status: fixed
        verification: |
          ARCH_CONSOLIDATED.yaml was rewritten from scratch. Now contains
          13 modules with features arrays referencing only F001-F038.
          Feature ID sets match exactly between SPEC and ARCH (perfect 1:1
          mapping of all 38 features). No old-style IDs (F039+) remain.

      - issue: P2-001
        title: "TEST_CONSOLIDATED header referencing CR-248"
        status: fixed
        verification: |
          Header now reads "# Consolidated test inventory" with no CR reference.

    additional_checks:
      - check: "SPEC x ARCH feature consistency"
        status: pass
        details: |
          ARCH features union = {F001..F038}, SPEC features = {F001..F038}.
          All 38 features are accounted for in module assignments. No orphaned
          features in either direction.

      - check: "ARCH quality"
        status: pass
        details: |
          New ARCH has 13 well-defined modules (vs 8 before), 14 ADRs,
          complete database schema, edge functions, provider hierarchy,
          and shared libraries sections. Clean, well-structured, no
          historical references.

      - check: "No CR/batch/phase references in docs or source"
        status: pass
        details: |
          Full grep across src/ and docs consolidated files returns zero
          matches for CR-\d+, batch, BATCH, phase-, PHASE_. Only expected
          references remain in CHANGE_REQUESTS_ARCHIVE.yaml (historical
          archive with warning header) and REVIEW_CONSOLIDATED.yaml
          (review records documenting the cleanup).

      - check: "No functional code lost"
        status: pass
        details: "All source files intact. Only docs and test files were deleted."

    advisory_notes:
      - note: |
          docs/PRODUCT_SPECIFICATION.md (180KB) and docs/SPEC.final.md (99KB)
          are the user's original specification documents that predate the CR
          system. They are superseded by SPEC_CONSOLIDATED.yaml but may have
          sentimental or reference value. The user may want to archive or
          delete them in a future cleanup pass.
      - note: |
          docs/SUPABASE_QUERY_PERF.md and docs/SUPABASE_SECURITY.md are raw
          data dumps from Supabase tooling, not CR-related documentation.
          They can remain as-is or be cleaned up at the user's discretion.

    pending_deploys: []

  # ===========================================================================
  # REV-003: Phase 1 - Quick fixes (CR-250, 251, 252, 254, 256, 257)
  # ===========================================================================

  - id: REV-003
    scope: "F039 Phase 1 - Quick fixes (CR-250, CR-251, CR-252, CR-254, CR-256, CR-257)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F039
    arch: M014
    commits:
      - hash: 4a83fe8
        message: "fix(send-reset-email): change email language fallback from pt-BR to en (CR-251)"
      - hash: 4b3a41f
        message: "fix(create-invitation): remove diagnostic mode (CR-252)"
      - hash: 1bbdd9d
        message: "fix(csvUtils): require 3-column CSV format only (CR-254)"
      - hash: b310ed8
        message: "fix(settings): allow secretary to toggle manage_prayers (CR-256)"
      - hash: f9625c9
        message: "docs(process-notifications): correct cron interval comment to 5 minutes (CR-257)"
    verdict: approved

    summary: |
      All 5 code commits for Phase 1 are correct, complete, and match the SPEC
      (F039) and ARCH (M014) specifications. Chain validation SPEC->ARCH->CODE
      is fully aligned. 589 tests pass (30 test files, 0 failures). The doc
      corrections to SPEC_CONSOLIDATED, ARCH_CONSOLIDATED, and the code changes
      are all consistent with each other. No P0 or P1 issues found.

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      # -----------------------------------------------------------------------
      # CR-251: Email language fallback
      # -----------------------------------------------------------------------
      - cr: CR-251
        title: "Change email language fallback from pt-BR to en"
        verdict: pass
        verification:
          - check: "Line 82: templates fallback changed from 'pt-BR' to 'en'"
            status: pass
            file: supabase/functions/send-reset-email/index.ts
            detail: "return templates[language] ?? templates['en'];"
          - check: "Line 152: Comment updated to say 'en'"
            status: pass
            detail: "// Priority: user_metadata.language > ward.language > 'en'"
          - check: "Line 153: Default language changed from 'pt-BR' to 'en'"
            status: pass
            detail: "let language = 'en';"
          - check: "Line 165: Ward fallback changed from 'pt-BR' to 'en'"
            status: pass
            detail: "language = ward?.language ?? 'en';"
          - check: "All 4 'pt-BR' fallbacks changed to 'en'"
            status: pass
            detail: "grep for pt-BR in send-reset-email shows zero fallback references. The only remaining 'pt-BR' is the template key itself (line 19), which is correct."
        notes: |
          SPEC_CONSOLIDATED F002 updated to say "en (en-US)". Code uses 'en' which
          matches the template map key. All 4 occurrences changed as specified.

      # -----------------------------------------------------------------------
      # CR-252: Remove diagnostic mode
      # -----------------------------------------------------------------------
      - cr: CR-252
        title: "Remove diagnostic mode from create-invitation"
        verdict: pass
        verification:
          - check: "diagnose field removed from CreateInvitationInput interface"
            status: pass
            detail: "Interface now has only email and role fields. No diagnose field."
          - check: "Diagnostic mode block removed (was lines 118-135)"
            status: pass
            detail: "grep for 'diagnos' in create-invitation returns zero matches."
          - check: "Header comment updated"
            status: pass
            detail: |
              Old: "F143 (CR-208): Structured error logging, error codes, and diagnostic mode."
              New: "F143 (CR-208): Structured error logging and error codes."
              The 'and diagnostic mode' text removed. ADR-094 line also removed.
          - check: "isBishopric variable removed from settings/index.tsx (CR-256 side-effect)"
            status: pass
            detail: "const isBishopric = role === 'bishopric' was removed since it's no longer used."
        notes: |
          Clean dead code removal. The diagnostic block, interface field, and
          related comments were all removed. No functional behavior changes.

      # -----------------------------------------------------------------------
      # CR-254: CSV 3-column only
      # -----------------------------------------------------------------------
      - cr: CR-254
        title: "Require 3-column CSV format only"
        verdict: pass
        verification:
          - check: "Header validation changed from 'at least 2' to 'at least 3'"
            status: pass
            file: src/lib/csvUtils.ts
            detail: "if (headerParts.length < 3) with INVALID_HEADER error"
          - check: "2-column fallback logic removed (hasThreeColumns branch)"
            status: pass
            detail: "hasThreeColumns variable and conditional branch eliminated. Direct 3-column parsing only."
          - check: "Row validation changed from 'at least 2' to 'at least 3'"
            status: pass
            detail: "if (parts.length < 3) with INSUFFICIENT_COLUMNS error"
          - check: "Comment updated to say '3-column format'"
            status: pass
            detail: "JSDoc now says 'Requires 3-column format: Name, Informal Name, Phone.'"
          - check: "Error messages updated"
            status: pass
            detail: |
              INVALID_HEADER: 'CSV must have at least 3 columns: Nome, Nome Informal, Telefone Completo'
              INSUFFICIENT_COLUMNS: 'Row must have at least 3 columns'
          - check: "Tests updated for 3-column enforcement"
            status: pass
            detail: |
              26 tests in csvUtils.test.ts. Key new tests:
              - 'rejects 2-column CSV (requires 3 columns)' -> expects INVALID_HEADER
              - 'rejects row with only 2 columns (INSUFFICIENT_COLUMNS)' -> expects INSUFFICIENT_COLUMNS
              - 'rejects 1-column header CSV' -> expects INVALID_HEADER
              - 'defaults informal name to first word of full name when empty'
        notes: |
          SPEC_CONSOLIDATED F007 and BR021 both updated to 3-column only.
          Code, tests, and docs are all consistent.

      # -----------------------------------------------------------------------
      # CR-256: manage_prayers toggle access
      # -----------------------------------------------------------------------
      - cr: CR-256
        title: "Allow secretary to toggle manage_prayers"
        verdict: pass
        verification:
          - check: "Guard changed from {isBishopric && ( to {!isObserver && ("
            status: pass
            file: src/app/(tabs)/settings/index.tsx
            detail: "Line 222: {!isObserver && ("
          - check: "isBishopric variable removed (no longer used)"
            status: pass
            detail: "const isBishopric = role === 'bishopric' was removed from the component."
          - check: "Consistent with parent Ward Settings guard"
            status: pass
            detail: "Parent group (line 209) also uses {!isObserver && (}, so inner toggle is consistent."
          - check: "Integration tests added for all 3 roles"
            status: pass
            detail: |
              Tests in permissions-roles.integration.test.ts:
              - bishopric: manage_prayers toggle visible
              - secretary: manage_prayers toggle visible
              - observer: manage_prayers toggle hidden
        notes: |
          SPEC_CONSOLIDATED F018 and F027 both updated to say "bishopric or secretary".
          RLS policy already allows any authenticated user with ward_id match to
          UPDATE the wards table, so no DB changes needed. UI-only fix.

      # -----------------------------------------------------------------------
      # CR-257: Cron interval comment
      # -----------------------------------------------------------------------
      - cr: CR-257
        title: "Correct cron interval comment to 5 minutes"
        verdict: pass
        verification:
          - check: "Code comment updated from '1 minute' to '5 minutes'"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Line 2: 'process-notifications: Supabase Edge Function (cron every 5 minutes).'"
          - check: "ARCH_CONSOLIDATED updated"
            status: pass
            file: docs/arch/ARCH_CONSOLIDATED.yaml
            detail: "Line 706: 'trigger: Cron (every 5 minutes)'"
          - check: "SPEC_CONSOLIDATED F034 updated"
            status: pass
            detail: "F034 description now says 'cron every 5 minutes'"
        notes: "Doc and comment-only change. No runtime behavior affected."

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_to_arch:
        status: pass
        detail: |
          SPEC_F001 defines 6 corrections (COR-001 through COR-006).
          ARCH_M001 provides architecture assessment for all 6, confirming
          'architecture_impact: none' for all. Modules and files match.

      arch_to_code:
        status: pass
        detail: |
          All files listed in ARCH_M001 were changed in the commits:
          - send-reset-email/index.ts (CR-251)
          - create-invitation/index.ts (CR-252)
          - csvUtils.ts + csvUtils.test.ts (CR-254)
          - settings/index.tsx (CR-256)
          - process-notifications/index.ts (CR-257)
          No extra files changed beyond what ARCH specified.

      spec_consolidated_updates:
        status: pass
        detail: |
          All 8 SPEC_CONSOLIDATED corrections verified in-place:
          - F001: "selected role (bishopric or secretary)" in acceptance criteria
          - F002: "en (en-US)" fallback in description
          - F003: No "diagnostic mode" mention
          - F007: "3-column (Name, Informal Name, Phone) format" only
          - BR021: "3-column format only" with informal name default
          - F018: "bishopric or secretary" in description and acceptance criteria
          - F027: "bishopric or secretary" in description and acceptance criteria
          - F034: "cron every 5 minutes" in description

      arch_consolidated_updates:
        status: pass
        detail: |
          ARCH_CONSOLIDATED process-notifications trigger updated to
          "Cron (every 5 minutes)". Confirmed at line 706.

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 589
      total_files: 30
      all_passing: true
      new_tests:
        - file: src/__tests__/csvUtils.test.ts
          count: 26
          covers: "CR-254 (3-column enforcement, 2-column rejection, informal name defaults)"
        - file: src/__tests__/integration/permissions-roles.integration.test.ts
          covers: "CR-256 (manage_prayers toggle visibility for all 3 roles)"
      coverage_gaps:
        - cr: CR-251
          reason: "Edge Function (Deno runtime) cannot be imported in vitest Node environment"
          risk: low
          mitigation: "Manual verification of all 4 fallback changes via code review"
        - cr: CR-252
          reason: "Edge Function (Deno runtime) - dead code removal"
          risk: low
          mitigation: "grep confirms zero references to 'diagnos' in the file"
        - cr: CR-257
          reason: "Comment-only change, no behavioral code"
          risk: none

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Pre-existing CR-208 reference in create-invitation comment"
        description: |
          Line 5 of create-invitation/index.ts contains:
          "// F143 (CR-208): Structured error logging and error codes."
          This CR-208 reference predates Phase 1 and was not in scope for this
          correction package. The coder correctly updated the text (removed
          "and diagnostic mode") but preserved the CR-208 reference.
          This should be cleaned up in a future pass if CR references are
          being eliminated from source code.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - cr: CR-251
        command: "supabase functions deploy send-reset-email --no-verify-jwt"
        reason: "Edge Function code changed (language fallback)"
        urgency: low
        notes: "Functional change: default email language will be English instead of Portuguese"

      - cr: CR-252
        command: "supabase functions deploy create-invitation"
        reason: "Edge Function code changed (diagnostic mode removed)"
        urgency: low
        notes: "Dead code removal only. No behavioral change for clients."

  # ===========================================================================
  # REV-004: Phase 2 - Language Code Standardization (CR-249)
  # ===========================================================================

  - id: REV-004
    scope: "F040 Phase 2 - Language Code Standardization (CR-249)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F040
    arch: M040
    plan: P002
    iteration: 1
    commits:
      - hash: b0ed8d3
        step: STEP-01
        message: "refactor(i18n): rename locale files and update i18n module to en-US/es-LA codes (CR-249)"
      - hash: 635a850
        step: STEP-02
        message: "refactor(dateUtils): update Record keys and switch cases to en-US/es-LA (CR-249)"
      - hash: 2d7183b
        step: STEP-03
        message: "refactor(whatsappUtils): update switch cases to en-US/es-LA (CR-249)"
      - hash: 08e17e4
        step: STEP-04
        message: "refactor(notificationUtils): update types and Record keys to en-US/es-LA (CR-249)"
      - hash: 69cf1cd
        step: STEP-05
        message: "refactor(edge-functions): update language codes to en-US/es-LA in all 3 Edge Functions (CR-249)"
      - hash: eada216
        step: STEP-06
        message: "refactor(import-hymns): update LANGUAGE_MAP and create migration 024 for language codes (CR-249)"
      - hash: e0ebda8
        step: STEP-07
        message: "test(i18n): update all test files to use en-US/es-LA language codes (CR-249)"
      - hash: bcaedfc
        step: STEP-11
        message: "refactor(i18n): update remaining old language codes in speechUtils and integration tests (CR-249)"
    verdict: approved

    summary: |
      All 8 commits for Phase 2 (CR-249) correctly standardize language codes from
      en/es to en-US/es-LA across the entire codebase. Chain validation passes:
      SPEC_F002 -> ARCH_M002 -> PLAN_P002 -> CODE is fully aligned. All 644 tests
      pass (31 test files, 0 failures). The coder appropriately added STEP-11
      (not in original plan) to catch remaining old codes in speechUtils.ts and
      integration tests. Database migration 024 is idempotent and correct.
      Documentation (SPEC_CONSOLIDATED, ARCH_CONSOLIDATED) updated consistently.
      No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested "Padronizar codigos de idioma: en->en-US, es->es-LA em docs e codigo".
          SPEC_F002 covers all areas: i18n config, date/whatsapp/notification utils,
          3 Edge Functions, import script, DB migration, tests, and docs. 33 acceptance
          criteria defined, comprehensive and faithful to user intent.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M002 maps each SPEC change to an architectural component (C1-C11).
          All 15 SPEC changes have a corresponding component. ADR-007 update,
          deploy order, and risk analysis are all sound. Architecture impact
          correctly assessed as "moderate" (cross-cutting but mechanical).

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P002 has 10 steps covering all 11 ARCH components. Dependency graph
          is correct: STEP-01 first (source of truth), then utilities and Edge
          Functions in parallel, tests last. Validation matrix maps all 33 ACs to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 10 plan steps implemented in 8 commits (some steps combined).
          Additional STEP-11 added by coder to fix previously-missed old codes
          in speechUtils.ts and integration tests. This deviation is justified
          since the PLAN did not anticipate these files but the coder correctly
          caught them during implementation. Files modified match PLAN expectations
          plus the additional speechUtils.ts and 5 integration test files.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION
    # =========================================================================

    step_reviews:

      - step: STEP-01
        title: "Translation file renames + i18n module"
        verdict: pass
        verification:
          - check: "en.json renamed to en-US.json"
            status: pass
            detail: "src/i18n/locales/ contains en-US.json, es-LA.json, pt-BR.json. No en.json or es.json."
          - check: "SUPPORTED_LANGUAGES updated"
            status: pass
            detail: "['pt-BR', 'en-US', 'es-LA'] as const"
          - check: "DB_LOCALE_MAP is identity"
            status: pass
            detail: "{ 'pt-BR': 'pt-BR', 'en-US': 'en-US', 'es-LA': 'es-LA' }"
          - check: "detectDeviceLocale returns en-US/es-LA for prefix match"
            status: pass
            detail: "langPrefix === 'en' -> 'en-US', langPrefix === 'es' -> 'es-LA'"
          - check: "resources use new keys"
            status: pass
            detail: "'en-US': { translation: enUS }, 'es-LA': { translation: esLA }"
          - check: "All exports correct"
            status: pass
            detail: "LANGUAGE_LABELS, DEFAULT_COUNTRY_CODES, DEFAULT_TIMEZONES all keyed by en-US/es-LA"

      - step: STEP-02
        title: "dateUtils Record keys and switch cases"
        verdict: pass
        verification:
          - check: "MONTH_ABBR, MONTH_FULL, DAY_NAMES keys"
            status: pass
            detail: "All use 'en-US' and 'es-LA' keys. No bare 'en' or 'es'."
          - check: "formatDate switch case"
            status: pass
            detail: "case 'en-US': month day format. es-LA falls through to default (day month)."
          - check: "formatDateHumanReadable switch case"
            status: pass
            detail: "case 'en-US': Month Day, Year. es-LA falls through to default."
          - check: "formatFullDate switch case"
            status: pass
            detail: "case 'en-US': DayName, Month Day, Year. es-LA falls through to default."

      - step: STEP-03
        title: "whatsappUtils switch cases"
        verdict: pass
        verification:
          - check: "getDefaultSpeechTemplate switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA' in all 3 position blocks."
          - check: "getDefaultPrayerTemplate switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA' in both opening/closing blocks."
          - check: "grep for case 'en' and case 'es' returns 0"
            status: pass
            detail: "No standalone 'en' or 'es' switch cases remain."

      - step: STEP-04
        title: "notificationUtils types and Record keys"
        verdict: pass
        verification:
          - check: "OrdinalLanguage type"
            status: pass
            detail: "'pt-BR' | 'en-US' | 'es-LA'"
          - check: "ORDINALS Record keys"
            status: pass
            detail: "Keys are 'pt-BR', 'en-US', 'es-LA'"
          - check: "formatNameList comparisons"
            status: pass
            detail: "language === 'en-US' ? ' and ' : language === 'es-LA' ? ' y ' : ' e '"
          - check: "getPrayerLabel keys"
            status: pass
            detail: "Record keys 'pt-BR', 'en-US', 'es-LA'"
          - check: "buildNotificationText top-level Record keys"
            status: pass
            detail: "All 3 language blocks use 'pt-BR', 'en-US', 'es-LA'"

      - step: STEP-05
        title: "Edge Functions (register-first-user, process-notifications, send-reset-email)"
        verdict: pass
        verification:
          - check: "register-first-user switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA'. No bare 'en' or 'es'."
          - check: "process-notifications ORDINALS, getPrayerLabel, formatNameList, all text builders"
            status: pass
            detail: "All Record keys use 'en-US' and 'es-LA'. Fallbacks remain 'pt-BR'."
          - check: "send-reset-email template keys"
            status: pass
            detail: "Templates Record: 'pt-BR', 'en-US', 'es-LA'. Fallback: templates['en-US']. Default: let language = 'en-US'. Ward fallback: ward?.language ?? 'en-US'."

      - step: STEP-06
        title: "Import hymns script + DB migration"
        verdict: pass
        verification:
          - check: "LANGUAGE_MAP values"
            status: pass
            detail: "{ 'PT-BR': 'pt-BR', 'EN-US': 'en-US', 'ES-ES': 'es-LA' }"
          - check: "Migration file exists"
            status: pass
            detail: "supabase/migrations/024_language_code_update.sql"
          - check: "Migration SQL correct"
            status: pass
            detail: |
              UPDATE hymns SET language = 'es-LA' WHERE language = 'es-ES';
              UPDATE general_collections SET language = 'es-LA' WHERE language = 'es-ES';
              UPDATE wards SET language = 'en-US' WHERE language = 'en';
              UPDATE wards SET language = 'es-LA' WHERE language = 'es';
          - check: "Migration is idempotent"
            status: pass
            detail: "WHERE clauses prevent double-update. Running twice is safe."

      - step: STEP-07
        title: "Test file updates"
        verdict: pass
        verification:
          - check: "i18n-module.test.ts updated"
            status: pass
          - check: "whatsapp-utils.test.ts updated"
            status: pass
          - check: "dateUtils.test.ts updated"
            status: pass
          - check: "i18n.test.ts imports updated"
            status: pass
          - check: "useNotifications-utils.test.ts updated"
            status: pass
          - check: "useTopics-utils.test.ts updated"
            status: pass
          - check: "All 644 tests pass"
            status: pass
            detail: "31 test files, 644 tests, 0 failures."

      - step: STEP-11 (additional, not in original plan)
        title: "speechUtils and integration tests"
        verdict: pass
        verification:
          - check: "speechUtils.ts type signatures updated"
            status: pass
            detail: "formatDateFn type changed from 'pt-BR' | 'en' | 'es' to 'pt-BR' | 'en-US' | 'es-LA'"
          - check: "Integration tests updated"
            status: pass
            detail: |
              5 integration test files and useHymns-utils.test.ts updated
              to use en-US/es-LA instead of en/es.

    # =========================================================================
    # DOC UPDATES VERIFICATION
    # =========================================================================

    doc_updates:
      spec_consolidated:
        status: pass
        detail: |
          - product_description: "pt-BR, en-US, es-LA" (correct)
          - F008: "locale codes: pt-BR, en-US, es-LA" (correct)
          - F028: "Supported languages: pt-BR, en-US, es-LA" (correct)
          - F040 feature entry exists (correct)
          - total_features: 39 (correct, was 38 + F040)
          - No old 'en' or 'es' standalone language codes remain

      arch_consolidated:
        status: pass
        detail: |
          - tech_stack.i18n.languages: ["pt-BR", "en-US", "es-LA"] (correct)
          - total_migrations: 24 (correct)
          - ADR-007: note about unified codes after CR-249 (correct)

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Pre-existing CR references in register-first-user comments"
        description: |
          Lines 90-91 of supabase/functions/register-first-user/index.ts contain:
          "// CR-231: Per-position default speech templates (3 per language)"
          "// CR-248: Add {nome} placeholder, update text, add prayer templates"
          These CR references predate Phase 2 and were not in scope for CR-249.
          Should be cleaned up in a future pass if CR references are being
          eliminated from source code.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration"
        command: "supabase db push (or apply 024_language_code_update.sql)"
        reason: "DB data must use new codes before app/functions read them"
        urgency: medium
        notes: "Must run BEFORE Edge Functions and app update. Migration is idempotent."

      - step: 2
        action: "Deploy Edge Functions"
        commands:
          - "supabase functions deploy register-first-user"
          - "supabase functions deploy process-notifications"
          - "supabase functions deploy send-reset-email --no-verify-jwt"
        reason: "Edge Functions must handle new language codes from DB"
        urgency: medium
        notes: "Deploy after migration. All 3 functions have updated language code keys."

      - step: 3
        action: "Deploy app update"
        reason: "App sends new codes and expects new codes from DB"
        urgency: medium
        notes: "Deploy after migration and Edge Functions."

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files: []

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 20
      total_commits: 8
      tests_total: 644
      tests_passing: 644
      test_files: 31

  # ===========================================================================
  # REV-005: Phase 3 - Permission Model + Actor Role Enum (CR-253, CR-255)
  # ===========================================================================

  - id: REV-005
    scope: "F041/F042 Phase 3 - Permission Model Rework (CR-253) + Actor Single Role Enum (CR-255)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F041, F042
    arch: M005
    plan: P003
    iteration: 1
    commits:
      # CR-253 (F041): Permission Model Rework
      - hash: f8c9c02
        step: STEP-01
        message: "feat(permissions): add prayer:assign/prayer:unassign and update secretary permissions (CR-253)"
      - hash: 31f2cb7
        step: STEP-02
        message: "feat(SpeechSlot): make canAssign/canUnassign position-aware for prayer vs speech (CR-253)"
      - hash: 3ba7280
        step: STEP-03
        message: "test(permissions): update permission matrix tests for prayer permissions and secretary changes (CR-253)"
      - hash: a4d6311
        step: STEP-11a
        message: "test(integration): update permissions-roles tests for secretary prayer permissions (CR-253)"
      # CR-255 (F042): Actor Single Role Enum
      - hash: 5403ddf
        step: STEP-04
        message: "refactor(types): add ActorRole type and replace boolean flags with single role field (CR-255)"
      - hash: 14f3b78
        step: STEP-05
        message: "refactor(useActors): replace boolean flags with single role field and remove enforceActorRules (CR-255)"
      - hash: 6f30f03
        step: STEP-06
        message: "refactor(ActorSelector): use single role field in handleAdd (CR-255)"
      - hash: bf7c770
        step: STEP-07
        message: "refactor(AgendaForm): update roleFilter values from can_X to direct role names (CR-255)"
      - hash: 5aaeba3
        step: STEP-08
        message: "test(useActors): update tests for single role field and remove enforceActorRules tests (CR-255)"
      - hash: e4aaa21
        step: STEP-09
        message: "feat(migration): add migration 025 to replace actor boolean flags with single role enum (CR-255)"
      - hash: eeb4bb9
        step: STEP-11b
        message: "refactor(tests): update remaining actor boolean references to single role field (CR-255)"
      # Documentation
      - hash: 5a9eab9
        step: STEP-10
        message: "docs(phase-3): add Phase 3 architecture, spec, plan, and QA docs (CR-253, CR-255)"
    verdict: approved

    summary: |
      All 12 commits for Phase 3 (CR-253, CR-255) correctly implement the permission
      model rework and actor single role enum refactoring. Chain validation passes:
      SPEC -> ARCH -> PLAN -> CODE is fully aligned. All 703 tests pass (32 test files,
      0 failures). One P2 documentation issue found: SPEC_F003 and SPEC_CONSOLIDATED
      BR009 have incorrect permission counts (say 25/22 but should be 26/23). The code
      and tests are correct with 26/23. This is a cosmetic doc error that does not
      block approval. No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested two changes:
          1. CR-253: Fix secretary/observer permissions - secretary cannot assign speakers
             for pos 1-3 but CAN for prayers (pos 0,4), CAN change status. Topics read-only.
          2. CR-255: Actor role as single enum (preside, conduct, recognize, pianist, conductor).
          SPEC_F003 covers all CR-253 requirements: new permissions prayer:assign/prayer:unassign,
          secretary restriction from speech:assign/speech:unassign, position-aware SpeechSlot.
          SPEC_F003 covers all CR-255 requirements: ActorRole type, single role field,
          migration 025, type changes, filtering changes.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M003 maps each SPEC change to architectural components:
          F041: C1 (types), C2 (permissions.ts), C3 (SpeechSlot.tsx) - 3 components.
          F042: C4 (types), C5 (useActors.ts), C6 (ActorSelector.tsx),
                C7 (AgendaForm.tsx), C8 (migration 025) - 5 components.
          ADR-015 (position-based permission branching) and ADR-016 (single role enum)
          properly document architectural decisions. Deploy order is correct
          (migration 025 before app update).

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P003 has 11 steps covering all 8 ARCH components plus tests and docs.
          Dependency graph is correct: F041 types first (STEP-01), then SpeechSlot
          (STEP-02), then F042 types (STEP-04) through components (STEP-05 to STEP-07),
          tests (STEP-03, STEP-08), migration (STEP-09), docs (STEP-10), final
          verification (STEP-11). Validation matrix maps all ACs to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 11 plan steps implemented in 12 commits (STEP-11 split into 11a and 11b
          for CR-253 and CR-255 respectively). Files modified match PLAN expectations
          exactly. Additional files in STEP-11b (activityLog.ts, database-types.test.ts,
          setup-integration.ts, hooks-supabase.integration.test.ts) are justified
          catch-up changes for remaining boolean references. The coder correctly
          preserved backward compatibility in activityLog.ts for old can_X format
          entries.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION: CR-253 (F041) - Permission Model Rework
    # =========================================================================

    step_reviews_cr253:

      - step: STEP-01
        title: "Add prayer permissions to types and permission matrix"
        verdict: pass
        verification:
          - check: "Permission type has prayer:assign and prayer:unassign"
            status: pass
            detail: "Lines 14-15 of database.ts: | 'prayer:assign' | 'prayer:unassign'"
          - check: "bishopric set has 26 permissions including prayer:*"
            status: pass
            detail: "Counted 26 entries in bishopric Set. Includes prayer:assign and prayer:unassign."
          - check: "secretary set has 23 permissions (no speech:assign/unassign, has prayer:*)"
            status: pass
            detail: |
              Counted 23 entries in secretary Set. Does NOT contain speech:assign or
              speech:unassign. Contains prayer:assign and prayer:unassign.
          - check: "observer set unchanged at 3 permissions"
            status: pass
            detail: "member:read, agenda:read, presentation:start. No prayer permissions."
          - check: "ALL_PERMISSIONS has 26 entries"
            status: pass
            detail: "Counted 26 entries in ALL_PERMISSIONS array. Includes both prayer:* entries."

      - step: STEP-02
        title: "SpeechSlot position-aware permission ternaries"
        verdict: pass
        verification:
          - check: "canAssign uses position-aware ternary"
            status: pass
            detail: "Line 96: const canAssign = hasPermission(isPrayer ? 'prayer:assign' : 'speech:assign');"
          - check: "canUnassign uses position-aware ternary"
            status: pass
            detail: "Line 97: const canUnassign = hasPermission(isPrayer ? 'prayer:unassign' : 'speech:unassign');"
          - check: "canChangeStatus is NOT position-aware (unchanged)"
            status: pass
            detail: "Line 98: const canChangeStatus = hasPermission('speech:change_status'); -- no ternary, applies to all positions"
          - check: "isPrayer prop used correctly"
            status: pass
            detail: "isPrayer defaults to false (line 88). Positions 0,4 have isPrayer=true from parent."
          - check: "Topic row gated by same canAssign"
            status: pass
            detail: "Topic press handler (line 115) checks canAssign. Topic X button (line 286) checks canAssign. Both use same position-aware value."
          - check: "Speaker row gated by canAssign/canUnassign"
            status: pass
            detail: "Speaker press (line 110) checks canAssign. Chevron (line 244) checks canAssign. X button (line 249) checks canUnassign."

      - step: STEP-03
        title: "Permission matrix tests"
        verdict: pass
        verification:
          - check: "EXPECTED_MATRIX has 26 rows"
            status: pass
            detail: "Matrix includes prayer:assign and prayer:unassign with correct values."
          - check: "speech:assign secretary = false"
            status: pass
            detail: "Line 15: 'speech:assign': { bishopric: true, secretary: false, observer: false }"
          - check: "speech:unassign secretary = false"
            status: pass
            detail: "Line 16: 'speech:unassign': { bishopric: true, secretary: false, observer: false }"
          - check: "prayer:assign secretary = true"
            status: pass
            detail: "Line 18: 'prayer:assign': { bishopric: true, secretary: true, observer: false }"
          - check: "prayer:unassign secretary = true"
            status: pass
            detail: "Line 19: 'prayer:unassign': { bishopric: true, secretary: true, observer: false }"
          - check: "Bishopric count test expects 26"
            status: pass
            detail: "Line 127: expect(perms.length).toBe(26)"
          - check: "Secretary count test expects 23"
            status: pass
            detail: "Line 131: expect(perms.length).toBe(23)"
          - check: "ALL_PERMISSIONS count test expects 26"
            status: pass
            detail: "Line 151: expect(ALL_PERMISSIONS.length).toBe(26)"

      - step: STEP-11a
        title: "Integration tests for secretary prayer permissions"
        verdict: pass
        verification:
          - check: "Integration test file updated"
            status: pass
            detail: "permissions-roles.integration.test.ts updated with prayer permission assertions"

    # =========================================================================
    # PER-STEP VERIFICATION: CR-255 (F042) - Actor Single Role Enum
    # =========================================================================

    step_reviews_cr255:

      - step: STEP-04
        title: "ActorRole type and interface changes"
        verdict: pass
        verification:
          - check: "ActorRole type defined"
            status: pass
            detail: "Line 157: export type ActorRole = 'preside' | 'conduct' | 'recognize' | 'pianist' | 'conductor';"
          - check: "MeetingActor has single role field"
            status: pass
            detail: "Line 163: role: ActorRole (no boolean fields)"
          - check: "CreateActorInput has required role"
            status: pass
            detail: "Line 322: role: ActorRole (not optional)"
          - check: "UpdateActorInput has optional role"
            status: pass
            detail: "Line 328: role?: ActorRole"
          - check: "No boolean role fields remain in any interface"
            status: pass
            detail: "grep for can_preside in database.ts returns 0 matches"

      - step: STEP-05
        title: "useActors hook refactor"
        verdict: pass
        verification:
          - check: "ActorRoleFilter uses new values"
            status: pass
            detail: "'all' | 'preside' | 'conduct' | 'recognize' | 'pianist' | 'conductor' (no can_ prefix)"
          - check: "enforceActorRules removed"
            status: pass
            detail: "grep for enforceActorRules in useActors.ts returns 0 matches"
          - check: "filterActorsByRole uses a.role === role"
            status: pass
            detail: "Line 38: return actors.filter((a) => a.role === role);"
          - check: "getPrimaryRole returns actor.role directly"
            status: pass
            detail: "Line 54: return actor.role;"
          - check: "useActors query filters by role column"
            status: pass
            detail: "Line 76: query = query.eq('role', roleFilter);"
          - check: "useCreateActor inserts { ward_id, name, role }"
            status: pass
            detail: "Lines 97-99: insert({ ward_id: wardId, name: input.name, role: input.role })"

      - step: STEP-06
        title: "ActorSelector handleAdd update"
        verdict: pass
        verification:
          - check: "handleAdd uses role field"
            status: pass
            detail: "Line 117-120: const input: CreateActorInput = { name: trimmed, role: roleFilter as ActorRole };"
          - check: "ActorRole imported"
            status: pass
            detail: "Line 26: import type { MeetingActor, CreateActorInput, ActorRole } from '../types/database';"

      - step: STEP-07
        title: "AgendaForm roleFilter values"
        verdict: pass
        verification:
          - check: "All 5 roleFilter values updated"
            status: pass
            detail: |
              Line 158: roleFilter: 'preside' (was 'can_preside')
              Line 179: roleFilter: 'conduct' (was 'can_conduct')
              Line 198: roleFilter: 'recognize' (was 'can_recognize')
              Line 259: roleFilter: 'pianist' (was 'can_pianist')
              Line 280: roleFilter: 'conductor' (was 'can_conductor')
          - check: "No can_ prefix remains in AgendaForm"
            status: pass
            detail: "grep for 'can_preside' in AgendaForm.tsx returns 0 matches"

      - step: STEP-08
        title: "useActors test updates"
        verdict: pass
        verification:
          - check: "makeActor uses role field"
            status: pass
            detail: "Line 14: role: overrides.role ?? 'preside' (no boolean defaults)"
          - check: "filterActorsByRole tests use new role values"
            status: pass
            detail: "All test cases use role: 'preside', role: 'conduct', etc."
          - check: "actorKeys tests use new role values"
            status: pass
            detail: "Line 94: actorKeys.byRole('ward-1', 'preside')"
          - check: "No enforceActorRules tests remain"
            status: pass
            detail: "grep for enforceActorRules in test file returns 0 matches"

      - step: STEP-09
        title: "Migration 025"
        verdict: pass
        verification:
          - check: "Adds role column with DEFAULT"
            status: pass
            detail: "Line 6: ALTER TABLE meeting_actors ADD COLUMN role TEXT NOT NULL DEFAULT 'preside';"
          - check: "Migrates data with priority order"
            status: pass
            detail: "Lines 9-16: CASE WHEN can_preside=true THEN 'preside' ... ELSE 'preside' END"
          - check: "Drops 5 boolean columns"
            status: pass
            detail: "Lines 19-24: DROP COLUMN can_preside, can_conduct, can_recognize, can_pianist, can_conductor"
          - check: "Drops old constraint"
            status: pass
            detail: "Line 27: DROP CONSTRAINT IF EXISTS chk_pianist_conductor_exclusive"
          - check: "Adds new CHECK constraint"
            status: pass
            detail: "Lines 30-31: ADD CONSTRAINT chk_actor_role CHECK (role IN (...))"
          - check: "Migration is idempotent-safe"
            status: pass
            detail: "DROP CONSTRAINT uses IF EXISTS. Second run would fail on ADD COLUMN (which is acceptable for a one-way migration)."

      - step: STEP-11b
        title: "Remaining boolean references cleanup"
        verdict: pass
        verification:
          - check: "activityLog.ts backward compatibility"
            status: pass
            detail: |
              ACTOR_ROLE_I18N_KEYS has both new (preside, conduct, etc.) and old
              (can_preside, can_conduct, etc.) keys. Both map to the same i18n keys.
              This correctly handles historical activity log entries.
          - check: "database-types.test.ts updated"
            status: pass
            detail: "MeetingActor test uses role: 'preside'. CreateActorInput test uses role: 'preside'. ActorRole type imported."
          - check: "setup-integration.ts updated"
            status: pass
            detail: "createMockActor uses role: 'preside' (no boolean fields)"
          - check: "hooks-supabase.integration.test.ts updated"
            status: pass
            detail: "Actor mock data uses role field"

    # =========================================================================
    # DOCUMENTATION UPDATES VERIFICATION
    # =========================================================================

    doc_updates:

      spec_consolidated:
        status: pass
        detail: |
          - F005: Description mentions prayer:assign/prayer:unassign, secretary limited access
          - F009: Description says single role field, CHECK constraint
          - F014: Position-aware permission note
          - F015: Secretary read-only for speech positions
          - BR009: Updated with new permission model (counts are 25/22 but code has 26/23 - see P2 issue)
          - BR014: Marked as REMOVED by CR-255
          - EC009: Marked as REMOVED by CR-255
          - F041 feature entry exists with full description
          - F042 feature entry exists with full description
          - total_features: 41 (was 39 + F041 + F042)

      arch_consolidated:
        status: pass
        detail: |
          - M004 description: "Each actor has a single role enum field" (correct)
          - M004 dependencies: CHECK constraint updated to role-based (correct)
          - meeting_actors key_columns: role TEXT column (correct)
          - meeting_actors constraints: new CHECK constraint (correct)
          - total_migrations: 25 (correct)
          - ADR-015 exists: Position-based permission branching
          - ADR-016 exists: Single role enum for meeting actors

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: P2-001
        severity: P2
        category: quality
        location: "docs/specs/SPEC_F003.yaml + docs/specs/SPEC_CONSOLIDATED.yaml"
        description: |
          Permission count mismatch between SPEC documents and actual code/tests:
          - SPEC_F003 BR009 says "Bishopric: 25 permissions" -> actual code has 26
          - SPEC_F003 BR009 says "Secretary: 22 permissions" -> actual code has 23
          - SPEC_CONSOLIDATED BR009 copies the same wrong counts (25/22)
          The code (permissions.ts) and tests (permissions.test.ts, f041-f042-phase3.test.ts)
          are consistent with each other at 26/23. The original Permission type had 24 values.
          Adding prayer:assign and prayer:unassign brings bishopric to 24+2=26.
          Secretary had 23 (all except home:next_assignments). Remove 2 + add 2 = 23.
          The SPEC incorrectly subtracted 1 from the starting counts.
        suggestion: |
          Update BR009 in both SPEC_F003.yaml and SPEC_CONSOLIDATED.yaml:
          - Bishopric: 25 -> 26
          - Secretary: 22 -> 23

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Backward-compatible can_ prefix in activityLog.ts"
        description: |
          activityLog.ts has ACTOR_ROLE_I18N_KEYS with both new (preside) and old
          (can_preside) keys. The old keys are backward compatibility for historical
          activity log entries stored in the database. This is by design per SPEC_F003
          decisions section. The activityLog.test.ts test at line 224 uses 'can_preside'
          to verify backward compatibility works. This is correct behavior.

      - id: ADV-002
        severity: P2
        title: "SPEC_F003 PLAN step count in done_when has arithmetic notes"
        description: |
          PLAN_P003 STEP-01 done_when has a note "old had 23 including speech:assign/unassign;
          remove 2, add 2 = 23... wait: old had 23..." showing the planner was confused about
          the count. The final plan values are inconsistent but the coder implemented the
          correct counts (26/23). Cosmetic plan artifact.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 025"
        command: "supabase db push (or apply 025_actor_single_role.sql)"
        reason: "DB must have 'role' column before app tries to query it"
        urgency: medium
        notes: |
          Must run BEFORE app update. Old app will break if migration runs first
          (queries can_preside column that no longer exists). Deploy sequence:
          migration -> app update with minimal gap. Migration is one-way
          (boolean-to-enum).

      - step: 2
        action: "Deploy app update"
        reason: "All TypeScript changes (permissions, SpeechSlot, actors) go live together"
        urgency: medium
        notes: |
          F041 (permission changes) and F042 (actor refactor) both take effect with
          the app update. No Edge Functions affected by Phase 3.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 16
      total_commits: 12
      tests_total: 703
      tests_passing: 703
      test_files: 32

  # ===========================================================================
  # REV-006: Phase 004 (CR-259, CR-260, CR-261) - Quick UX Fixes
  # ===========================================================================

  - id: REV-006
    scope: "Phase 004: CR-259 (Play Icon), CR-260 (Remove Agenda History), CR-261 (User Language from Ward)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: 7fb10dc
        message: "feat(agenda): restyle play button to filled primary container (CR-259)"
      - hash: 9d1523b
        message: "refactor(useAgenda): remove agenda:edit logAction call and unused imports (CR-260)"
      - hash: 63b114a
        message: "refactor(i18n): remove agenda-related activity log keys from all locales (CR-260)"
      - hash: 83fbe83
        message: "feat(migration): add migration 026 to delete agenda activity log entries (CR-260)"
      - hash: 3e5a932
        message: "feat(register-first-user): set user_metadata.language to ward language (CR-261)"
      - hash: 1e50b40
        message: "feat(register-invited-user): fetch ward language and set user_metadata.language (CR-261)"
    verdict: approved

    summary: |
      All 6 committed code changes across 3 CRs are correct, well-structured, and
      align with SPEC, ARCH, and PLAN. Chain validation passes on all 4 links. The
      implementation is minimal and focused. No P0 or P1 issues found. One P2
      advisory for pre-existing dead code in useAgenda.ts (not introduced by this phase).

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request: pass
      spec_vs_request_notes: |
        SPEC F043 covers CR-259 (play icon redesign) with 4 ACs.
        SPEC F044 covers CR-260 (remove agenda history) with 4 ACs.
        SPEC F045 covers CR-261 (user language from ward) with 5 ACs.
        All user requirements are covered. No scope creep.

      arch_vs_spec: pass
      arch_vs_spec_notes: |
        ARCH M004 defines 6 components (C1-C6) that map to all SPEC ACs.
        C1 for F043 (agenda.tsx style change).
        C2-C4 for F044 (logAction removal, i18n cleanup, migration).
        C5-C6 for F045 (register-first-user, register-invited-user).
        All components align with SPEC. No extra components.

      plan_vs_arch: pass
      plan_vs_arch_notes: |
        PLAN P004 has 7 steps (STEP-01 to STEP-07).
        STEP-01 implements C1, STEP-02 implements C2, STEP-03 implements C3,
        STEP-04 implements C4, STEP-05 implements C5, STEP-06 implements C6,
        STEP-07 is verification. All steps match ARCH components.

      code_vs_plan: pass
      code_vs_plan_notes: |
        6 commits match STEP-01 through STEP-06. Each commit modifies
        exactly the files specified in the PLAN. Implementation details
        match the PLAN's done_when criteria. No extra changes.

      chain_intact: true

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:

      - id: R6-1
        severity: P2
        category: quality
        location: "src/hooks/useAgenda.ts:164"
        description: |
          Pre-existing dead code: useUpdateAgendaByDate destructures
          `user` and `userName` from useAuth() but never uses them.
          This was NOT introduced by CR-260 (the function never had a
          logAction call), so it is pre-existing technical debt.
        suggestion: |
          Clean up in a future refactoring pass: change line 164 from
          `const { wardId, user, userName } = useAuth()` to
          `const { wardId } = useAuth()`.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 026_delete_agenda_activity_log.sql"
        command: "supabase db push (or apply 026_delete_agenda_activity_log.sql)"
        reason: "Delete existing agenda-related activity_log entries per CR-260"
        urgency: low
        notes: |
          Data-only migration (DELETE, no schema changes). Safe to run before
          or after app deploy. Safe on empty tables.

      - step: 2
        action: "Deploy Edge Functions (register-first-user, register-invited-user)"
        command: "supabase functions deploy register-first-user && supabase functions deploy register-invited-user"
        reason: "CR-261: new users get user_metadata.language set from ward language"
        urgency: low
        notes: |
          Edge Functions deploy independently. New users created after deploy
          will have user_metadata.language set. Existing users are unaffected.

      - step: 3
        action: "Deploy app update"
        reason: "CR-259 (play button style) and CR-260 (remove logAction + i18n keys)"
        urgency: low
        notes: |
          All three deploy steps are independent and can happen in any order.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M004.yaml"
        - "docs/plans/PLAN_P004.yaml"
        - "docs/qa/QA_PLAN_004.yaml"
        - "docs/specs/SPEC_F004.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 8
      total_commits: 6
      tests_total: 776
      tests_passing: 776
      test_files: 33

  # ===========================================================================
  # REV-005: Phase 5 - CR-258 Speech Count per Member (Last 6 Months)
  # ===========================================================================

  - id: REV-005
    scope: "CR-258 - Speech Count per Member (Last 6 Months)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    phase: 5
    iteration: 1
    feature: F046
    commits:
      - hash: f68be6d
        step: STEP-01
        message: "feat(migration): add partial index for speech count query (CR-258)"
      - hash: 0534e7d
        step: STEP-02
        message: "feat(i18n): add speechCount singular/plural keys in 3 languages (CR-258)"
      - hash: fc64aff
        step: STEP-03
        message: "feat(useSpeechCounts): add bulk speech count hook with client-side aggregation (CR-258)"
      - hash: f899e8e
        step: STEP-04
        message: "feat(sync): invalidate speechCounts on speech table changes (CR-258)"
      - hash: c84e991
        step: STEP-05
        message: "feat(members): display speech count below member info in member list (CR-258)"
      - hash: cf4c358
        step: STEP-06
        message: "feat(MemberSelectorModal): display speech count below member name in selector (CR-258)"
      - hash: eab3ed1
        step: STEP-07
        message: "test(sync): update sync test to expect speechCounts in speeches query keys (CR-258)"
    verdict: approved

    summary: |
      CR-258 (Speech Count per Member) is fully implemented with 7 commits
      following the PLAN exactly. Chain validation passes on all 4 levels.
      The implementation creates a new useSpeechCounts hook with bulk query
      and client-side aggregation, adds a partial DB index, displays counts
      in MemberRow and MemberSelectorModal, adds i18n keys in 3 languages
      with singular/plural support, and updates sync.ts for realtime
      invalidation. All 794 tests pass (18 new for F046). No P0 or P1 issues
      found. One migration (027) needs deployment.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        verdict: pass
        notes: |
          CR-258 request: "Show speech count in last 6 months per member. Display
          in (1) member list in settings, (2) assignment speaker list. Text:
          'X discursos nos ultimos 6 meses'. Hide if zero. Smart DB strategy
          to avoid excessive queries."

          SPEC_F005 F046 covers ALL of these: 12 acceptance criteria mapping to
          the two display locations, hide-if-zero, singular/plural text, bulk
          query strategy, 6-month window, prayer exclusion, index optimization,
          i18n in 3 languages, all roles visibility, and query invalidation.

      arch_vs_spec:
        verdict: pass
        notes: |
          ARCH_M005 maps directly to SPEC_F005. 6 components (C1-C6) cover
          all ACs: C1 (useSpeechCounts hook), C2 (MemberRow), C3 (MemberSelectorModal),
          C4 (migration), C5 (i18n), C6 (sync). ADR-017 (client-side aggregation)
          and ADR-018 (dedicated query key) are sound. Dependency graph and
          implementation order are correct.

      plan_vs_arch:
        verdict: pass
        notes: |
          PLAN_P005 has 7 steps mapping 1:1 to ARCH components plus a
          verification step. Step dependencies respect ARCH dependency graph.
          done_when criteria are specific and testable. Validation matrix covers
          all 12 ACs and 10 edge cases.

      code_vs_plan:
        verdict: pass
        notes: |
          CODE_LEDGER shows 7 commits (f68be6d through eab3ed1) matching
          PLAN steps 1-7. Each commit modifies exactly the files listed in the
          corresponding PLAN step. Code implements precisely what was specified:
          - STEP-01: migration with correct partial index syntax
          - STEP-02: i18n keys with _one/_other suffixes in all 3 locales
          - STEP-03: useSpeechCounts with exact query, Map aggregation, memoized dates
          - STEP-04: sync.ts with speechCountKeys.all added to speeches entry
          - STEP-05: MemberRow with speechCount prop, conditional rendering, style
          - STEP-06: MemberSelectorModal with inline speechCounts.get lookup
          - STEP-07: sync integration test updated

      chain_intact: true

    # =========================================================================
    # CODE REVIEW DETAILS
    # =========================================================================

    code_review:

      migration_027:
        file: supabase/migrations/027_speech_count_index.sql
        verdict: pass
        notes: |
          Correct partial composite index:
          CREATE INDEX idx_speeches_member_count
            ON speeches(ward_id, member_id, position, sunday_date)
            WHERE member_id IS NOT NULL;
          Column order is appropriate (ward_id for equality, then member_id
          for grouping, position for IN filter, sunday_date for range).
          Partial index correctly excludes NULL member_id rows.
          Comment references CR-258.

      useSpeechCounts_hook:
        file: src/hooks/useSpeechCounts.ts
        verdict: pass
        notes: |
          Clean implementation:
          - speechCountKeys factory with .all and .byWard (correct pattern)
          - cutoffDate and today memoized with useMemo (correct deps: [])
          - Query: .select('member_id').eq('ward_id', wardId).in('position', [1,2,3])
            .not('member_id', 'is', null).gte('sunday_date', cutoffDate)
            .lte('sunday_date', today) -- matches ARCH exactly
          - Client-side Map aggregation: O(n) loop, correct get/set pattern
          - enabled: !!wardId -- correct guard
          - Returns data ?? new Map() -- safe default for consumers
          - No unnecessary dependencies, clean 58-line file

      sync_ts:
        file: src/lib/sync.ts
        verdict: pass
        notes: |
          speechCountKeys imported from correct path.
          TABLE_TO_QUERY_KEYS.speeches now includes both speechKeys.all and
          speechCountKeys.all. Existing functionality unchanged.

      members_tsx:
        file: src/app/(tabs)/settings/members.tsx
        verdict: pass
        notes: |
          - useSpeechCounts imported and called in MembersScreen
          - MemberRowProps extended with speechCount: number
          - speechCount passed via speechCounts.get(item.id) ?? 0 in renderItem
          - speechCounts added to renderItem useCallback dependencies
          - Conditional rendering: speechCount > 0 && <Text ...>
          - Style: memberSpeechCount { fontSize: 12, marginTop: 2 }
          - Text uses t('members.speechCount', { count: speechCount })
          - Editing mode correctly shows MemberEditor (no speech count shown)

      member_selector_modal:
        file: src/components/MemberSelectorModal.tsx
        verdict: pass
        notes: |
          - useSpeechCounts imported and called inside MemberSelectorModal
          - Inline lookup: (speechCounts.get(item.id) ?? 0) > 0 &&
          - Same style as members.tsx (fontSize: 12, marginTop: 2)
          - Text uses same i18n key with count interpolation
          - Consistent with MemberRow implementation

      i18n_keys:
        files:
          - src/i18n/locales/pt-BR.json
          - src/i18n/locales/en-US.json
          - src/i18n/locales/es-LA.json
        verdict: pass
        notes: |
          All 3 files have identical key structure:
          - speechCount_one: singular form (count=1)
          - speechCount_other: plural form (count!=1)
          pt-BR: "discurso/discursos nos ultimos 6 meses"
          en-US: "speech/speeches in the last 6 months"
          es-LA: "discurso/discursos en los ultimos 6 meses"
          Keys are inside the "members" object. Valid JSON confirmed by
          existing i18n tests passing.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found. Implementation is clean and complete.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 027_speech_count_index.sql"
        command: "supabase db push (or apply 027_speech_count_index.sql)"
        reason: "Creates partial composite index idx_speeches_member_count for optimized speech count query"
        urgency: low
        notes: |
          Index-only migration, no schema changes. Safe to run before or after
          app deploy. The feature works without the index (just slower), so
          deploy order is flexible.

      - step: 2
        action: "Deploy app update"
        reason: "CR-258: speech count display in member list and speaker selector"
        urgency: low
        notes: |
          All code changes (hook, UI, i18n, sync) ship together. The feature
          is purely additive and read-only; no risk of breaking existing functionality.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M005.yaml"
        - "docs/plans/PLAN_P005.yaml"
        - "docs/qa/QA_PLAN_005.yaml"
        - "docs/specs/SPEC_F005.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 8
      total_commits: 7
      tests_total: 794
      tests_passing: 794
      test_files: 35

  # ===========================================================================
  # REV-006: Phase 6 - CR-262 Optimized Offline Mode (F047-F052)
  # ===========================================================================

  - id: REV-006
    scope: "CR-262 - Optimized Offline Mode (F047-F052)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: b13281a
        message: "feat(offline): add PersistQueryClientProvider for cache persistence (CR-262)"
      - hash: 12630b9
        message: "feat(offline): add OnlineStatusContext and wire into SyncProvider (CR-262)"
      - hash: 46c46d4
        message: "feat(offline): add showChevron prop to SundayCard (CR-262)"
      - hash: cfd1534
        message: "feat(offline): add disabled prop to AgendaForm and SpeechSlot (CR-262)"
      - hash: 3d5f302
        message: "feat(offline): add useOfflinePrefetch hook for proactive cache warming (CR-262)"
      - hash: 2703f51
        message: "feat(offline): wire useOfflinePrefetch into SyncProvider (CR-262)"
      - hash: 1fb7f2e
        message: "feat(offline): adapt Home tab UI for offline mode (CR-262)"
      - hash: f93a8de
        message: "feat(offline): adapt Agenda tab UI for offline mode (CR-262)"
      - hash: 13589d8
        message: "feat(offline): adapt Speeches tab UI for offline mode (CR-262)"
      - hash: 344a195
        message: "feat(offline): hide Users item in Settings when offline (CR-262)"
    verdict: approved

    summary: |
      CR-262 Optimized Offline Mode is fully implemented across 10 commits following
      the PLAN_P006 step sequence. All 6 features (F047-F052) correctly implemented:
      cache persistence via PersistQueryClientProvider, OnlineStatusContext for offline
      state distribution, proactive prefetch hook, and per-tab UI adaptation (Home,
      Agenda, Speeches, Settings). No database changes, no Edge Functions, purely
      client-side. 943 tests pass (47 test files), 12 new test files cover all ACs
      and edge cases. No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          SPEC_F006 covers all 6 features requested for offline mode: cache persistence
          (F047), proactive prefetch (F048), Home tab offline (F049), Agenda tab offline
          (F050), Speeches tab offline (F051), Settings tab offline (F052). All ACs are
          faithful to the user's request for read-only offline mode with cached data.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M006 maps 11 components (C1-C11) to the 6 features. Architecture correctly
          identifies: no new DB tables, no new migrations, no Edge Functions. Impact
          assessment is "medium" (correct - modifies layout, sync, all 4 tabs, 2 components).
          Design decisions (PersistQueryClientProvider, OnlineStatusContext, prefetchQuery)
          are sound and consistent with existing patterns.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P006 has 11 steps covering all 11 ARCH components. Dependency graph is
          correct: infrastructure steps (STEP-01 to STEP-04) are parallelizable, STEP-06
          depends on STEP-02 and STEP-05, tab UI steps (STEP-07 to STEP-10) depend on
          STEP-06. STEP-11 is final verification. Validation matrix maps all 27 ACs and
          12 edge cases to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 11 plan steps implemented in 10 commits. STEP-02 and STEP-06 were partially
          combined (OnlineStatusContext creation and SyncProvider wiring happened in 2
          commits instead of exactly matching step boundaries). This is a minor deviation
          that does not affect correctness. All files modified match PLAN expectations.
          All done_when criteria are satisfied. All tests pass.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION
    # =========================================================================

    step_reviews:

      - step: STEP-01
        title: "PersistQueryClientProvider + gcTime"
        verdict: pass
        verification:
          - check: "npm packages installed"
            status: pass
            detail: "@tanstack/query-async-storage-persister and @tanstack/react-query-persist-client in package.json"
          - check: "PersistQueryClientProvider replaces QueryClientProvider"
            status: pass
            detail: "Import from @tanstack/react-query-persist-client. QueryClientProvider removed from imports."
          - check: "asyncStoragePersister created with correct config"
            status: pass
            detail: "key: '@query_cache', throttleTime: 1000"
          - check: "gcTime set to 7 days"
            status: pass
            detail: "SEVEN_DAYS_MS = 1000 * 60 * 60 * 24 * 7 (604800000)"
          - check: "persistOptions correct"
            status: pass
            detail: "persister, maxAge: SEVEN_DAYS_MS, buster: Constants.expoConfig?.version ?? '1.0.0'"

      - step: STEP-02
        title: "OnlineStatusContext"
        verdict: pass
        verification:
          - check: "File exists at src/contexts/OnlineStatusContext.tsx"
            status: pass
          - check: "Exports OnlineStatusProvider and useOnlineStatus"
            status: pass
            detail: "OnlineStatusProvider accepts isOnline + children. useOnlineStatus returns boolean."
          - check: "Default context value is { isOnline: true }"
            status: pass

      - step: STEP-03
        title: "SundayCard showChevron prop"
        verdict: pass
        verification:
          - check: "showChevron added to SundayCardProps"
            status: pass
            detail: "showChevron?: boolean with default true"
          - check: "Chevron conditionally rendered"
            status: pass
            detail: "onPress={showChevron ? onToggle : undefined} on header Pressable"

      - step: STEP-04
        title: "AgendaForm + SpeechSlot disabled prop"
        verdict: pass
        verification:
          - check: "AgendaForm disabled prop"
            status: pass
            detail: "disabled?: boolean defaults to false. isObserver = !hasPermission('agenda:write') || disabled"
          - check: "AgendaForm propagates disabled to sub-components"
            status: pass
            detail: "DebouncedTextInput editable={!isObserver}, SelectorField disabled={isObserver}, Switch disabled={isObserver}"
          - check: "SpeechSlot disabled prop"
            status: pass
            detail: "disabled?: boolean defaults to false. canAssign, canUnassign, canChangeStatus all AND with !disabled"

      - step: STEP-05
        title: "useOfflinePrefetch hook"
        verdict: pass
        verification:
          - check: "getNext3Sundays utility"
            status: pass
            detail: "Returns 3 consecutive Sunday ISO dates starting from getTodaySundayDate()"
          - check: "Prefetches 4 queries"
            status: pass
            detail: "agenda bySunday, agenda byDateRange, speeches byDateRange, exceptions"
          - check: "Uses same query keys as tab hooks"
            status: pass
            detail: "Imports agendaKeys, speechKeys, sundayTypeKeys from respective hooks"
          - check: "Offline->online transition tracking"
            status: pass
            detail: "useRef(prevOnlineRef) tracks previous state. Only prefetches on transition."
          - check: "Each prefetchQuery independent"
            status: pass
            detail: "4 separate prefetchQuery calls, no chaining"

      - step: STEP-06
        title: "SyncProvider integration"
        verdict: pass
        verification:
          - check: "OnlineStatusProvider wraps children"
            status: pass
            detail: "<OnlineStatusProvider isOnline={isOnline}> wraps OfflineBanner + children"
          - check: "useOfflinePrefetch called"
            status: pass
            detail: "useOfflinePrefetch(isOnline) after useOfflineQueueProcessor"
          - check: "Existing hooks unchanged"
            status: pass
            detail: "useConnection, useRealtimeSync, useOfflineQueueProcessor, useRegisterPushToken, useNotificationHandler all intact"

      - step: STEP-07
        title: "Home tab offline UI"
        verdict: pass
        verification:
          - check: "useOnlineStatus imported and called"
            status: pass
          - check: "NextSundaysSection conditional"
            status: pass
            detail: "{isOnline && <NextSundaysSection />}"
          - check: "NextAssignmentsSection conditional"
            status: pass
            detail: "{isOnline && <NextAssignmentsSection />}"
          - check: "InviteManagementSection conditional"
            status: pass
            detail: "{isOnline && <InviteManagementSection />}"
          - check: "Pencil button conditional"
            status: pass
            detail: "{isOnline && (<Pressable ... pencil ... />)}"
          - check: "Start Meeting always visible"
            status: pass
            detail: "Not wrapped in isOnline conditional"

      - step: STEP-08
        title: "Agenda tab offline UI"
        verdict: pass
        verification:
          - check: "Expandable logic includes online check"
            status: pass
            detail: "isExpandable = baseExpandable && (isOnline || date === nextSunday)"
          - check: "Auto-collapse on offline"
            status: pass
            detail: "useEffect collapses if !isOnline && expandedDate !== nextSunday"
          - check: "AgendaForm disabled when offline"
            status: pass
            detail: "disabled={isOffline} passed to AgendaForm"
          - check: "SundayTypeDropdown disabled when offline"
            status: pass
            detail: "disabled={typeDisabled || isOffline}"
          - check: "lazyCreate guarded by isOnline"
            status: pass
            detail: "if (isOnline) { lazyCreate.mutate(date) }"
          - check: "Chevron uses expandable"
            status: pass
            detail: "{expandable && (isExpanded ? ChevronUp : ChevronDown)}"

      - step: STEP-09
        title: "Speeches tab offline UI"
        verdict: pass
        verification:
          - check: "offlineExpandableDates computed"
            status: pass
            detail: "useMemo(() => new Set(getNext3Sundays()), [])"
          - check: "isExpandable per card"
            status: pass
            detail: "isOnline || offlineExpandableDates.has(item.date)"
          - check: "onToggle guarded"
            status: pass
            detail: "onToggle={isExpandable ? () => handleToggle(item.date) : undefined}"
          - check: "showChevron guarded"
            status: pass
            detail: "showChevron={isExpandable}"
          - check: "SpeechSlot disabled when offline"
            status: pass
            detail: "disabled={!isOnline} on all SpeechSlot instances"
          - check: "typeDisabled includes isOnline"
            status: pass
            detail: "typeDisabled={!canWriteSundayType || !isOnline}"
          - check: "Auto-collapse on offline"
            status: pass
            detail: "useEffect: !isOnline && !offlineExpandableDates.has(expandedDate) -> collapse"
          - check: "lazyCreate guarded by isOnline"
            status: pass
            detail: "if (isOnline) { lazyCreate.mutate(...) }"

      - step: STEP-10
        title: "Settings tab offline UI"
        verdict: pass
        verification:
          - check: "Users item conditional on isOnline"
            status: pass
            detail: "{hasPermission('settings:users') && isOnline && (<SettingsItem />)}"
          - check: "Other settings not conditional on isOnline"
            status: pass
            detail: "Members, topics, whatsapp, language, timezone, manage_prayers, history, app language, theme, about - none gated by isOnline"

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-262: Offline mode is purely client-side. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native). No database migrations.
          No Edge Function changes. Ship whenever ready. New npm packages
          (@tanstack/query-async-storage-persister, @tanstack/react-query-persist-client)
          are bundled in the app binary.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M006.yaml"
        - "docs/plans/PLAN_P006.yaml"
        - "docs/qa/QA_PLAN_006.yaml"
        - "docs/specs/SPEC_F006.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 12
      total_commits: 10
      tests_total: 943
      tests_passing: 943
      test_files: 47

  # ===========================================================================
  # REV-008: Phase 7 - Web Bugfixes (CR-263, CR-264, CR-265)
  # ===========================================================================

  - id: REV-008
    scope: "CR-263, CR-264, CR-265 - Web Bugfixes (Registration Errors, ActorSelector TDZ, Hymn Selector Web)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    phase: 7
    iteration: 1

    commits:
      - hash: 648bb3c
        message: "feat(i18n): add auth.registrationFailed key to all 3 locales (CR-263)"
      - hash: a697ff9
        message: "fix(register): show friendly i18n errors instead of raw technical messages (CR-263)"
      - hash: 57eea91
        message: "fix(ActorSelector): reorder hooks to fix TDZ crash + add web overlay fix (CR-264, CR-265)"
      - hash: 4e0a1c1
        message: "fix(AgendaForm): add web overlay fix + initialNumToRender to HymnSelectorModal (CR-265)"
      - hash: 4ac585a
        message: "test(phase-7): add 57 tests for F053-F054-F055 web bugfixes (CR-263, CR-264, CR-265)"
      - hash: 7be6a15
        message: "docs(tests): update TEST_CONSOLIDATED for F053-F055 (57 new tests, 1000 total)"

    verdict: approved

    summary: |
      All three bugfixes (CR-263, CR-264, CR-265) are correctly implemented with
      minimal, focused changes. Chain validation passes completely: SPEC_F007 accurately
      captures the bugs and fixes, ARCH_M007 provides correct architectural guidance,
      PLAN_P007 orders steps properly, and the code faithfully implements each step.
      No P0 or P1 issues found. 57 new tests all pass. 1000 total tests, 999 pass
      (1 pre-existing flaky test from Phase 5 unrelated to these changes). No database
      migrations, no Edge Function changes -- pure client-side fixes. No deploy needed
      beyond the existing pending EAS Build from CR-262.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request: pass
      arch_vs_spec: pass
      plan_vs_arch: pass
      code_vs_plan: pass
      chain_intact: true

      details:
        spec_vs_request:
          status: pass
          notes: |
            SPEC_F007 accurately captures all three bugs:
            - F053 (CR-263): Root cause (FunctionsHttpError thrown before data checks),
              fix strategy (read data.error for known codes), error mapping (4 scenarios),
              new i18n key (auth.registrationFailed). 7 ACs cover all error scenarios.
            - F054 (CR-264): Root cause (TDZ due to hook declaration order), fix (reorder
              hooks). 6 ACs cover all actor roles and scroll behavior.
            - F055 (CR-265): Root cause (web click bubbling, FlatList windowed rendering),
              fix (onClick stopPropagation, initialNumToRender). 8 ACs cover web and native.

        arch_vs_spec:
          status: pass
          notes: |
            ARCH_M007 correctly identifies 5 components (C007-01 through C007-05) mapping
            to the 3 features. Architecture impact is correctly assessed as "low" (no new
            modules, hooks, providers, database tables). Two ADRs (ADR-022 for web overlay
            pattern, ADR-023 for error extraction) document the key decisions. Contracts
            are well-defined (Supabase functions.invoke return, Platform.OS onClick).

        plan_vs_arch:
          status: pass
          notes: |
            PLAN_P007 has 5 steps matching the architecture. Step ordering respects
            dependencies (STEP-01 i18n before STEP-02 register.tsx). STEP-03 correctly
            combines C007-03 and C007-05 (both modify ActorSelector.tsx). Coverage matrix
            shows all 21 ACs covered across steps.

        code_vs_plan:
          status: pass
          notes: |
            Each commit maps precisely to a PLAN step:
            - 648bb3c -> STEP-01: auth.registrationFailed in 3 locale files
            - a697ff9 -> STEP-02: register.tsx error handling refactored
            - 57eea91 -> STEP-03: ActorSelector hook reorder + web overlay
            - 4e0a1c1 -> STEP-04: HymnSelectorModal web overlay + initialNumToRender

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      - file: "src/i18n/locales/pt-BR.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "Placement is after auth.stakeWardExists (logical order)"
          - "JSON remains valid"

      - file: "src/i18n/locales/en-US.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "English text is natural and user-friendly"

      - file: "src/i18n/locales/es-LA.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "Spanish text is natural and user-friendly"

      - file: "src/app/(auth)/register.tsx"
        verdict: pass
        changes: "Refactored handleRegister() error handling"
        checks:
          - check: "No 'throw response.error' -- removed as planned"
            status: pass
            detail: "Lines 81-94: response.error is checked as boolean, then data?.error is inspected for known codes"
          - check: "email_exists mapped to t('auth.emailExists')"
            status: pass
            detail: "Line 84-87"
          - check: "stake_ward_exists mapped to t('auth.stakeWardExists')"
            status: pass
            detail: "Line 88-91"
          - check: "Fallback for unknown non-2xx errors: t('auth.registrationFailed')"
            status: pass
            detail: "Line 92-93"
          - check: "Success path unchanged (data?.session)"
            status: pass
            detail: "Lines 97-105"
          - check: "Catch block: network errors -> t('auth.requiresConnection')"
            status: pass
            detail: "Line 108-109"
          - check: "Catch block: non-network errors -> t('auth.registrationFailed')"
            status: pass
            detail: "Line 110-111"
          - check: "Optional chaining on data?.error for null safety"
            status: pass
            detail: "Lines 84, 88 use data?.error"
          - check: "No raw error.message ever shown to user"
            status: pass
            detail: "All error paths use i18n-translated strings"

      - file: "src/components/ActorSelector.tsx"
        verdict: pass
        changes: "Hook reorder (TDZ fix) + web overlay fix"
        checks:
          - check: "useActors and mutation hooks declared before useEffect that uses filtered"
            status: pass
            detail: "Lines 69-72: useActors, useCreateActor, useUpdateActor, useDeleteActor"
          - check: "useMemo filtered declared before useEffect that references it"
            status: pass
            detail: "Lines 74-79: useMemo. Lines 82-91: useEffect uses filtered"
          - check: "No hooks removed or added, only reordered"
            status: pass
            detail: "Same hooks as before, in different order"
          - check: "Web onClick stopPropagation on inner sheet View"
            status: pass
            detail: "Line 231: {...(Platform.OS === 'web' ? { onClick: (e: any) => e.stopPropagation() } : {})}"
          - check: "onStartShouldSetResponder preserved for native"
            status: pass
            detail: "Line 230: onStartShouldSetResponder={() => true}"
          - check: "Platform already imported (no new import needed)"
            status: pass
            detail: "Line 19: Platform in import list"

      - file: "src/components/AgendaForm.tsx"
        verdict: pass
        changes: "HymnSelectorModal web overlay fix + initialNumToRender"
        checks:
          - check: "Platform imported from react-native"
            status: pass
            detail: "Line 19: Platform in import list"
          - check: "Web onClick stopPropagation on HymnSelectorModal inner View"
            status: pass
            detail: "Line 834: {...(Platform.OS === 'web' ? { onClick: (e: any) => e.stopPropagation() } : {})}"
          - check: "onStartShouldSetResponder preserved for native"
            status: pass
            detail: "Line 833: onStartShouldSetResponder={() => true}"
          - check: "FlatList has initialNumToRender={filtered.length}"
            status: pass
            detail: "Line 856: initialNumToRender={filtered.length}"

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      test_file: "src/__tests__/f053-f054-f055-web-bugfixes.test.ts"
      test_count: 57
      test_result: "57/57 pass"
      coverage_assessment: |
        Tests cover all 21 acceptance criteria across 3 features:
        - F053 (CR-263): 30 tests covering i18n keys (3 locales x 4 keys),
          error-handling decision logic (13 scenarios including edge cases),
          frontend validation (8 field validation tests)
        - F054 (CR-264): 15 tests covering filterActorsByRole for all 5 roles
          plus 'all', scroll-to-editingId findIndex behavior, TDZ-safe
          useMemo with undefined/empty/populated actors
        - F055 (CR-265): 12 tests covering initialNumToRender matching data
          length, web onClick stopPropagation pattern, onStartShouldSetResponder
          preservation, search filtering behavior, overlay close pattern,
          native no-regression

      full_suite:
        total: 1000
        passing: 999
        failing: 1
        flaky_test: |
          useSpeechCounts.integration.test.ts > "makes a single Supabase call
          per fetch (AC-046-06 bulk query)" -- pre-existing flaky test from
          Phase 5 (CR-258). Passes when run in isolation, fails in full suite
          due to shared mock state. Also fails on pre-Phase 7 code. NOT caused
          by Phase 7 changes.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-263, CR-264, CR-265: All changes are client-side. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native). No database migrations.
          No Edge Function changes. Can be bundled with the existing pending
          EAS Build from CR-262. No additional deploy steps needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/deploy-commands.yaml"
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M007.yaml"
        - "docs/plans/PLAN_P007.yaml"
        - "docs/qa/QA_PLAN_007.yaml"
        - "docs/specs/SPEC_F007.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 6
      total_commits: 6
      tests_total: 1000
      tests_passing: 999
      tests_flaky: 1
      test_files: 48

  # ===========================================================================
  # REV-010: Phase 8 - CR-266, CR-267, CR-268
  # ===========================================================================

  - id: REV-010
    scope: "Phase 8 - Fixed collection sort order (CR-266), Topic field visibility parity (CR-267), Offline NextSundaysSection (CR-268)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    spec: SPEC_F008.yaml
    arch: ARCH_M008.yaml
    plan: PLAN_P008.yaml
    features: [F056, F057, F058]
    iteration: 1
    commits:
      - hash: 0160b00
        message: "fix(useCollections): replace active/inactive sort with fixed-order priority map (CR-266)"
      - hash: 6cd57b7
        message: "fix(SpeechSlot): remove !!speech guard from showTopicRow for visibility parity (CR-267)"
      - hash: c04ed86
        message: "fix(home): show NextSundaysSection when offline (CR-268)"
      - hash: 00e67f0
        message: "fix(NextSundaysSection): hide pencil buttons when offline (CR-268)"
      - hash: 6bc08ca
        message: "test(phase-8): add 20 tests for F056-F057-F058 (CR-266, CR-267, CR-268)"
      - hash: f2a888b
        message: "docs(ledger): add phase 8 commits for CR-266, CR-267, CR-268"
    verdict: approved

    summary: |
      All 3 CRs implemented correctly. Chain validation fully intact from user
      request through SPEC, ARCH, PLAN, and CODE. 4 source commits plus 1 test
      commit. All 1055 tests pass (51 test files). No P0, P1, or P2 issues found.
      All changes are client-side only; no migrations, no Edge Functions. Clean repo.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        details: |
          CR-266: User asked to fix collection order in Topics settings with specific
          sequence (Temas Especiais, FSY, Principios do Evangelho, conferences newest
          first). SPEC F056 captures this exactly with 3 ACs and 2 edge cases.
          CR-267: User reported topic and speaker field visibility mismatch. SPEC F057
          identifies the root cause (showTopicRow has !!speech guard, speaker row does not)
          and specifies the one-line fix. 2 ACs and 2 edge cases.
          CR-268: User reported can't see upcoming speeches offline on Home tab. SPEC F058
          specifies showing NextSundaysSection offline with pencil buttons hidden. 5 ACs
          and 2 edge cases. Correctly notes this overrides CR-262 F049 behavior.

      arch_vs_spec:
        status: pass
        details: |
          ARCH M008 defines 4 components (C008-01 through C008-04) mapping directly to
          the 3 features. Each component specifies the exact file, function, and code
          change. ADR-024 documents the client-side sort approach decision. Integrations
          mapped for query cache and OnlineStatusContext dependencies.

      plan_vs_arch:
        status: pass
        details: |
          PLAN P008 has 5 steps: STEP-01 (F056 sort), STEP-02 (F057 visibility),
          STEP-03 (F058 Home tab), STEP-04 (F058 pencil button), STEP-05 (verification).
          Each step references the correct files and specifies done_when criteria matching
          the ARCH components. Steps are parallelizable as specified.

      code_vs_plan:
        status: pass
        details: |
          STEP-01: FIXED_COLLECTION_ORDER constant with 9 entries, select function sorts
          by priority then name descending. Exported for testability. JSDoc updated.
          STEP-02: Line 169 changed from "!isPrayer && !!speech" to "!isPrayer".
          STEP-03: NextSundaysSection rendered without isOnline guard. Other 2 sections
          still guarded.
          STEP-04: useOnlineStatus imported, renderHeaderRight conditional on isOnline.

      chain_intact: true

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      - cr: CR-266
        title: "Fixed collection sort order in useCollections"
        verdict: pass
        verification:
          - check: "FIXED_COLLECTION_ORDER constant exported with 9 entries"
            status: pass
            file: src/hooks/useTopics.ts
            detail: "Lines 187-200: 3 pt-BR, 3 en-US, 3 es-LA entries with priorities 0, 1, 2"
          - check: "select function uses priority map for sorting"
            status: pass
            detail: "Lines 244-250: sorts by priority (0-2 named, 3 conferences), then name descending"
          - check: "Active/inactive status not referenced in sort"
            status: pass
            detail: "No reference to .active property in the select function"
          - check: "JSDoc comment updated"
            status: pass
            detail: "Lines 202-207: 'Collections are sorted in fixed order: known collections by priority (0-2), then conference collections (priority 3) by name descending (newest first).'"
          - check: "Test file created with comprehensive coverage"
            status: pass
            detail: "f056-collection-sort.test.ts: 18 tests covering all 3 ACs, 2 edge cases, plus additional edge cases"

      - cr: CR-267
        title: "Topic field visibility parity with speaker field"
        verdict: pass
        verification:
          - check: "showTopicRow guard changed to !isPrayer only"
            status: pass
            file: src/components/SpeechSlot.tsx
            detail: "Line 169: const showTopicRow = !isPrayer; (removed !!speech)"
          - check: "Topic Pressable disabled prop still has !speech check"
            status: pass
            detail: "Line 271: disabled={!canAssign || !speech} -- ensures tapping topic when speech=null is a no-op"
          - check: "Test file created"
            status: pass
            detail: "f057-topic-visibility.test.ts: 16 tests covering both ACs, both edge cases, plus regression guard"

      - cr: CR-268
        title: "Show NextSundaysSection offline on Home tab"
        verdict: pass
        verification:
          - check: "NextSundaysSection isOnline guard removed in Home tab"
            status: pass
            file: src/app/(tabs)/index.tsx
            detail: "Line 245: <NextSundaysSection /> (no isOnline guard)"
          - check: "NextAssignmentsSection still has isOnline guard"
            status: pass
            detail: "Line 246: {isOnline && <NextAssignmentsSection />}"
          - check: "InviteManagementSection still has isOnline guard"
            status: pass
            detail: "Line 247: {isOnline && <InviteManagementSection />}"
          - check: "NextSundaysSection imports useOnlineStatus"
            status: pass
            file: src/components/NextSundaysSection.tsx
            detail: "Line 18: import { useOnlineStatus } from '../contexts/OnlineStatusContext'"
          - check: "isOnline used in NextSundaysSection"
            status: pass
            detail: "Line 37: const isOnline = useOnlineStatus();"
          - check: "renderHeaderRight conditional on isOnline"
            status: pass
            detail: "Line 106: renderHeaderRight={isOnline ? () => (...pencil...) : undefined}"
          - check: "f049 test updated for CR-268 change"
            status: pass
            detail: "f049-home-offline.test.ts updated: AC-049-03 now tests NextSundaysSection always visible"
          - check: "New test file created"
            status: pass
            detail: "f058-offline-next-sundays.test.ts: 15 tests covering all 5 ACs, 2 edge cases"

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      new_test_files:
        - file: "src/__tests__/f056-collection-sort.test.ts"
          test_count: 18
          coverage: "AC-056-1, AC-056-2, AC-056-3, EC-056-1, EC-056-2, plus additional edge cases"
        - file: "src/__tests__/f057-topic-visibility.test.ts"
          test_count: 16
          coverage: "AC-057-1, AC-057-2, EC-057-1, EC-057-2, plus regression guard and display logic"
        - file: "src/__tests__/f058-offline-next-sundays.test.ts"
          test_count: 15
          coverage: "AC-058-1 through AC-058-5, EC-058-1, EC-058-2"
      updated_test_files:
        - file: "src/__tests__/f049-home-offline.test.ts"
          changes: "AC-049-03 updated to reflect CR-268 (NextSundaysSection always visible)"

      full_suite:
        total: 1055
        passing: 1055
        failing: 0
        test_files: 51

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-266, CR-267, CR-268: All changes are client-side only. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native / TypeScript). No database
          migrations. No Edge Function changes. Can be bundled with existing
          pending EAS Build from CR-262 (which is still pending for offline mode
          npm packages). No additional deploy steps needed beyond the app binary.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M008.yaml"
        - "docs/plans/PLAN_P008.yaml"
        - "docs/qa/QA_PLAN_008.yaml"
        - "docs/specs/SPEC_F008.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 5
      total_commits: 6
      tests_total: 1055
      tests_passing: 1055
      tests_flaky: 0
      test_files: 51

  # ===========================================================================
  # REV-009: Phase 9 - CR-269 (F059)
  # ===========================================================================

  - id: REV-009
    scope: "CR-269 - Designation Notification Initial Assignment Guard (F059)"
    date: "2026-03-04"
    phase: 9
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: 9bffbf3
        message: "fix(notification): add OLD.status guard to designation trigger Case 1 (CR-269)"
      - hash: 9bdaa4d
        message: "docs(ledger): add phase 9 commit for CR-269"
      - hash: 77971de
        message: "test(F059): replace fs.readFileSync tests with behavioral trigger logic tests (CR-269)"
    verdict: approved

    summary: |
      Clean, minimal DB trigger fix. Migration 028 redefines enqueue_speech_notification()
      with a strict OLD.status = 'not_assigned' guard on Case 1 (designation), ensuring
      notifications only fire on initial speaker assignment. 35 behavioral tests cover
      all 7 ACs, 3 edge cases, early-exit guard, and exhaustive status transitions.
      No client-side code changes. Full test suite passes (1090 tests, 52 files).

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        details: |
          User request: designation push notification should only fire when bishopric
          selects a speaker name in the expanded speech card (initial assignment).
          SPEC_F009 captures this correctly as F059 with 7 ACs and 3 edge cases.
          The fix scope (DB trigger only, no client changes) matches the root cause.
      arch_vs_spec:
        status: pass
        details: |
          ARCH_M009 correctly maps the fix to a single component C009-01 (migration 028).
          The strict positive equality guard (OLD.status = 'not_assigned') is justified
          by ADR-025 and is more restrictive than the negative guard used by Cases 4/5,
          which matches the different business requirement (only initial assignment vs
          prevent duplicate fires).
      plan_vs_arch:
        status: pass
        details: |
          PLAN_P009 has 2 steps: STEP-01 (create migration + tests) and STEP-02
          (verify full suite). The plan includes the complete function body copied
          from migration 013 with only Case 1 guard added. done_when criteria are
          comprehensive (9 checks).
      code_vs_plan:
        status: pass
        details: |
          Migration 028 exactly matches the planned function body. The diff between
          migrations 013 and 028 shows only: (1) header comments updated, (2) Case 1
          comment updated, (3) Case 1 guard line changed from
          "IF NEW.status = 'assigned_not_invited' THEN" to
          "IF NEW.status = 'assigned_not_invited' AND (TG_OP = 'INSERT' OR OLD.status = 'not_assigned') THEN".
          All other cases (4, 5, cancellation, early-exit) are byte-identical.
      chain_intact: true

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      migration_028:
        file: supabase/migrations/028_fix_designation_notification_guard.sql
        status: pass
        checks:
          - check: "CREATE OR REPLACE FUNCTION enqueue_speech_notification()"
            status: pass
            detail: "Line 20: correct function signature"
          - check: "SECURITY DEFINER preserved"
            status: pass
            detail: "Line 96: $$ LANGUAGE plpgsql SECURITY DEFINER;"
          - check: "Early-exit guard unchanged"
            status: pass
            detail: "Lines 24-26: IF TG_OP = 'UPDATE' AND OLD.status = NEW.status THEN RETURN NEW;"
          - check: "Case 1 guard added correctly"
            status: pass
            detail: "Lines 32-33: IF NEW.status = 'assigned_not_invited' AND (TG_OP = 'INSERT' OR OLD.status = 'not_assigned') THEN"
          - check: "Case 4 guard unchanged"
            status: pass
            detail: "Line 50: IF NEW.status = 'assigned_confirmed' AND (TG_OP = 'INSERT' OR OLD.status != 'assigned_confirmed') THEN"
          - check: "Case 5 guard unchanged"
            status: pass
            detail: "Line 67: IF NEW.status = 'gave_up' AND (TG_OP = 'INSERT' OR OLD.status != 'gave_up') THEN"
          - check: "Cancellation case unchanged"
            status: pass
            detail: "Lines 84-91: IF NEW.status = 'not_assigned' AND TG_OP = 'UPDATE' AND OLD.status != 'not_assigned' THEN ..."
          - check: "INSERT INTO notification_queue columns match original"
            status: pass
            detail: "All 3 INSERT statements (Cases 1, 4, 5) have identical column lists and value expressions as migration 013"
          - check: "ADR-025 referenced in comments"
            status: pass
            detail: "Lines 13-15 and 31"
          - check: "No trigger re-creation (CREATE OR REPLACE FUNCTION only)"
            status: pass
            detail: "File contains only function definition, no CREATE TRIGGER or DROP TRIGGER"

      test_file:
        file: src/__tests__/f059-designation-notification-guard.test.ts
        status: pass
        checks:
          - check: "Behavioral simulation approach (not fs.readFileSync)"
            status: pass
            detail: "simulateTrigger() function mirrors SQL logic, tests behavior not string content"
          - check: "All 7 ACs covered"
            status: pass
            detail: "AC-059-1 through AC-059-7 each have dedicated describe blocks"
          - check: "All 3 edge cases covered"
            status: pass
            detail: "EC-059-1 (reassign cycle), EC-059-2 (unmanaged prayer), EC-059-3 (INSERT safety)"
          - check: "Early-exit guard exhaustive test"
            status: pass
            detail: "it.each(ALL_STATUSES) tests all 5 same-status transitions"
          - check: "Exhaustive guard test"
            status: pass
            detail: "Tests all 3 non-not_assigned statuses blocked from designation + positive confirmation"
          - check: "35 tests, all passing"
            status: pass

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      new_test_files:
        - file: "src/__tests__/f059-designation-notification-guard.test.ts"
          test_count: 35
          coverage: "AC-059-1, AC-059-2, AC-059-3, AC-059-4, AC-059-5, AC-059-6, AC-059-7, EC-059-1, EC-059-2, EC-059-3, early-exit guard, exhaustive guard, Case 4/5 self-transitions, INSERT cancellation guard"
      updated_test_files: []

      full_suite:
        total: 1090
        passing: 1090
        failing: 0
        test_files: 52

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Apply migration 028 to Supabase (supabase db push or apply via dashboard)"
        reason: "CR-269: New SQL migration redefines enqueue_speech_notification() trigger function"
        urgency: medium
        instructions: |
          Run the migration against the production Supabase database:
            supabase db push
          or apply 028_fix_designation_notification_guard.sql manually via
          the Supabase SQL Editor in the dashboard.
          No downtime needed - CREATE OR REPLACE FUNCTION is atomic.
          No client-side changes are needed; the fix is entirely server-side.
        notes: |
          This migration does NOT require an app binary update (EAS Build).
          It can be deployed independently of the pending EAS Build from CR-262.
          After applying, verify in Supabase SQL Editor:
            SELECT prosrc FROM pg_proc WHERE proname = 'enqueue_speech_notification';
          Should contain "OLD.status = 'not_assigned'" in the Case 1 guard.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/tests/TEST_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M009.yaml"
        - "docs/plans/PLAN_P009.yaml"
        - "docs/qa/QA_PLAN_009.yaml"
        - "docs/specs/SPEC_F009.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 3
      total_commits: 3
      tests_total: 1090
      tests_passing: 1090
      tests_flaky: 0
      test_files: 52

  # ===========================================================================
  # REV-012: Phase 10 - Notification text, error i18n, session refresh
  #          (CR-270, CR-271, CR-272)
  # ===========================================================================

  - id: REV-012
    scope: "F060/F061/F062 Phase 10 - Prayer-Aware Designation Notifications (CR-270), User-Friendly Error Messages (CR-271), Refresh Session Before Edge Function Invoke (CR-272)"
    date: "2026-03-05"
    reviewer: reviewer-agent
    spec: SPEC_F010
    arch: ARCH_M010
    plan: PLAN_P010
    iteration: 1
    commits:
      # STEP-01: i18n keys (CR-270/CR-271/CR-272)
      - hash: 9612ef9
        step: STEP-01
        message: "feat(i18n): add 5 new error message i18n keys to all 3 locales (CR-270, CR-271, CR-272)"
      # STEP-02: Server-side prayer-aware designation text (CR-270)
      - hash: 59734d5
        step: STEP-02
        message: "feat(process-notifications): prayer-aware designation text for position 0/4 (CR-270)"
      # STEP-03: Client-side prayer-aware designation text (CR-270)
      - hash: 6749af7
        step: STEP-03
        message: "feat(notificationUtils): prayer-aware designation text for position 0/4 (CR-270)"
      # STEP-04: Invite error i18n (CR-271) - users.tsx
      - hash: cb03b90
        step: STEP-04
        message: "fix(users): replace raw error with i18n key in invite error handler (CR-271)"
      # STEP-05 (actually STEP-06 in PLAN): Settings error i18n (CR-271)
      - hash: 1f4b56b
        step: STEP-05
        message: "fix(settings): replace raw errors with i18n keys for language change and sign out (CR-271)"
      # STEP-06 (actually STEP-07 in PLAN): Members error i18n (CR-271)
      - hash: 5155acf
        step: STEP-06
        message: "fix(members): replace raw errors with i18n keys for delete warning and import error (CR-271)"
      # STEP-07 (actually STEP-05 in PLAN): Invite registration error i18n (CR-271)
      - hash: dc84aaf
        step: STEP-07
        message: "fix(invite): replace raw errors with i18n registrationFailed key (CR-271)"
      # STEP-08: refreshSession guard (CR-272) - initial attempt
      - hash: 3e29e1c
        step: STEP-08
        message: "feat(users): add refreshSession() after role change and user deletion (CR-272)"
      # STEP-08-fix: Correct refreshSession placement (CR-272)
      - hash: 98f2149
        step: STEP-08-fix
        message: "fix(users): use refreshSession() in callEdgeFunction guard (CR-272)"
    verdict: approved

    summary: |
      All 9 commits for Phase 10 (CR-270, CR-271, CR-272) correctly implement
      prayer-aware designation notification text, user-friendly error messages
      in all UI alerts, and session refresh before edge function invocation.
      Chain validation passes: SPEC -> ARCH -> PLAN -> CODE is fully aligned.
      All 1148 tests pass (53 test files, 0 failures). Repository is clean
      (no uncommitted code files). One P2 deviation noted: members.tsx import
      error discrimination uses message-content checking instead of the tag-based
      approach specified in PLAN/ARCH (ADR-027), but is functionally correct.
      No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested 3 bug fixes:
          CR-270: Designation notification uses speech text for prayers - SPEC_F010
          defines F060 with 5 ACs covering prayer-specific title/body for positions
          0 and 4, individual (not grouped) processing, and client-server consistency.
          CR-271: Raw error messages shown in UI alerts - SPEC_F010 defines F061
          with 8 ACs covering all 7 identified error points across 4 files.
          CR-272: Stale JWT when invoking edge functions - SPEC_F010 defines F062
          with 3 ACs covering refreshSession replacement.
          All user requirements are faithfully captured.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M010 maps each feature to architectural components:
          F060: C010-01 (server grouping + buildDesignationText), C010-02 (client notificationUtils)
          F061: C010-03 (users.tsx), C010-04 (invite/[token].tsx), C010-05 (settings/index.tsx),
                C010-06 (members.tsx), C010-07 (i18n keys)
          F062: C010-08 (callEdgeFunction refreshSession)
          7 integrations mapped. 3 ADRs documented (ADR-026, ADR-027, ADR-028).
          All components map correctly to SPEC features.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P010 has 8 steps covering all 8 ARCH components. Step order is
          correct: i18n keys first (STEP-01), then server notification (STEP-02),
          client notification (STEP-03), then error i18n fixes (STEP-04 to STEP-07),
          verification (STEP-08). STEP-04 combines F061+F062 users.tsx changes
          (correct since both modify same file, different functions).
          Validation matrix maps all 22 ACs and 9 edge cases to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 8 plan steps implemented in 9 commits. STEP-08 was split into
          two commits (3e29e1c initial attempt + 98f2149 fix) to correctly place
          refreshSession() in the callEdgeFunction guard rather than after
          individual mutation calls. The step ordering was slightly different
          from PLAN (invite/[token].tsx done as STEP-07 instead of STEP-05),
          but all changes are complete and correct. One implementation deviation:
          members.tsx uses message-content checking for CSV error discrimination
          instead of the isCsvValidation tag specified in PLAN/ARCH (P2 issue).

      chain_intact: true

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      # -----------------------------------------------------------------------
      # CR-270: Prayer-Aware Designation Notification Text (F060)
      # -----------------------------------------------------------------------
      - cr: CR-270
        title: "Prayer-aware designation notification text"
        verdict: pass
        verification:

          # Server-side (process-notifications/index.ts)
          - check: "buildDesignationText accepts optional position parameter"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Line 66-70: function buildDesignationText(language, names, date, position?: number | null)"

          - check: "Prayer position (0,4) returns prayer-specific title and body"
            status: pass
            detail: |
              Lines 73-90: When getPrayerLabel returns non-null, returns prayer-specific
              text. pt-BR: 'Designacao de Oracao', en-US: 'Prayer Assignment',
              es-LA: 'Asignacion de Oracion'. Body includes prayer label (abertura/encerramento).

          - check: "Speech position (1-3, null) returns existing text unchanged"
            status: pass
            detail: "Lines 93-109: Existing speech text with nameList, verb conjugation, unchanged."

          - check: "Grouping loop splits prayer vs speech designations"
            status: pass
            detail: |
              Lines 244-258: entry.speech_position === 0 || 4 -> immediateEntries.
              Otherwise -> designationGroups map by (ward_id:sunday_date).

          - check: "immediateEntries switch has 'designation' case"
            status: pass
            detail: |
              Lines 316-325: case 'designation' calls buildDesignationText with
              [entry.speaker_name ?? ''], entry.sunday_date, entry.speech_position.

          # Client-side (notificationUtils.ts)
          - check: "pt-BR designation builder checks prayer position"
            status: pass
            file: src/lib/notificationUtils.ts
            detail: "Lines 67-83: Checks d.position != null, calls getPrayerLabel. Returns 'Designacao de Oracao' for prayer."

          - check: "en-US designation builder checks prayer position"
            status: pass
            detail: "Lines 120-135: Same pattern. Returns 'Prayer Assignment' for prayer."

          - check: "es-LA designation builder checks prayer position"
            status: pass
            detail: "Lines 173-188: Same pattern. Returns 'Asignacion de Oracion' for prayer."

          - check: "Client and server text match"
            status: pass
            detail: |
              Both use getPrayerLabel() for position detection. Both produce
              identical title strings per language. Body format matches
              (name + prayer label + date + call to action).

        ac_coverage:
          - ac: AC-060-1
            status: covered
            detail: "Position 0 -> getPrayerLabel returns 'oracao de abertura'/'opening prayer' -> prayer title+body"
          - ac: AC-060-2
            status: covered
            detail: "Position 4 -> getPrayerLabel returns 'oracao de encerramento'/'closing prayer' -> prayer title+body"
          - ac: AC-060-3
            status: covered
            detail: "Positions 1-3 -> grouped by (ward_id, sunday_date), speech text unchanged"
          - ac: AC-060-4
            status: covered
            detail: "Prayer entries (0,4) pushed to immediateEntries, not designationGroups"
          - ac: AC-060-5
            status: covered
            detail: "Client notificationUtils mirrors server behavior with same getPrayerLabel"

        ec_coverage:
          - ec: EC-060-1
            status: covered
            detail: "Both position 0 and 4 go to immediateEntries independently, each gets own notification"
          - ec: EC-060-2
            status: covered
            detail: "null position -> getPrayerLabel returns null -> falls through to speech text"

      # -----------------------------------------------------------------------
      # CR-271: User-Friendly Error Messages (F061)
      # -----------------------------------------------------------------------
      - cr: CR-271
        title: "User-friendly error messages in all alerts"
        verdict: pass
        verification:

          # P0: users.tsx invite error
          - check: "inviteMutation.onError maps auth/no-session to i18n key"
            status: pass
            file: src/app/(tabs)/settings/users.tsx
            detail: |
              Lines 110-116: if (err.message === 'auth/no-session') -> t('users.sessionExpired')
              else -> t('users.inviteFailed'). No raw err.message shown.

          # P1: invite/[token].tsx unknown error
          - check: "Unknown data.error uses t('auth.registrationFailed')"
            status: pass
            file: src/app/(auth)/invite/[token].tsx
            detail: "Line 117: setError(t('auth.registrationFailed')) after known codes check."

          # P1: invite/[token].tsx catch-all
          - check: "Non-network catch uses t('auth.registrationFailed')"
            status: pass
            detail: "Line 134: setError(t('auth.registrationFailed')) instead of raw message."

          # P1: Known codes preserved
          - check: "Known error codes (token_expired/used/invalid) still mapped correctly"
            status: pass
            detail: |
              Lines 101-114: token_expired -> t('auth.inviteExpired'),
              token_used -> t('auth.inviteUsed'), token_invalid -> t('auth.inviteInvalid').
              Unchanged from before.

          # P1: Network error preserved
          - check: "Network error still uses t('auth.requiresConnection')"
            status: pass
            detail: "Lines 131-132: message includes 'Failed to fetch' or 'Network' -> t('auth.requiresConnection')"

          # P2: settings/index.tsx language change
          - check: "handleAppLanguageSelect uses t('settings.languageChangeFailed')"
            status: pass
            file: src/app/(tabs)/settings/index.tsx
            detail: "Line 153: Alert.alert(t('common.error'), t('settings.languageChangeFailed'))"

          # P2: settings/index.tsx sign out
          - check: "handleSignOut uses t('settings.signOutFailed')"
            status: pass
            detail: "Line 195: Alert.alert(t('common.error'), t('settings.signOutFailed'))"

          # P3: members.tsx import RPC error
          - check: "Import RPC error shows t('members.importRpcError')"
            status: pass
            file: src/app/(tabs)/settings/members.tsx
            detail: |
              Lines 474-479: onError checks isCsvParseError (message-based detection),
              shows err.message for CSV errors, t('members.importRpcError') for RPC errors.

          # P3: CSV validation errors preserved
          - check: "CSV validation errors still show detailed per-line messages"
            status: pass
            detail: "CSV parse errors contain newlines from join('\\n'). Detected by includes('\\n')."

          # P4: members.tsx future speeches i18n
          - check: "Future speeches uses t('members.futureSpeechWarning')"
            status: pass
            detail: "Line 356: t('members.futureSpeechWarning', { count: futureCount })"

          # i18n keys
          - check: "All 6 new i18n keys present in all 3 locales"
            status: pass
            detail: |
              Verified in pt-BR.json, en-US.json, es-LA.json:
              - settings.languageChangeFailed: present in all 3
              - settings.signOutFailed: present in all 3
              - members.futureSpeechWarning: present in all 3 with {{count}}
              - members.importRpcError: present in all 3
              - users.sessionExpired: present in all 3
              - auth.registrationFailed: present in all 3
              Total: 18 new entries (6 per locale).

        ac_coverage:
          - ac: AC-061-1
            status: covered
            detail: "inviteMutation.onError uses t('users.inviteFailed'), not err.message"
          - ac: AC-061-2
            status: covered
            detail: "Unknown data.error shows t('auth.registrationFailed')"
          - ac: AC-061-3
            status: covered
            detail: "Non-network catch shows t('auth.registrationFailed')"
          - ac: AC-061-4
            status: covered
            detail: "handleAppLanguageSelect catch shows t('settings.languageChangeFailed')"
          - ac: AC-061-5
            status: covered
            detail: "handleSignOut catch shows t('settings.signOutFailed')"
          - ac: AC-061-6
            status: covered
            detail: "RPC import error shows t('members.importRpcError')"
          - ac: AC-061-7
            status: covered
            detail: "CSV validation errors show err.message (already i18n'd)"
          - ac: AC-061-8
            status: covered
            detail: "Future speeches uses t('members.futureSpeechWarning', { count })"

      # -----------------------------------------------------------------------
      # CR-272: Refresh Session Before Edge Function Invoke (F062)
      # -----------------------------------------------------------------------
      - cr: CR-272
        title: "Refresh session before edge function invoke"
        verdict: pass
        verification:
          - check: "callEdgeFunction uses refreshSession() not getSession()"
            status: pass
            file: src/app/(tabs)/settings/users.tsx
            detail: "Line 47: const { data: { session }, error: refreshError } = await supabase.auth.refreshSession();"

          - check: "refreshError checked in guard"
            status: pass
            detail: "Line 48: if (refreshError || !session) throw new Error('auth/no-session');"

          - check: "Comment references ADR-028"
            status: pass
            detail: "Line 46: // Guard: refresh session before calling invoke (ADR-028)"

        ac_coverage:
          - ac: AC-062-1
            status: covered
            detail: "refreshSession() returns fresh JWT; invoke() uses updated internal state"
          - ac: AC-062-2
            status: covered
            detail: "refreshError || !session -> throws 'auth/no-session'"
          - ac: AC-062-3
            status: covered
            detail: "Supabase SDK internally returns existing session if token is still valid (no unnecessary network call)"

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: P2-010-01
        severity: P2
        category: quality
        location: "src/app/(tabs)/settings/members.tsx:474-479"
        description: |
          Import error discrimination uses message-content checking (includes('\n')
          and includes(t('members.importEmpty'))) instead of the isCsvValidation tag
          approach specified in PLAN_P010 STEP-07 and ARCH_M010 ADR-027. The ARCH
          explicitly noted "Parse error message format (check for newlines): Fragile
          and breaks if error message format changes" as a rejected alternative.
        suggestion: |
          Consider tagging errors with a custom property (isCsvValidation = true)
          before throwing, as specified in the PLAN. However, this is a minor quality
          concern - the current approach works correctly for all current error formats.
          Not blocking approval.

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 1148
      total_files: 53
      all_passing: true
      new_tests:
        - file: src/__tests__/f060-f061-f062.test.ts
          covers: "All 3 CRs: prayer-aware designation text, error i18n, refreshSession guard"
      coverage_gaps:
        - cr: CR-270
          component: "Server-side process-notifications grouping"
          reason: "Edge Function (Deno runtime) cannot be imported in vitest Node environment"
          risk: low
          mitigation: "Code review verified grouping logic. Client-side equivalent is tested."
        - cr: CR-272
          component: "refreshSession actual behavior"
          reason: "Cannot mock Supabase auth.refreshSession in unit tests for real SDK behavior"
          risk: low
          mitigation: "Code review verified correct API usage. Tests verify the code path exists."

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy process-notifications Edge Function"
        command: "supabase functions deploy process-notifications"
        reason: "CR-270: Prayer-aware designation grouping and text changes"
        urgency: medium
        instructions: |
          Deploy the updated process-notifications Edge Function:
            supabase functions deploy process-notifications
          No downtime needed. The change is backward-compatible:
          - Existing speech designations continue to be grouped as before
          - New prayer designations (position 0,4) will be sent individually
          No database migration needed for this change.
        notes: |
          This deployment can be done independently of the app update.
          After deploying, verify by assigning a prayer and checking that
          the notification has prayer-specific title ('Prayer Assignment')
          instead of speech title ('Speech Assignment').

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M010.yaml"
        - "docs/plans/PLAN_P010.yaml"
        - "docs/qa/QA_PLAN_010.yaml"
        - "docs/specs/SPEC_F010.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 10
      total_commits: 9
      tests_total: 1148
      tests_passing: 1148
      tests_flaky: 0
      test_files: 53
