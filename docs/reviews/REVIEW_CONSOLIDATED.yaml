# =============================================================================
# REVIEW_CONSOLIDATED.yaml
# Consolidated code review records
# =============================================================================

metadata:
  project: Sacrament Meeting Planner
  last_updated: "2026-03-14"
  version: 23
  last_updated_by: "phase-021"

reviews:
  - id: REV-001
    scope: "CR-248 - Project Cleanup & Consolidation"
    date: "2026-03-03"
    reviewer: reviewer-agent
    commits:
      - hash: e1a0bb8
        message: "refactor(cleanup): consolidate project removing all CR/batch history (CR-248 phase 1)"
      - hash: 12e0d92
        message: "test(cleanup): remove CR/SPEC/ARCH references from tests and populate TEST_CONSOLIDATED (CR-248 phase 3)"
    verdict: changes_required

    summary: |
      The cleanup was largely successful: 245k+ lines of historical docs removed,
      batch/CR/phase tests deleted, SPEC_CONSOLIDATED rewritten from scratch with
      38 features, TEST_CONSOLIDATED populated with 577 tests, process docs zeroed,
      archive created. No functional code was lost. However, two categories of
      residual references remain that must be addressed before this can be approved.

    issues:
      - id: P1-001
        severity: P1
        title: "13 CR-XXX references remain in source code comments"
        description: |
          The user explicitly requested "remover comentarios CR do codigo-fonte".
          Phase 1 commit (e1a0bb8) removed some CR comments but missed 13 references
          across 7 source files. These are all in code comments (not functional code).
        files:
          - path: src/components/DebouncedTextInput.tsx
            line: 4
            content: "Prevents excessive mutation calls during typing (CR-27)."
          - path: src/hooks/useSundayTypes.ts
            line: 25
            content: "Per F007/CR-56: 'speeches' is stored in sunday_exceptions table"
          - path: src/contexts/AuthContext.tsx
            line: 104
            content: "F116 (CR-178): App language from user_metadata"
          - path: src/components/AgendaForm.tsx
            line: 509
            content: "Special meeting: dynamic section header based on type (CR-29)"
          - path: src/components/SundayCard.tsx
            lines: [47, 103, 461, 533, 590]
            content: "Multiple CR-221 references in JSDoc and JSX comments"
          - path: src/components/SpeechSlot.tsx
            line: 55
            content: "CR-221: Whether this slot is a prayer"
          - path: src/app/(tabs)/speeches.tsx
            lines: [436, 489]
            content: "CR-221 references in JSX comments"
          - path: src/app/(tabs)/settings/whatsapp.tsx
            line: 415
            content: "CR-231: Segmented control always shown"
        fix: |
          Remove the CR-XXX portion of each comment while preserving the useful
          descriptive text. For example:
          - "CR-221: Whether managed prayers is enabled" -> "Whether managed prayers is enabled"
          - "Per F007/CR-56: speeches is stored..." -> "speeches is stored..."
          - "Special meeting: dynamic section header based on type (CR-29)" -> "Special meeting: dynamic section header based on type"

      - id: P1-002
        severity: P1
        title: "ARCH_CONSOLIDATED spec_features reference 98 old-style feature IDs not in new SPEC"
        description: |
          SPEC_CONSOLIDATED was rewritten from scratch with new sequential IDs F001-F038.
          However, ARCH_CONSOLIDATED still references the old feature IDs (F044-F173)
          in its spec_features arrays for each module. These 98 old IDs do not exist
          in the new SPEC, making the cross-references broken and meaningless.
        files:
          - path: docs/arch/ARCH_CONSOLIDATED.yaml
            lines: [124, 179, 239, 306, 380, 403, 426, 449]
            content: "spec_features arrays contain old IDs like F050, F093-F173"
        fix: |
          Re-map each module's spec_features to reference only the new F001-F038 IDs
          from SPEC_CONSOLIDATED.yaml. Each module should list which of the 38 new
          features it implements.

      - id: P2-001
        severity: P2
        title: "TEST_CONSOLIDATED header references CR-248"
        description: |
          Line 3 of TEST_CONSOLIDATED.yaml contains:
          "Consolidated test inventory - all surviving tests after CR-248 cleanup"
          This is a minor cosmetic issue in a comment header.
        files:
          - path: docs/tests/TEST_CONSOLIDATED.yaml
            line: 3
            content: "# Consolidated test inventory - all surviving tests after CR-248 cleanup"
        fix: |
          Change to: "# Consolidated test inventory for all project tests"

    verified_items:
      - item: "Individual doc files deleted (SPEC_F*, ARCH_M*, PLAN_P*, QA_PLAN*)"
        status: pass
      - item: "history/ directories deleted"
        status: pass
      - item: ".devteam/phases/ deleted"
        status: pass
      - item: "Batch/CR/phase/bug/qa test files deleted"
        status: pass
      - item: "SPEC_CONSOLIDATED.yaml rewritten from scratch (38 features, no CR refs)"
        status: pass
      - item: "ARCH_CONSOLIDATED.yaml free of CR-XXX text references"
        status: pass
      - item: "CODE_LEDGER.yaml zeroed with structure"
        status: pass
      - item: "REVIEW_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "QA_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "PLAN_CONSOLIDATED.yaml zeroed with structure"
        status: pass
      - item: "TEST_CONSOLIDATED.yaml populated (30 files, 577 tests)"
        status: pass
      - item: "CHANGE_REQUESTS_ARCHIVE.yaml exists with warning header"
        status: pass
      - item: "No source code files deleted (only docs and tests)"
        status: pass
      - item: "No batch/BATCH/phase-/PHASE_ references in docs or src"
        status: pass
      - item: "Tests passing (577 confirmed by tester)"
        status: pass
      - item: "Test files clean of CR references"
        status: pass

    pending_deploys: []

  - id: REV-002
    scope: "CR-248 - Re-review after P1/P2 fixes"
    date: "2026-03-03"
    reviewer: reviewer-agent
    commits_reviewed:
      - hash: 93b0c76
        description: "Remove 13 CR-XXX references from source code + fix TEST_CONSOLIDATED header"
      - description: "ARCH_CONSOLIDATED.yaml rewritten from scratch by architect (aligned with new SPEC F001-F038)"
    verdict: approved

    summary: |
      All three issues from REV-001 have been resolved. The project cleanup
      is now complete.

    fixes_verified:
      - issue: P1-001
        title: "CR-XXX references in source code"
        status: fixed
        verification: |
          grep for CR-\d+ in src/ returns zero matches. All 13 references
          across 7 files have been removed.

      - issue: P1-002
        title: "ARCH spec_features referencing old feature IDs"
        status: fixed
        verification: |
          ARCH_CONSOLIDATED.yaml was rewritten from scratch. Now contains
          13 modules with features arrays referencing only F001-F038.
          Feature ID sets match exactly between SPEC and ARCH (perfect 1:1
          mapping of all 38 features). No old-style IDs (F039+) remain.

      - issue: P2-001
        title: "TEST_CONSOLIDATED header referencing CR-248"
        status: fixed
        verification: |
          Header now reads "# Consolidated test inventory" with no CR reference.

    additional_checks:
      - check: "SPEC x ARCH feature consistency"
        status: pass
        details: |
          ARCH features union = {F001..F038}, SPEC features = {F001..F038}.
          All 38 features are accounted for in module assignments. No orphaned
          features in either direction.

      - check: "ARCH quality"
        status: pass
        details: |
          New ARCH has 13 well-defined modules (vs 8 before), 14 ADRs,
          complete database schema, edge functions, provider hierarchy,
          and shared libraries sections. Clean, well-structured, no
          historical references.

      - check: "No CR/batch/phase references in docs or source"
        status: pass
        details: |
          Full grep across src/ and docs consolidated files returns zero
          matches for CR-\d+, batch, BATCH, phase-, PHASE_. Only expected
          references remain in CHANGE_REQUESTS_ARCHIVE.yaml (historical
          archive with warning header) and REVIEW_CONSOLIDATED.yaml
          (review records documenting the cleanup).

      - check: "No functional code lost"
        status: pass
        details: "All source files intact. Only docs and test files were deleted."

    advisory_notes:
      - note: |
          docs/PRODUCT_SPECIFICATION.md (180KB) and docs/SPEC.final.md (99KB)
          are the user's original specification documents that predate the CR
          system. They are superseded by SPEC_CONSOLIDATED.yaml but may have
          sentimental or reference value. The user may want to archive or
          delete them in a future cleanup pass.
      - note: |
          docs/SUPABASE_QUERY_PERF.md and docs/SUPABASE_SECURITY.md are raw
          data dumps from Supabase tooling, not CR-related documentation.
          They can remain as-is or be cleaned up at the user's discretion.

    pending_deploys: []

  # ===========================================================================
  # REV-003: Phase 1 - Quick fixes (CR-250, 251, 252, 254, 256, 257)
  # ===========================================================================

  - id: REV-003
    scope: "F039 Phase 1 - Quick fixes (CR-250, CR-251, CR-252, CR-254, CR-256, CR-257)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F039
    arch: M014
    commits:
      - hash: 4a83fe8
        message: "fix(send-reset-email): change email language fallback from pt-BR to en (CR-251)"
      - hash: 4b3a41f
        message: "fix(create-invitation): remove diagnostic mode (CR-252)"
      - hash: 1bbdd9d
        message: "fix(csvUtils): require 3-column CSV format only (CR-254)"
      - hash: b310ed8
        message: "fix(settings): allow secretary to toggle manage_prayers (CR-256)"
      - hash: f9625c9
        message: "docs(process-notifications): correct cron interval comment to 5 minutes (CR-257)"
    verdict: approved

    summary: |
      All 5 code commits for Phase 1 are correct, complete, and match the SPEC
      (F039) and ARCH (M014) specifications. Chain validation SPEC->ARCH->CODE
      is fully aligned. 589 tests pass (30 test files, 0 failures). The doc
      corrections to SPEC_CONSOLIDATED, ARCH_CONSOLIDATED, and the code changes
      are all consistent with each other. No P0 or P1 issues found.

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      # -----------------------------------------------------------------------
      # CR-251: Email language fallback
      # -----------------------------------------------------------------------
      - cr: CR-251
        title: "Change email language fallback from pt-BR to en"
        verdict: pass
        verification:
          - check: "Line 82: templates fallback changed from 'pt-BR' to 'en'"
            status: pass
            file: supabase/functions/send-reset-email/index.ts
            detail: "return templates[language] ?? templates['en'];"
          - check: "Line 152: Comment updated to say 'en'"
            status: pass
            detail: "// Priority: user_metadata.language > ward.language > 'en'"
          - check: "Line 153: Default language changed from 'pt-BR' to 'en'"
            status: pass
            detail: "let language = 'en';"
          - check: "Line 165: Ward fallback changed from 'pt-BR' to 'en'"
            status: pass
            detail: "language = ward?.language ?? 'en';"
          - check: "All 4 'pt-BR' fallbacks changed to 'en'"
            status: pass
            detail: "grep for pt-BR in send-reset-email shows zero fallback references. The only remaining 'pt-BR' is the template key itself (line 19), which is correct."
        notes: |
          SPEC_CONSOLIDATED F002 updated to say "en (en-US)". Code uses 'en' which
          matches the template map key. All 4 occurrences changed as specified.

      # -----------------------------------------------------------------------
      # CR-252: Remove diagnostic mode
      # -----------------------------------------------------------------------
      - cr: CR-252
        title: "Remove diagnostic mode from create-invitation"
        verdict: pass
        verification:
          - check: "diagnose field removed from CreateInvitationInput interface"
            status: pass
            detail: "Interface now has only email and role fields. No diagnose field."
          - check: "Diagnostic mode block removed (was lines 118-135)"
            status: pass
            detail: "grep for 'diagnos' in create-invitation returns zero matches."
          - check: "Header comment updated"
            status: pass
            detail: |
              Old: "F143 (CR-208): Structured error logging, error codes, and diagnostic mode."
              New: "F143 (CR-208): Structured error logging and error codes."
              The 'and diagnostic mode' text removed. ADR-094 line also removed.
          - check: "isBishopric variable removed from settings/index.tsx (CR-256 side-effect)"
            status: pass
            detail: "const isBishopric = role === 'bishopric' was removed since it's no longer used."
        notes: |
          Clean dead code removal. The diagnostic block, interface field, and
          related comments were all removed. No functional behavior changes.

      # -----------------------------------------------------------------------
      # CR-254: CSV 3-column only
      # -----------------------------------------------------------------------
      - cr: CR-254
        title: "Require 3-column CSV format only"
        verdict: pass
        verification:
          - check: "Header validation changed from 'at least 2' to 'at least 3'"
            status: pass
            file: src/lib/csvUtils.ts
            detail: "if (headerParts.length < 3) with INVALID_HEADER error"
          - check: "2-column fallback logic removed (hasThreeColumns branch)"
            status: pass
            detail: "hasThreeColumns variable and conditional branch eliminated. Direct 3-column parsing only."
          - check: "Row validation changed from 'at least 2' to 'at least 3'"
            status: pass
            detail: "if (parts.length < 3) with INSUFFICIENT_COLUMNS error"
          - check: "Comment updated to say '3-column format'"
            status: pass
            detail: "JSDoc now says 'Requires 3-column format: Name, Informal Name, Phone.'"
          - check: "Error messages updated"
            status: pass
            detail: |
              INVALID_HEADER: 'CSV must have at least 3 columns: Nome, Nome Informal, Telefone Completo'
              INSUFFICIENT_COLUMNS: 'Row must have at least 3 columns'
          - check: "Tests updated for 3-column enforcement"
            status: pass
            detail: |
              26 tests in csvUtils.test.ts. Key new tests:
              - 'rejects 2-column CSV (requires 3 columns)' -> expects INVALID_HEADER
              - 'rejects row with only 2 columns (INSUFFICIENT_COLUMNS)' -> expects INSUFFICIENT_COLUMNS
              - 'rejects 1-column header CSV' -> expects INVALID_HEADER
              - 'defaults informal name to first word of full name when empty'
        notes: |
          SPEC_CONSOLIDATED F007 and BR021 both updated to 3-column only.
          Code, tests, and docs are all consistent.

      # -----------------------------------------------------------------------
      # CR-256: manage_prayers toggle access
      # -----------------------------------------------------------------------
      - cr: CR-256
        title: "Allow secretary to toggle manage_prayers"
        verdict: pass
        verification:
          - check: "Guard changed from {isBishopric && ( to {!isObserver && ("
            status: pass
            file: src/app/(tabs)/settings/index.tsx
            detail: "Line 222: {!isObserver && ("
          - check: "isBishopric variable removed (no longer used)"
            status: pass
            detail: "const isBishopric = role === 'bishopric' was removed from the component."
          - check: "Consistent with parent Ward Settings guard"
            status: pass
            detail: "Parent group (line 209) also uses {!isObserver && (}, so inner toggle is consistent."
          - check: "Integration tests added for all 3 roles"
            status: pass
            detail: |
              Tests in permissions-roles.integration.test.ts:
              - bishopric: manage_prayers toggle visible
              - secretary: manage_prayers toggle visible
              - observer: manage_prayers toggle hidden
        notes: |
          SPEC_CONSOLIDATED F018 and F027 both updated to say "bishopric or secretary".
          RLS policy already allows any authenticated user with ward_id match to
          UPDATE the wards table, so no DB changes needed. UI-only fix.

      # -----------------------------------------------------------------------
      # CR-257: Cron interval comment
      # -----------------------------------------------------------------------
      - cr: CR-257
        title: "Correct cron interval comment to 5 minutes"
        verdict: pass
        verification:
          - check: "Code comment updated from '1 minute' to '5 minutes'"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Line 2: 'process-notifications: Supabase Edge Function (cron every 5 minutes).'"
          - check: "ARCH_CONSOLIDATED updated"
            status: pass
            file: docs/arch/ARCH_CONSOLIDATED.yaml
            detail: "Line 706: 'trigger: Cron (every 5 minutes)'"
          - check: "SPEC_CONSOLIDATED F034 updated"
            status: pass
            detail: "F034 description now says 'cron every 5 minutes'"
        notes: "Doc and comment-only change. No runtime behavior affected."

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_to_arch:
        status: pass
        detail: |
          SPEC_F001 defines 6 corrections (COR-001 through COR-006).
          ARCH_M001 provides architecture assessment for all 6, confirming
          'architecture_impact: none' for all. Modules and files match.

      arch_to_code:
        status: pass
        detail: |
          All files listed in ARCH_M001 were changed in the commits:
          - send-reset-email/index.ts (CR-251)
          - create-invitation/index.ts (CR-252)
          - csvUtils.ts + csvUtils.test.ts (CR-254)
          - settings/index.tsx (CR-256)
          - process-notifications/index.ts (CR-257)
          No extra files changed beyond what ARCH specified.

      spec_consolidated_updates:
        status: pass
        detail: |
          All 8 SPEC_CONSOLIDATED corrections verified in-place:
          - F001: "selected role (bishopric or secretary)" in acceptance criteria
          - F002: "en (en-US)" fallback in description
          - F003: No "diagnostic mode" mention
          - F007: "3-column (Name, Informal Name, Phone) format" only
          - BR021: "3-column format only" with informal name default
          - F018: "bishopric or secretary" in description and acceptance criteria
          - F027: "bishopric or secretary" in description and acceptance criteria
          - F034: "cron every 5 minutes" in description

      arch_consolidated_updates:
        status: pass
        detail: |
          ARCH_CONSOLIDATED process-notifications trigger updated to
          "Cron (every 5 minutes)". Confirmed at line 706.

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 589
      total_files: 30
      all_passing: true
      new_tests:
        - file: src/__tests__/csvUtils.test.ts
          count: 26
          covers: "CR-254 (3-column enforcement, 2-column rejection, informal name defaults)"
        - file: src/__tests__/integration/permissions-roles.integration.test.ts
          covers: "CR-256 (manage_prayers toggle visibility for all 3 roles)"
      coverage_gaps:
        - cr: CR-251
          reason: "Edge Function (Deno runtime) cannot be imported in vitest Node environment"
          risk: low
          mitigation: "Manual verification of all 4 fallback changes via code review"
        - cr: CR-252
          reason: "Edge Function (Deno runtime) - dead code removal"
          risk: low
          mitigation: "grep confirms zero references to 'diagnos' in the file"
        - cr: CR-257
          reason: "Comment-only change, no behavioral code"
          risk: none

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Pre-existing CR-208 reference in create-invitation comment"
        description: |
          Line 5 of create-invitation/index.ts contains:
          "// F143 (CR-208): Structured error logging and error codes."
          This CR-208 reference predates Phase 1 and was not in scope for this
          correction package. The coder correctly updated the text (removed
          "and diagnostic mode") but preserved the CR-208 reference.
          This should be cleaned up in a future pass if CR references are
          being eliminated from source code.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - cr: CR-251
        command: "supabase functions deploy send-reset-email --no-verify-jwt"
        reason: "Edge Function code changed (language fallback)"
        urgency: low
        notes: "Functional change: default email language will be English instead of Portuguese"

      - cr: CR-252
        command: "supabase functions deploy create-invitation"
        reason: "Edge Function code changed (diagnostic mode removed)"
        urgency: low
        notes: "Dead code removal only. No behavioral change for clients."

  # ===========================================================================
  # REV-004: Phase 2 - Language Code Standardization (CR-249)
  # ===========================================================================

  - id: REV-004
    scope: "F040 Phase 2 - Language Code Standardization (CR-249)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F040
    arch: M040
    plan: P002
    iteration: 1
    commits:
      - hash: b0ed8d3
        step: STEP-01
        message: "refactor(i18n): rename locale files and update i18n module to en-US/es-LA codes (CR-249)"
      - hash: 635a850
        step: STEP-02
        message: "refactor(dateUtils): update Record keys and switch cases to en-US/es-LA (CR-249)"
      - hash: 2d7183b
        step: STEP-03
        message: "refactor(whatsappUtils): update switch cases to en-US/es-LA (CR-249)"
      - hash: 08e17e4
        step: STEP-04
        message: "refactor(notificationUtils): update types and Record keys to en-US/es-LA (CR-249)"
      - hash: 69cf1cd
        step: STEP-05
        message: "refactor(edge-functions): update language codes to en-US/es-LA in all 3 Edge Functions (CR-249)"
      - hash: eada216
        step: STEP-06
        message: "refactor(import-hymns): update LANGUAGE_MAP and create migration 024 for language codes (CR-249)"
      - hash: e0ebda8
        step: STEP-07
        message: "test(i18n): update all test files to use en-US/es-LA language codes (CR-249)"
      - hash: bcaedfc
        step: STEP-11
        message: "refactor(i18n): update remaining old language codes in speechUtils and integration tests (CR-249)"
    verdict: approved

    summary: |
      All 8 commits for Phase 2 (CR-249) correctly standardize language codes from
      en/es to en-US/es-LA across the entire codebase. Chain validation passes:
      SPEC_F002 -> ARCH_M002 -> PLAN_P002 -> CODE is fully aligned. All 644 tests
      pass (31 test files, 0 failures). The coder appropriately added STEP-11
      (not in original plan) to catch remaining old codes in speechUtils.ts and
      integration tests. Database migration 024 is idempotent and correct.
      Documentation (SPEC_CONSOLIDATED, ARCH_CONSOLIDATED) updated consistently.
      No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested "Padronizar codigos de idioma: en->en-US, es->es-LA em docs e codigo".
          SPEC_F002 covers all areas: i18n config, date/whatsapp/notification utils,
          3 Edge Functions, import script, DB migration, tests, and docs. 33 acceptance
          criteria defined, comprehensive and faithful to user intent.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M002 maps each SPEC change to an architectural component (C1-C11).
          All 15 SPEC changes have a corresponding component. ADR-007 update,
          deploy order, and risk analysis are all sound. Architecture impact
          correctly assessed as "moderate" (cross-cutting but mechanical).

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P002 has 10 steps covering all 11 ARCH components. Dependency graph
          is correct: STEP-01 first (source of truth), then utilities and Edge
          Functions in parallel, tests last. Validation matrix maps all 33 ACs to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 10 plan steps implemented in 8 commits (some steps combined).
          Additional STEP-11 added by coder to fix previously-missed old codes
          in speechUtils.ts and integration tests. This deviation is justified
          since the PLAN did not anticipate these files but the coder correctly
          caught them during implementation. Files modified match PLAN expectations
          plus the additional speechUtils.ts and 5 integration test files.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION
    # =========================================================================

    step_reviews:

      - step: STEP-01
        title: "Translation file renames + i18n module"
        verdict: pass
        verification:
          - check: "en.json renamed to en-US.json"
            status: pass
            detail: "src/i18n/locales/ contains en-US.json, es-LA.json, pt-BR.json. No en.json or es.json."
          - check: "SUPPORTED_LANGUAGES updated"
            status: pass
            detail: "['pt-BR', 'en-US', 'es-LA'] as const"
          - check: "DB_LOCALE_MAP is identity"
            status: pass
            detail: "{ 'pt-BR': 'pt-BR', 'en-US': 'en-US', 'es-LA': 'es-LA' }"
          - check: "detectDeviceLocale returns en-US/es-LA for prefix match"
            status: pass
            detail: "langPrefix === 'en' -> 'en-US', langPrefix === 'es' -> 'es-LA'"
          - check: "resources use new keys"
            status: pass
            detail: "'en-US': { translation: enUS }, 'es-LA': { translation: esLA }"
          - check: "All exports correct"
            status: pass
            detail: "LANGUAGE_LABELS, DEFAULT_COUNTRY_CODES, DEFAULT_TIMEZONES all keyed by en-US/es-LA"

      - step: STEP-02
        title: "dateUtils Record keys and switch cases"
        verdict: pass
        verification:
          - check: "MONTH_ABBR, MONTH_FULL, DAY_NAMES keys"
            status: pass
            detail: "All use 'en-US' and 'es-LA' keys. No bare 'en' or 'es'."
          - check: "formatDate switch case"
            status: pass
            detail: "case 'en-US': month day format. es-LA falls through to default (day month)."
          - check: "formatDateHumanReadable switch case"
            status: pass
            detail: "case 'en-US': Month Day, Year. es-LA falls through to default."
          - check: "formatFullDate switch case"
            status: pass
            detail: "case 'en-US': DayName, Month Day, Year. es-LA falls through to default."

      - step: STEP-03
        title: "whatsappUtils switch cases"
        verdict: pass
        verification:
          - check: "getDefaultSpeechTemplate switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA' in all 3 position blocks."
          - check: "getDefaultPrayerTemplate switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA' in both opening/closing blocks."
          - check: "grep for case 'en' and case 'es' returns 0"
            status: pass
            detail: "No standalone 'en' or 'es' switch cases remain."

      - step: STEP-04
        title: "notificationUtils types and Record keys"
        verdict: pass
        verification:
          - check: "OrdinalLanguage type"
            status: pass
            detail: "'pt-BR' | 'en-US' | 'es-LA'"
          - check: "ORDINALS Record keys"
            status: pass
            detail: "Keys are 'pt-BR', 'en-US', 'es-LA'"
          - check: "formatNameList comparisons"
            status: pass
            detail: "language === 'en-US' ? ' and ' : language === 'es-LA' ? ' y ' : ' e '"
          - check: "getPrayerLabel keys"
            status: pass
            detail: "Record keys 'pt-BR', 'en-US', 'es-LA'"
          - check: "buildNotificationText top-level Record keys"
            status: pass
            detail: "All 3 language blocks use 'pt-BR', 'en-US', 'es-LA'"

      - step: STEP-05
        title: "Edge Functions (register-first-user, process-notifications, send-reset-email)"
        verdict: pass
        verification:
          - check: "register-first-user switch cases"
            status: pass
            detail: "case 'en-US' and case 'es-LA'. No bare 'en' or 'es'."
          - check: "process-notifications ORDINALS, getPrayerLabel, formatNameList, all text builders"
            status: pass
            detail: "All Record keys use 'en-US' and 'es-LA'. Fallbacks remain 'pt-BR'."
          - check: "send-reset-email template keys"
            status: pass
            detail: "Templates Record: 'pt-BR', 'en-US', 'es-LA'. Fallback: templates['en-US']. Default: let language = 'en-US'. Ward fallback: ward?.language ?? 'en-US'."

      - step: STEP-06
        title: "Import hymns script + DB migration"
        verdict: pass
        verification:
          - check: "LANGUAGE_MAP values"
            status: pass
            detail: "{ 'PT-BR': 'pt-BR', 'EN-US': 'en-US', 'ES-ES': 'es-LA' }"
          - check: "Migration file exists"
            status: pass
            detail: "supabase/migrations/024_language_code_update.sql"
          - check: "Migration SQL correct"
            status: pass
            detail: |
              UPDATE hymns SET language = 'es-LA' WHERE language = 'es-ES';
              UPDATE general_collections SET language = 'es-LA' WHERE language = 'es-ES';
              UPDATE wards SET language = 'en-US' WHERE language = 'en';
              UPDATE wards SET language = 'es-LA' WHERE language = 'es';
          - check: "Migration is idempotent"
            status: pass
            detail: "WHERE clauses prevent double-update. Running twice is safe."

      - step: STEP-07
        title: "Test file updates"
        verdict: pass
        verification:
          - check: "i18n-module.test.ts updated"
            status: pass
          - check: "whatsapp-utils.test.ts updated"
            status: pass
          - check: "dateUtils.test.ts updated"
            status: pass
          - check: "i18n.test.ts imports updated"
            status: pass
          - check: "useNotifications-utils.test.ts updated"
            status: pass
          - check: "useTopics-utils.test.ts updated"
            status: pass
          - check: "All 644 tests pass"
            status: pass
            detail: "31 test files, 644 tests, 0 failures."

      - step: STEP-11 (additional, not in original plan)
        title: "speechUtils and integration tests"
        verdict: pass
        verification:
          - check: "speechUtils.ts type signatures updated"
            status: pass
            detail: "formatDateFn type changed from 'pt-BR' | 'en' | 'es' to 'pt-BR' | 'en-US' | 'es-LA'"
          - check: "Integration tests updated"
            status: pass
            detail: |
              5 integration test files and useHymns-utils.test.ts updated
              to use en-US/es-LA instead of en/es.

    # =========================================================================
    # DOC UPDATES VERIFICATION
    # =========================================================================

    doc_updates:
      spec_consolidated:
        status: pass
        detail: |
          - product_description: "pt-BR, en-US, es-LA" (correct)
          - F008: "locale codes: pt-BR, en-US, es-LA" (correct)
          - F028: "Supported languages: pt-BR, en-US, es-LA" (correct)
          - F040 feature entry exists (correct)
          - total_features: 39 (correct, was 38 + F040)
          - No old 'en' or 'es' standalone language codes remain

      arch_consolidated:
        status: pass
        detail: |
          - tech_stack.i18n.languages: ["pt-BR", "en-US", "es-LA"] (correct)
          - total_migrations: 24 (correct)
          - ADR-007: note about unified codes after CR-249 (correct)

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Pre-existing CR references in register-first-user comments"
        description: |
          Lines 90-91 of supabase/functions/register-first-user/index.ts contain:
          "// CR-231: Per-position default speech templates (3 per language)"
          "// CR-248: Add {nome} placeholder, update text, add prayer templates"
          These CR references predate Phase 2 and were not in scope for CR-249.
          Should be cleaned up in a future pass if CR references are being
          eliminated from source code.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration"
        command: "supabase db push (or apply 024_language_code_update.sql)"
        reason: "DB data must use new codes before app/functions read them"
        urgency: medium
        notes: "Must run BEFORE Edge Functions and app update. Migration is idempotent."

      - step: 2
        action: "Deploy Edge Functions"
        commands:
          - "supabase functions deploy register-first-user"
          - "supabase functions deploy process-notifications"
          - "supabase functions deploy send-reset-email --no-verify-jwt"
        reason: "Edge Functions must handle new language codes from DB"
        urgency: medium
        notes: "Deploy after migration. All 3 functions have updated language code keys."

      - step: 3
        action: "Deploy app update"
        reason: "App sends new codes and expects new codes from DB"
        urgency: medium
        notes: "Deploy after migration and Edge Functions."

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files: []

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 20
      total_commits: 8
      tests_total: 644
      tests_passing: 644
      test_files: 31

  # ===========================================================================
  # REV-005: Phase 3 - Permission Model + Actor Role Enum (CR-253, CR-255)
  # ===========================================================================

  - id: REV-005
    scope: "F041/F042 Phase 3 - Permission Model Rework (CR-253) + Actor Single Role Enum (CR-255)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    spec: F041, F042
    arch: M005
    plan: P003
    iteration: 1
    commits:
      # CR-253 (F041): Permission Model Rework
      - hash: f8c9c02
        step: STEP-01
        message: "feat(permissions): add prayer:assign/prayer:unassign and update secretary permissions (CR-253)"
      - hash: 31f2cb7
        step: STEP-02
        message: "feat(SpeechSlot): make canAssign/canUnassign position-aware for prayer vs speech (CR-253)"
      - hash: 3ba7280
        step: STEP-03
        message: "test(permissions): update permission matrix tests for prayer permissions and secretary changes (CR-253)"
      - hash: a4d6311
        step: STEP-11a
        message: "test(integration): update permissions-roles tests for secretary prayer permissions (CR-253)"
      # CR-255 (F042): Actor Single Role Enum
      - hash: 5403ddf
        step: STEP-04
        message: "refactor(types): add ActorRole type and replace boolean flags with single role field (CR-255)"
      - hash: 14f3b78
        step: STEP-05
        message: "refactor(useActors): replace boolean flags with single role field and remove enforceActorRules (CR-255)"
      - hash: 6f30f03
        step: STEP-06
        message: "refactor(ActorSelector): use single role field in handleAdd (CR-255)"
      - hash: bf7c770
        step: STEP-07
        message: "refactor(AgendaForm): update roleFilter values from can_X to direct role names (CR-255)"
      - hash: 5aaeba3
        step: STEP-08
        message: "test(useActors): update tests for single role field and remove enforceActorRules tests (CR-255)"
      - hash: e4aaa21
        step: STEP-09
        message: "feat(migration): add migration 025 to replace actor boolean flags with single role enum (CR-255)"
      - hash: eeb4bb9
        step: STEP-11b
        message: "refactor(tests): update remaining actor boolean references to single role field (CR-255)"
      # Documentation
      - hash: 5a9eab9
        step: STEP-10
        message: "docs(phase-3): add Phase 3 architecture, spec, plan, and QA docs (CR-253, CR-255)"
    verdict: approved

    summary: |
      All 12 commits for Phase 3 (CR-253, CR-255) correctly implement the permission
      model rework and actor single role enum refactoring. Chain validation passes:
      SPEC -> ARCH -> PLAN -> CODE is fully aligned. All 703 tests pass (32 test files,
      0 failures). One P2 documentation issue found: SPEC_F003 and SPEC_CONSOLIDATED
      BR009 have incorrect permission counts (say 25/22 but should be 26/23). The code
      and tests are correct with 26/23. This is a cosmetic doc error that does not
      block approval. No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested two changes:
          1. CR-253: Fix secretary/observer permissions - secretary cannot assign speakers
             for pos 1-3 but CAN for prayers (pos 0,4), CAN change status. Topics read-only.
          2. CR-255: Actor role as single enum (preside, conduct, recognize, pianist, conductor).
          SPEC_F003 covers all CR-253 requirements: new permissions prayer:assign/prayer:unassign,
          secretary restriction from speech:assign/speech:unassign, position-aware SpeechSlot.
          SPEC_F003 covers all CR-255 requirements: ActorRole type, single role field,
          migration 025, type changes, filtering changes.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M003 maps each SPEC change to architectural components:
          F041: C1 (types), C2 (permissions.ts), C3 (SpeechSlot.tsx) - 3 components.
          F042: C4 (types), C5 (useActors.ts), C6 (ActorSelector.tsx),
                C7 (AgendaForm.tsx), C8 (migration 025) - 5 components.
          ADR-015 (position-based permission branching) and ADR-016 (single role enum)
          properly document architectural decisions. Deploy order is correct
          (migration 025 before app update).

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P003 has 11 steps covering all 8 ARCH components plus tests and docs.
          Dependency graph is correct: F041 types first (STEP-01), then SpeechSlot
          (STEP-02), then F042 types (STEP-04) through components (STEP-05 to STEP-07),
          tests (STEP-03, STEP-08), migration (STEP-09), docs (STEP-10), final
          verification (STEP-11). Validation matrix maps all ACs to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 11 plan steps implemented in 12 commits (STEP-11 split into 11a and 11b
          for CR-253 and CR-255 respectively). Files modified match PLAN expectations
          exactly. Additional files in STEP-11b (activityLog.ts, database-types.test.ts,
          setup-integration.ts, hooks-supabase.integration.test.ts) are justified
          catch-up changes for remaining boolean references. The coder correctly
          preserved backward compatibility in activityLog.ts for old can_X format
          entries.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION: CR-253 (F041) - Permission Model Rework
    # =========================================================================

    step_reviews_cr253:

      - step: STEP-01
        title: "Add prayer permissions to types and permission matrix"
        verdict: pass
        verification:
          - check: "Permission type has prayer:assign and prayer:unassign"
            status: pass
            detail: "Lines 14-15 of database.ts: | 'prayer:assign' | 'prayer:unassign'"
          - check: "bishopric set has 26 permissions including prayer:*"
            status: pass
            detail: "Counted 26 entries in bishopric Set. Includes prayer:assign and prayer:unassign."
          - check: "secretary set has 23 permissions (no speech:assign/unassign, has prayer:*)"
            status: pass
            detail: |
              Counted 23 entries in secretary Set. Does NOT contain speech:assign or
              speech:unassign. Contains prayer:assign and prayer:unassign.
          - check: "observer set unchanged at 3 permissions"
            status: pass
            detail: "member:read, agenda:read, presentation:start. No prayer permissions."
          - check: "ALL_PERMISSIONS has 26 entries"
            status: pass
            detail: "Counted 26 entries in ALL_PERMISSIONS array. Includes both prayer:* entries."

      - step: STEP-02
        title: "SpeechSlot position-aware permission ternaries"
        verdict: pass
        verification:
          - check: "canAssign uses position-aware ternary"
            status: pass
            detail: "Line 96: const canAssign = hasPermission(isPrayer ? 'prayer:assign' : 'speech:assign');"
          - check: "canUnassign uses position-aware ternary"
            status: pass
            detail: "Line 97: const canUnassign = hasPermission(isPrayer ? 'prayer:unassign' : 'speech:unassign');"
          - check: "canChangeStatus is NOT position-aware (unchanged)"
            status: pass
            detail: "Line 98: const canChangeStatus = hasPermission('speech:change_status'); -- no ternary, applies to all positions"
          - check: "isPrayer prop used correctly"
            status: pass
            detail: "isPrayer defaults to false (line 88). Positions 0,4 have isPrayer=true from parent."
          - check: "Topic row gated by same canAssign"
            status: pass
            detail: "Topic press handler (line 115) checks canAssign. Topic X button (line 286) checks canAssign. Both use same position-aware value."
          - check: "Speaker row gated by canAssign/canUnassign"
            status: pass
            detail: "Speaker press (line 110) checks canAssign. Chevron (line 244) checks canAssign. X button (line 249) checks canUnassign."

      - step: STEP-03
        title: "Permission matrix tests"
        verdict: pass
        verification:
          - check: "EXPECTED_MATRIX has 26 rows"
            status: pass
            detail: "Matrix includes prayer:assign and prayer:unassign with correct values."
          - check: "speech:assign secretary = false"
            status: pass
            detail: "Line 15: 'speech:assign': { bishopric: true, secretary: false, observer: false }"
          - check: "speech:unassign secretary = false"
            status: pass
            detail: "Line 16: 'speech:unassign': { bishopric: true, secretary: false, observer: false }"
          - check: "prayer:assign secretary = true"
            status: pass
            detail: "Line 18: 'prayer:assign': { bishopric: true, secretary: true, observer: false }"
          - check: "prayer:unassign secretary = true"
            status: pass
            detail: "Line 19: 'prayer:unassign': { bishopric: true, secretary: true, observer: false }"
          - check: "Bishopric count test expects 26"
            status: pass
            detail: "Line 127: expect(perms.length).toBe(26)"
          - check: "Secretary count test expects 23"
            status: pass
            detail: "Line 131: expect(perms.length).toBe(23)"
          - check: "ALL_PERMISSIONS count test expects 26"
            status: pass
            detail: "Line 151: expect(ALL_PERMISSIONS.length).toBe(26)"

      - step: STEP-11a
        title: "Integration tests for secretary prayer permissions"
        verdict: pass
        verification:
          - check: "Integration test file updated"
            status: pass
            detail: "permissions-roles.integration.test.ts updated with prayer permission assertions"

    # =========================================================================
    # PER-STEP VERIFICATION: CR-255 (F042) - Actor Single Role Enum
    # =========================================================================

    step_reviews_cr255:

      - step: STEP-04
        title: "ActorRole type and interface changes"
        verdict: pass
        verification:
          - check: "ActorRole type defined"
            status: pass
            detail: "Line 157: export type ActorRole = 'preside' | 'conduct' | 'recognize' | 'pianist' | 'conductor';"
          - check: "MeetingActor has single role field"
            status: pass
            detail: "Line 163: role: ActorRole (no boolean fields)"
          - check: "CreateActorInput has required role"
            status: pass
            detail: "Line 322: role: ActorRole (not optional)"
          - check: "UpdateActorInput has optional role"
            status: pass
            detail: "Line 328: role?: ActorRole"
          - check: "No boolean role fields remain in any interface"
            status: pass
            detail: "grep for can_preside in database.ts returns 0 matches"

      - step: STEP-05
        title: "useActors hook refactor"
        verdict: pass
        verification:
          - check: "ActorRoleFilter uses new values"
            status: pass
            detail: "'all' | 'preside' | 'conduct' | 'recognize' | 'pianist' | 'conductor' (no can_ prefix)"
          - check: "enforceActorRules removed"
            status: pass
            detail: "grep for enforceActorRules in useActors.ts returns 0 matches"
          - check: "filterActorsByRole uses a.role === role"
            status: pass
            detail: "Line 38: return actors.filter((a) => a.role === role);"
          - check: "getPrimaryRole returns actor.role directly"
            status: pass
            detail: "Line 54: return actor.role;"
          - check: "useActors query filters by role column"
            status: pass
            detail: "Line 76: query = query.eq('role', roleFilter);"
          - check: "useCreateActor inserts { ward_id, name, role }"
            status: pass
            detail: "Lines 97-99: insert({ ward_id: wardId, name: input.name, role: input.role })"

      - step: STEP-06
        title: "ActorSelector handleAdd update"
        verdict: pass
        verification:
          - check: "handleAdd uses role field"
            status: pass
            detail: "Line 117-120: const input: CreateActorInput = { name: trimmed, role: roleFilter as ActorRole };"
          - check: "ActorRole imported"
            status: pass
            detail: "Line 26: import type { MeetingActor, CreateActorInput, ActorRole } from '../types/database';"

      - step: STEP-07
        title: "AgendaForm roleFilter values"
        verdict: pass
        verification:
          - check: "All 5 roleFilter values updated"
            status: pass
            detail: |
              Line 158: roleFilter: 'preside' (was 'can_preside')
              Line 179: roleFilter: 'conduct' (was 'can_conduct')
              Line 198: roleFilter: 'recognize' (was 'can_recognize')
              Line 259: roleFilter: 'pianist' (was 'can_pianist')
              Line 280: roleFilter: 'conductor' (was 'can_conductor')
          - check: "No can_ prefix remains in AgendaForm"
            status: pass
            detail: "grep for 'can_preside' in AgendaForm.tsx returns 0 matches"

      - step: STEP-08
        title: "useActors test updates"
        verdict: pass
        verification:
          - check: "makeActor uses role field"
            status: pass
            detail: "Line 14: role: overrides.role ?? 'preside' (no boolean defaults)"
          - check: "filterActorsByRole tests use new role values"
            status: pass
            detail: "All test cases use role: 'preside', role: 'conduct', etc."
          - check: "actorKeys tests use new role values"
            status: pass
            detail: "Line 94: actorKeys.byRole('ward-1', 'preside')"
          - check: "No enforceActorRules tests remain"
            status: pass
            detail: "grep for enforceActorRules in test file returns 0 matches"

      - step: STEP-09
        title: "Migration 025"
        verdict: pass
        verification:
          - check: "Adds role column with DEFAULT"
            status: pass
            detail: "Line 6: ALTER TABLE meeting_actors ADD COLUMN role TEXT NOT NULL DEFAULT 'preside';"
          - check: "Migrates data with priority order"
            status: pass
            detail: "Lines 9-16: CASE WHEN can_preside=true THEN 'preside' ... ELSE 'preside' END"
          - check: "Drops 5 boolean columns"
            status: pass
            detail: "Lines 19-24: DROP COLUMN can_preside, can_conduct, can_recognize, can_pianist, can_conductor"
          - check: "Drops old constraint"
            status: pass
            detail: "Line 27: DROP CONSTRAINT IF EXISTS chk_pianist_conductor_exclusive"
          - check: "Adds new CHECK constraint"
            status: pass
            detail: "Lines 30-31: ADD CONSTRAINT chk_actor_role CHECK (role IN (...))"
          - check: "Migration is idempotent-safe"
            status: pass
            detail: "DROP CONSTRAINT uses IF EXISTS. Second run would fail on ADD COLUMN (which is acceptable for a one-way migration)."

      - step: STEP-11b
        title: "Remaining boolean references cleanup"
        verdict: pass
        verification:
          - check: "activityLog.ts backward compatibility"
            status: pass
            detail: |
              ACTOR_ROLE_I18N_KEYS has both new (preside, conduct, etc.) and old
              (can_preside, can_conduct, etc.) keys. Both map to the same i18n keys.
              This correctly handles historical activity log entries.
          - check: "database-types.test.ts updated"
            status: pass
            detail: "MeetingActor test uses role: 'preside'. CreateActorInput test uses role: 'preside'. ActorRole type imported."
          - check: "setup-integration.ts updated"
            status: pass
            detail: "createMockActor uses role: 'preside' (no boolean fields)"
          - check: "hooks-supabase.integration.test.ts updated"
            status: pass
            detail: "Actor mock data uses role field"

    # =========================================================================
    # DOCUMENTATION UPDATES VERIFICATION
    # =========================================================================

    doc_updates:

      spec_consolidated:
        status: pass
        detail: |
          - F005: Description mentions prayer:assign/prayer:unassign, secretary limited access
          - F009: Description says single role field, CHECK constraint
          - F014: Position-aware permission note
          - F015: Secretary read-only for speech positions
          - BR009: Updated with new permission model (counts are 25/22 but code has 26/23 - see P2 issue)
          - BR014: Marked as REMOVED by CR-255
          - EC009: Marked as REMOVED by CR-255
          - F041 feature entry exists with full description
          - F042 feature entry exists with full description
          - total_features: 41 (was 39 + F041 + F042)

      arch_consolidated:
        status: pass
        detail: |
          - M004 description: "Each actor has a single role enum field" (correct)
          - M004 dependencies: CHECK constraint updated to role-based (correct)
          - meeting_actors key_columns: role TEXT column (correct)
          - meeting_actors constraints: new CHECK constraint (correct)
          - total_migrations: 25 (correct)
          - ADR-015 exists: Position-based permission branching
          - ADR-016 exists: Single role enum for meeting actors

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: P2-001
        severity: P2
        category: quality
        location: "docs/specs/SPEC_F003.yaml + docs/specs/SPEC_CONSOLIDATED.yaml"
        description: |
          Permission count mismatch between SPEC documents and actual code/tests:
          - SPEC_F003 BR009 says "Bishopric: 25 permissions" -> actual code has 26
          - SPEC_F003 BR009 says "Secretary: 22 permissions" -> actual code has 23
          - SPEC_CONSOLIDATED BR009 copies the same wrong counts (25/22)
          The code (permissions.ts) and tests (permissions.test.ts, f041-f042-phase3.test.ts)
          are consistent with each other at 26/23. The original Permission type had 24 values.
          Adding prayer:assign and prayer:unassign brings bishopric to 24+2=26.
          Secretary had 23 (all except home:next_assignments). Remove 2 + add 2 = 23.
          The SPEC incorrectly subtracted 1 from the starting counts.
        suggestion: |
          Update BR009 in both SPEC_F003.yaml and SPEC_CONSOLIDATED.yaml:
          - Bishopric: 25 -> 26
          - Secretary: 22 -> 23

    # =========================================================================
    # ADVISORY NOTES
    # =========================================================================

    advisory_notes:
      - id: ADV-001
        severity: P2
        title: "Backward-compatible can_ prefix in activityLog.ts"
        description: |
          activityLog.ts has ACTOR_ROLE_I18N_KEYS with both new (preside) and old
          (can_preside) keys. The old keys are backward compatibility for historical
          activity log entries stored in the database. This is by design per SPEC_F003
          decisions section. The activityLog.test.ts test at line 224 uses 'can_preside'
          to verify backward compatibility works. This is correct behavior.

      - id: ADV-002
        severity: P2
        title: "SPEC_F003 PLAN step count in done_when has arithmetic notes"
        description: |
          PLAN_P003 STEP-01 done_when has a note "old had 23 including speech:assign/unassign;
          remove 2, add 2 = 23... wait: old had 23..." showing the planner was confused about
          the count. The final plan values are inconsistent but the coder implemented the
          correct counts (26/23). Cosmetic plan artifact.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 025"
        command: "supabase db push (or apply 025_actor_single_role.sql)"
        reason: "DB must have 'role' column before app tries to query it"
        urgency: medium
        notes: |
          Must run BEFORE app update. Old app will break if migration runs first
          (queries can_preside column that no longer exists). Deploy sequence:
          migration -> app update with minimal gap. Migration is one-way
          (boolean-to-enum).

      - step: 2
        action: "Deploy app update"
        reason: "All TypeScript changes (permissions, SpeechSlot, actors) go live together"
        urgency: medium
        notes: |
          F041 (permission changes) and F042 (actor refactor) both take effect with
          the app update. No Edge Functions affected by Phase 3.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 16
      total_commits: 12
      tests_total: 703
      tests_passing: 703
      test_files: 32

  # ===========================================================================
  # REV-006: Phase 004 (CR-259, CR-260, CR-261) - Quick UX Fixes
  # ===========================================================================

  - id: REV-006
    scope: "Phase 004: CR-259 (Play Icon), CR-260 (Remove Agenda History), CR-261 (User Language from Ward)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: 7fb10dc
        message: "feat(agenda): restyle play button to filled primary container (CR-259)"
      - hash: 9d1523b
        message: "refactor(useAgenda): remove agenda:edit logAction call and unused imports (CR-260)"
      - hash: 63b114a
        message: "refactor(i18n): remove agenda-related activity log keys from all locales (CR-260)"
      - hash: 83fbe83
        message: "feat(migration): add migration 026 to delete agenda activity log entries (CR-260)"
      - hash: 3e5a932
        message: "feat(register-first-user): set user_metadata.language to ward language (CR-261)"
      - hash: 1e50b40
        message: "feat(register-invited-user): fetch ward language and set user_metadata.language (CR-261)"
    verdict: approved

    summary: |
      All 6 committed code changes across 3 CRs are correct, well-structured, and
      align with SPEC, ARCH, and PLAN. Chain validation passes on all 4 links. The
      implementation is minimal and focused. No P0 or P1 issues found. One P2
      advisory for pre-existing dead code in useAgenda.ts (not introduced by this phase).

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request: pass
      spec_vs_request_notes: |
        SPEC F043 covers CR-259 (play icon redesign) with 4 ACs.
        SPEC F044 covers CR-260 (remove agenda history) with 4 ACs.
        SPEC F045 covers CR-261 (user language from ward) with 5 ACs.
        All user requirements are covered. No scope creep.

      arch_vs_spec: pass
      arch_vs_spec_notes: |
        ARCH M004 defines 6 components (C1-C6) that map to all SPEC ACs.
        C1 for F043 (agenda.tsx style change).
        C2-C4 for F044 (logAction removal, i18n cleanup, migration).
        C5-C6 for F045 (register-first-user, register-invited-user).
        All components align with SPEC. No extra components.

      plan_vs_arch: pass
      plan_vs_arch_notes: |
        PLAN P004 has 7 steps (STEP-01 to STEP-07).
        STEP-01 implements C1, STEP-02 implements C2, STEP-03 implements C3,
        STEP-04 implements C4, STEP-05 implements C5, STEP-06 implements C6,
        STEP-07 is verification. All steps match ARCH components.

      code_vs_plan: pass
      code_vs_plan_notes: |
        6 commits match STEP-01 through STEP-06. Each commit modifies
        exactly the files specified in the PLAN. Implementation details
        match the PLAN's done_when criteria. No extra changes.

      chain_intact: true

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:

      - id: R6-1
        severity: P2
        category: quality
        location: "src/hooks/useAgenda.ts:164"
        description: |
          Pre-existing dead code: useUpdateAgendaByDate destructures
          `user` and `userName` from useAuth() but never uses them.
          This was NOT introduced by CR-260 (the function never had a
          logAction call), so it is pre-existing technical debt.
        suggestion: |
          Clean up in a future refactoring pass: change line 164 from
          `const { wardId, user, userName } = useAuth()` to
          `const { wardId } = useAuth()`.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 026_delete_agenda_activity_log.sql"
        command: "supabase db push (or apply 026_delete_agenda_activity_log.sql)"
        reason: "Delete existing agenda-related activity_log entries per CR-260"
        urgency: low
        notes: |
          Data-only migration (DELETE, no schema changes). Safe to run before
          or after app deploy. Safe on empty tables.

      - step: 2
        action: "Deploy Edge Functions (register-first-user, register-invited-user)"
        command: "supabase functions deploy register-first-user && supabase functions deploy register-invited-user"
        reason: "CR-261: new users get user_metadata.language set from ward language"
        urgency: low
        notes: |
          Edge Functions deploy independently. New users created after deploy
          will have user_metadata.language set. Existing users are unaffected.

      - step: 3
        action: "Deploy app update"
        reason: "CR-259 (play button style) and CR-260 (remove logAction + i18n keys)"
        urgency: low
        notes: |
          All three deploy steps are independent and can happen in any order.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M004.yaml"
        - "docs/plans/PLAN_P004.yaml"
        - "docs/qa/QA_PLAN_004.yaml"
        - "docs/specs/SPEC_F004.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 8
      total_commits: 6
      tests_total: 776
      tests_passing: 776
      test_files: 33

  # ===========================================================================
  # REV-005: Phase 5 - CR-258 Speech Count per Member (Last 6 Months)
  # ===========================================================================

  - id: REV-005
    scope: "CR-258 - Speech Count per Member (Last 6 Months)"
    date: "2026-03-03"
    reviewer: reviewer-agent
    phase: 5
    iteration: 1
    feature: F046
    commits:
      - hash: f68be6d
        step: STEP-01
        message: "feat(migration): add partial index for speech count query (CR-258)"
      - hash: 0534e7d
        step: STEP-02
        message: "feat(i18n): add speechCount singular/plural keys in 3 languages (CR-258)"
      - hash: fc64aff
        step: STEP-03
        message: "feat(useSpeechCounts): add bulk speech count hook with client-side aggregation (CR-258)"
      - hash: f899e8e
        step: STEP-04
        message: "feat(sync): invalidate speechCounts on speech table changes (CR-258)"
      - hash: c84e991
        step: STEP-05
        message: "feat(members): display speech count below member info in member list (CR-258)"
      - hash: cf4c358
        step: STEP-06
        message: "feat(MemberSelectorModal): display speech count below member name in selector (CR-258)"
      - hash: eab3ed1
        step: STEP-07
        message: "test(sync): update sync test to expect speechCounts in speeches query keys (CR-258)"
    verdict: approved

    summary: |
      CR-258 (Speech Count per Member) is fully implemented with 7 commits
      following the PLAN exactly. Chain validation passes on all 4 levels.
      The implementation creates a new useSpeechCounts hook with bulk query
      and client-side aggregation, adds a partial DB index, displays counts
      in MemberRow and MemberSelectorModal, adds i18n keys in 3 languages
      with singular/plural support, and updates sync.ts for realtime
      invalidation. All 794 tests pass (18 new for F046). No P0 or P1 issues
      found. One migration (027) needs deployment.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        verdict: pass
        notes: |
          CR-258 request: "Show speech count in last 6 months per member. Display
          in (1) member list in settings, (2) assignment speaker list. Text:
          'X discursos nos ultimos 6 meses'. Hide if zero. Smart DB strategy
          to avoid excessive queries."

          SPEC_F005 F046 covers ALL of these: 12 acceptance criteria mapping to
          the two display locations, hide-if-zero, singular/plural text, bulk
          query strategy, 6-month window, prayer exclusion, index optimization,
          i18n in 3 languages, all roles visibility, and query invalidation.

      arch_vs_spec:
        verdict: pass
        notes: |
          ARCH_M005 maps directly to SPEC_F005. 6 components (C1-C6) cover
          all ACs: C1 (useSpeechCounts hook), C2 (MemberRow), C3 (MemberSelectorModal),
          C4 (migration), C5 (i18n), C6 (sync). ADR-017 (client-side aggregation)
          and ADR-018 (dedicated query key) are sound. Dependency graph and
          implementation order are correct.

      plan_vs_arch:
        verdict: pass
        notes: |
          PLAN_P005 has 7 steps mapping 1:1 to ARCH components plus a
          verification step. Step dependencies respect ARCH dependency graph.
          done_when criteria are specific and testable. Validation matrix covers
          all 12 ACs and 10 edge cases.

      code_vs_plan:
        verdict: pass
        notes: |
          CODE_LEDGER shows 7 commits (f68be6d through eab3ed1) matching
          PLAN steps 1-7. Each commit modifies exactly the files listed in the
          corresponding PLAN step. Code implements precisely what was specified:
          - STEP-01: migration with correct partial index syntax
          - STEP-02: i18n keys with _one/_other suffixes in all 3 locales
          - STEP-03: useSpeechCounts with exact query, Map aggregation, memoized dates
          - STEP-04: sync.ts with speechCountKeys.all added to speeches entry
          - STEP-05: MemberRow with speechCount prop, conditional rendering, style
          - STEP-06: MemberSelectorModal with inline speechCounts.get lookup
          - STEP-07: sync integration test updated

      chain_intact: true

    # =========================================================================
    # CODE REVIEW DETAILS
    # =========================================================================

    code_review:

      migration_027:
        file: supabase/migrations/027_speech_count_index.sql
        verdict: pass
        notes: |
          Correct partial composite index:
          CREATE INDEX idx_speeches_member_count
            ON speeches(ward_id, member_id, position, sunday_date)
            WHERE member_id IS NOT NULL;
          Column order is appropriate (ward_id for equality, then member_id
          for grouping, position for IN filter, sunday_date for range).
          Partial index correctly excludes NULL member_id rows.
          Comment references CR-258.

      useSpeechCounts_hook:
        file: src/hooks/useSpeechCounts.ts
        verdict: pass
        notes: |
          Clean implementation:
          - speechCountKeys factory with .all and .byWard (correct pattern)
          - cutoffDate and today memoized with useMemo (correct deps: [])
          - Query: .select('member_id').eq('ward_id', wardId).in('position', [1,2,3])
            .not('member_id', 'is', null).gte('sunday_date', cutoffDate)
            .lte('sunday_date', today) -- matches ARCH exactly
          - Client-side Map aggregation: O(n) loop, correct get/set pattern
          - enabled: !!wardId -- correct guard
          - Returns data ?? new Map() -- safe default for consumers
          - No unnecessary dependencies, clean 58-line file

      sync_ts:
        file: src/lib/sync.ts
        verdict: pass
        notes: |
          speechCountKeys imported from correct path.
          TABLE_TO_QUERY_KEYS.speeches now includes both speechKeys.all and
          speechCountKeys.all. Existing functionality unchanged.

      members_tsx:
        file: src/app/(tabs)/settings/members.tsx
        verdict: pass
        notes: |
          - useSpeechCounts imported and called in MembersScreen
          - MemberRowProps extended with speechCount: number
          - speechCount passed via speechCounts.get(item.id) ?? 0 in renderItem
          - speechCounts added to renderItem useCallback dependencies
          - Conditional rendering: speechCount > 0 && <Text ...>
          - Style: memberSpeechCount { fontSize: 12, marginTop: 2 }
          - Text uses t('members.speechCount', { count: speechCount })
          - Editing mode correctly shows MemberEditor (no speech count shown)

      member_selector_modal:
        file: src/components/MemberSelectorModal.tsx
        verdict: pass
        notes: |
          - useSpeechCounts imported and called inside MemberSelectorModal
          - Inline lookup: (speechCounts.get(item.id) ?? 0) > 0 &&
          - Same style as members.tsx (fontSize: 12, marginTop: 2)
          - Text uses same i18n key with count interpolation
          - Consistent with MemberRow implementation

      i18n_keys:
        files:
          - src/i18n/locales/pt-BR.json
          - src/i18n/locales/en-US.json
          - src/i18n/locales/es-LA.json
        verdict: pass
        notes: |
          All 3 files have identical key structure:
          - speechCount_one: singular form (count=1)
          - speechCount_other: plural form (count!=1)
          pt-BR: "discurso/discursos nos ultimos 6 meses"
          en-US: "speech/speeches in the last 6 months"
          es-LA: "discurso/discursos en los ultimos 6 meses"
          Keys are inside the "members" object. Valid JSON confirmed by
          existing i18n tests passing.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found. Implementation is clean and complete.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Run database migration 027_speech_count_index.sql"
        command: "supabase db push (or apply 027_speech_count_index.sql)"
        reason: "Creates partial composite index idx_speeches_member_count for optimized speech count query"
        urgency: low
        notes: |
          Index-only migration, no schema changes. Safe to run before or after
          app deploy. The feature works without the index (just slower), so
          deploy order is flexible.

      - step: 2
        action: "Deploy app update"
        reason: "CR-258: speech count display in member list and speaker selector"
        urgency: low
        notes: |
          All code changes (hook, UI, i18n, sync) ship together. The feature
          is purely additive and read-only; no risk of breaking existing functionality.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M005.yaml"
        - "docs/plans/PLAN_P005.yaml"
        - "docs/qa/QA_PLAN_005.yaml"
        - "docs/specs/SPEC_F005.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 8
      total_commits: 7
      tests_total: 794
      tests_passing: 794
      test_files: 35

  # ===========================================================================
  # REV-006: Phase 6 - CR-262 Optimized Offline Mode (F047-F052)
  # ===========================================================================

  - id: REV-006
    scope: "CR-262 - Optimized Offline Mode (F047-F052)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: b13281a
        message: "feat(offline): add PersistQueryClientProvider for cache persistence (CR-262)"
      - hash: 12630b9
        message: "feat(offline): add OnlineStatusContext and wire into SyncProvider (CR-262)"
      - hash: 46c46d4
        message: "feat(offline): add showChevron prop to SundayCard (CR-262)"
      - hash: cfd1534
        message: "feat(offline): add disabled prop to AgendaForm and SpeechSlot (CR-262)"
      - hash: 3d5f302
        message: "feat(offline): add useOfflinePrefetch hook for proactive cache warming (CR-262)"
      - hash: 2703f51
        message: "feat(offline): wire useOfflinePrefetch into SyncProvider (CR-262)"
      - hash: 1fb7f2e
        message: "feat(offline): adapt Home tab UI for offline mode (CR-262)"
      - hash: f93a8de
        message: "feat(offline): adapt Agenda tab UI for offline mode (CR-262)"
      - hash: 13589d8
        message: "feat(offline): adapt Speeches tab UI for offline mode (CR-262)"
      - hash: 344a195
        message: "feat(offline): hide Users item in Settings when offline (CR-262)"
    verdict: approved

    summary: |
      CR-262 Optimized Offline Mode is fully implemented across 10 commits following
      the PLAN_P006 step sequence. All 6 features (F047-F052) correctly implemented:
      cache persistence via PersistQueryClientProvider, OnlineStatusContext for offline
      state distribution, proactive prefetch hook, and per-tab UI adaptation (Home,
      Agenda, Speeches, Settings). No database changes, no Edge Functions, purely
      client-side. 943 tests pass (47 test files), 12 new test files cover all ACs
      and edge cases. No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          SPEC_F006 covers all 6 features requested for offline mode: cache persistence
          (F047), proactive prefetch (F048), Home tab offline (F049), Agenda tab offline
          (F050), Speeches tab offline (F051), Settings tab offline (F052). All ACs are
          faithful to the user's request for read-only offline mode with cached data.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M006 maps 11 components (C1-C11) to the 6 features. Architecture correctly
          identifies: no new DB tables, no new migrations, no Edge Functions. Impact
          assessment is "medium" (correct - modifies layout, sync, all 4 tabs, 2 components).
          Design decisions (PersistQueryClientProvider, OnlineStatusContext, prefetchQuery)
          are sound and consistent with existing patterns.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P006 has 11 steps covering all 11 ARCH components. Dependency graph is
          correct: infrastructure steps (STEP-01 to STEP-04) are parallelizable, STEP-06
          depends on STEP-02 and STEP-05, tab UI steps (STEP-07 to STEP-10) depend on
          STEP-06. STEP-11 is final verification. Validation matrix maps all 27 ACs and
          12 edge cases to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 11 plan steps implemented in 10 commits. STEP-02 and STEP-06 were partially
          combined (OnlineStatusContext creation and SyncProvider wiring happened in 2
          commits instead of exactly matching step boundaries). This is a minor deviation
          that does not affect correctness. All files modified match PLAN expectations.
          All done_when criteria are satisfied. All tests pass.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION
    # =========================================================================

    step_reviews:

      - step: STEP-01
        title: "PersistQueryClientProvider + gcTime"
        verdict: pass
        verification:
          - check: "npm packages installed"
            status: pass
            detail: "@tanstack/query-async-storage-persister and @tanstack/react-query-persist-client in package.json"
          - check: "PersistQueryClientProvider replaces QueryClientProvider"
            status: pass
            detail: "Import from @tanstack/react-query-persist-client. QueryClientProvider removed from imports."
          - check: "asyncStoragePersister created with correct config"
            status: pass
            detail: "key: '@query_cache', throttleTime: 1000"
          - check: "gcTime set to 7 days"
            status: pass
            detail: "SEVEN_DAYS_MS = 1000 * 60 * 60 * 24 * 7 (604800000)"
          - check: "persistOptions correct"
            status: pass
            detail: "persister, maxAge: SEVEN_DAYS_MS, buster: Constants.expoConfig?.version ?? '1.0.0'"

      - step: STEP-02
        title: "OnlineStatusContext"
        verdict: pass
        verification:
          - check: "File exists at src/contexts/OnlineStatusContext.tsx"
            status: pass
          - check: "Exports OnlineStatusProvider and useOnlineStatus"
            status: pass
            detail: "OnlineStatusProvider accepts isOnline + children. useOnlineStatus returns boolean."
          - check: "Default context value is { isOnline: true }"
            status: pass

      - step: STEP-03
        title: "SundayCard showChevron prop"
        verdict: pass
        verification:
          - check: "showChevron added to SundayCardProps"
            status: pass
            detail: "showChevron?: boolean with default true"
          - check: "Chevron conditionally rendered"
            status: pass
            detail: "onPress={showChevron ? onToggle : undefined} on header Pressable"

      - step: STEP-04
        title: "AgendaForm + SpeechSlot disabled prop"
        verdict: pass
        verification:
          - check: "AgendaForm disabled prop"
            status: pass
            detail: "disabled?: boolean defaults to false. isObserver = !hasPermission('agenda:write') || disabled"
          - check: "AgendaForm propagates disabled to sub-components"
            status: pass
            detail: "DebouncedTextInput editable={!isObserver}, SelectorField disabled={isObserver}, Switch disabled={isObserver}"
          - check: "SpeechSlot disabled prop"
            status: pass
            detail: "disabled?: boolean defaults to false. canAssign, canUnassign, canChangeStatus all AND with !disabled"

      - step: STEP-05
        title: "useOfflinePrefetch hook"
        verdict: pass
        verification:
          - check: "getNext3Sundays utility"
            status: pass
            detail: "Returns 3 consecutive Sunday ISO dates starting from getTodaySundayDate()"
          - check: "Prefetches 4 queries"
            status: pass
            detail: "agenda bySunday, agenda byDateRange, speeches byDateRange, exceptions"
          - check: "Uses same query keys as tab hooks"
            status: pass
            detail: "Imports agendaKeys, speechKeys, sundayTypeKeys from respective hooks"
          - check: "Offline->online transition tracking"
            status: pass
            detail: "useRef(prevOnlineRef) tracks previous state. Only prefetches on transition."
          - check: "Each prefetchQuery independent"
            status: pass
            detail: "4 separate prefetchQuery calls, no chaining"

      - step: STEP-06
        title: "SyncProvider integration"
        verdict: pass
        verification:
          - check: "OnlineStatusProvider wraps children"
            status: pass
            detail: "<OnlineStatusProvider isOnline={isOnline}> wraps OfflineBanner + children"
          - check: "useOfflinePrefetch called"
            status: pass
            detail: "useOfflinePrefetch(isOnline) after useOfflineQueueProcessor"
          - check: "Existing hooks unchanged"
            status: pass
            detail: "useConnection, useRealtimeSync, useOfflineQueueProcessor, useRegisterPushToken, useNotificationHandler all intact"

      - step: STEP-07
        title: "Home tab offline UI"
        verdict: pass
        verification:
          - check: "useOnlineStatus imported and called"
            status: pass
          - check: "NextSundaysSection conditional"
            status: pass
            detail: "{isOnline && <NextSundaysSection />}"
          - check: "NextAssignmentsSection conditional"
            status: pass
            detail: "{isOnline && <NextAssignmentsSection />}"
          - check: "InviteManagementSection conditional"
            status: pass
            detail: "{isOnline && <InviteManagementSection />}"
          - check: "Pencil button conditional"
            status: pass
            detail: "{isOnline && (<Pressable ... pencil ... />)}"
          - check: "Start Meeting always visible"
            status: pass
            detail: "Not wrapped in isOnline conditional"

      - step: STEP-08
        title: "Agenda tab offline UI"
        verdict: pass
        verification:
          - check: "Expandable logic includes online check"
            status: pass
            detail: "isExpandable = baseExpandable && (isOnline || date === nextSunday)"
          - check: "Auto-collapse on offline"
            status: pass
            detail: "useEffect collapses if !isOnline && expandedDate !== nextSunday"
          - check: "AgendaForm disabled when offline"
            status: pass
            detail: "disabled={isOffline} passed to AgendaForm"
          - check: "SundayTypeDropdown disabled when offline"
            status: pass
            detail: "disabled={typeDisabled || isOffline}"
          - check: "lazyCreate guarded by isOnline"
            status: pass
            detail: "if (isOnline) { lazyCreate.mutate(date) }"
          - check: "Chevron uses expandable"
            status: pass
            detail: "{expandable && (isExpanded ? ChevronUp : ChevronDown)}"

      - step: STEP-09
        title: "Speeches tab offline UI"
        verdict: pass
        verification:
          - check: "offlineExpandableDates computed"
            status: pass
            detail: "useMemo(() => new Set(getNext3Sundays()), [])"
          - check: "isExpandable per card"
            status: pass
            detail: "isOnline || offlineExpandableDates.has(item.date)"
          - check: "onToggle guarded"
            status: pass
            detail: "onToggle={isExpandable ? () => handleToggle(item.date) : undefined}"
          - check: "showChevron guarded"
            status: pass
            detail: "showChevron={isExpandable}"
          - check: "SpeechSlot disabled when offline"
            status: pass
            detail: "disabled={!isOnline} on all SpeechSlot instances"
          - check: "typeDisabled includes isOnline"
            status: pass
            detail: "typeDisabled={!canWriteSundayType || !isOnline}"
          - check: "Auto-collapse on offline"
            status: pass
            detail: "useEffect: !isOnline && !offlineExpandableDates.has(expandedDate) -> collapse"
          - check: "lazyCreate guarded by isOnline"
            status: pass
            detail: "if (isOnline) { lazyCreate.mutate(...) }"

      - step: STEP-10
        title: "Settings tab offline UI"
        verdict: pass
        verification:
          - check: "Users item conditional on isOnline"
            status: pass
            detail: "{hasPermission('settings:users') && isOnline && (<SettingsItem />)}"
          - check: "Other settings not conditional on isOnline"
            status: pass
            detail: "Members, topics, whatsapp, language, timezone, manage_prayers, history, app language, theme, about - none gated by isOnline"

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-262: Offline mode is purely client-side. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native). No database migrations.
          No Edge Function changes. Ship whenever ready. New npm packages
          (@tanstack/query-async-storage-persister, @tanstack/react-query-persist-client)
          are bundled in the app binary.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M006.yaml"
        - "docs/plans/PLAN_P006.yaml"
        - "docs/qa/QA_PLAN_006.yaml"
        - "docs/specs/SPEC_F006.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 12
      total_commits: 10
      tests_total: 943
      tests_passing: 943
      test_files: 47

  # ===========================================================================
  # REV-008: Phase 7 - Web Bugfixes (CR-263, CR-264, CR-265)
  # ===========================================================================

  - id: REV-008
    scope: "CR-263, CR-264, CR-265 - Web Bugfixes (Registration Errors, ActorSelector TDZ, Hymn Selector Web)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    phase: 7
    iteration: 1

    commits:
      - hash: 648bb3c
        message: "feat(i18n): add auth.registrationFailed key to all 3 locales (CR-263)"
      - hash: a697ff9
        message: "fix(register): show friendly i18n errors instead of raw technical messages (CR-263)"
      - hash: 57eea91
        message: "fix(ActorSelector): reorder hooks to fix TDZ crash + add web overlay fix (CR-264, CR-265)"
      - hash: 4e0a1c1
        message: "fix(AgendaForm): add web overlay fix + initialNumToRender to HymnSelectorModal (CR-265)"
      - hash: 4ac585a
        message: "test(phase-7): add 57 tests for F053-F054-F055 web bugfixes (CR-263, CR-264, CR-265)"
      - hash: 7be6a15
        message: "docs(tests): update TEST_CONSOLIDATED for F053-F055 (57 new tests, 1000 total)"

    verdict: approved

    summary: |
      All three bugfixes (CR-263, CR-264, CR-265) are correctly implemented with
      minimal, focused changes. Chain validation passes completely: SPEC_F007 accurately
      captures the bugs and fixes, ARCH_M007 provides correct architectural guidance,
      PLAN_P007 orders steps properly, and the code faithfully implements each step.
      No P0 or P1 issues found. 57 new tests all pass. 1000 total tests, 999 pass
      (1 pre-existing flaky test from Phase 5 unrelated to these changes). No database
      migrations, no Edge Function changes -- pure client-side fixes. No deploy needed
      beyond the existing pending EAS Build from CR-262.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request: pass
      arch_vs_spec: pass
      plan_vs_arch: pass
      code_vs_plan: pass
      chain_intact: true

      details:
        spec_vs_request:
          status: pass
          notes: |
            SPEC_F007 accurately captures all three bugs:
            - F053 (CR-263): Root cause (FunctionsHttpError thrown before data checks),
              fix strategy (read data.error for known codes), error mapping (4 scenarios),
              new i18n key (auth.registrationFailed). 7 ACs cover all error scenarios.
            - F054 (CR-264): Root cause (TDZ due to hook declaration order), fix (reorder
              hooks). 6 ACs cover all actor roles and scroll behavior.
            - F055 (CR-265): Root cause (web click bubbling, FlatList windowed rendering),
              fix (onClick stopPropagation, initialNumToRender). 8 ACs cover web and native.

        arch_vs_spec:
          status: pass
          notes: |
            ARCH_M007 correctly identifies 5 components (C007-01 through C007-05) mapping
            to the 3 features. Architecture impact is correctly assessed as "low" (no new
            modules, hooks, providers, database tables). Two ADRs (ADR-022 for web overlay
            pattern, ADR-023 for error extraction) document the key decisions. Contracts
            are well-defined (Supabase functions.invoke return, Platform.OS onClick).

        plan_vs_arch:
          status: pass
          notes: |
            PLAN_P007 has 5 steps matching the architecture. Step ordering respects
            dependencies (STEP-01 i18n before STEP-02 register.tsx). STEP-03 correctly
            combines C007-03 and C007-05 (both modify ActorSelector.tsx). Coverage matrix
            shows all 21 ACs covered across steps.

        code_vs_plan:
          status: pass
          notes: |
            Each commit maps precisely to a PLAN step:
            - 648bb3c -> STEP-01: auth.registrationFailed in 3 locale files
            - a697ff9 -> STEP-02: register.tsx error handling refactored
            - 57eea91 -> STEP-03: ActorSelector hook reorder + web overlay
            - 4e0a1c1 -> STEP-04: HymnSelectorModal web overlay + initialNumToRender

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      - file: "src/i18n/locales/pt-BR.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "Placement is after auth.stakeWardExists (logical order)"
          - "JSON remains valid"

      - file: "src/i18n/locales/en-US.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "English text is natural and user-friendly"

      - file: "src/i18n/locales/es-LA.json"
        verdict: pass
        changes: "Added auth.registrationFailed key"
        checks:
          - "Key present at line 52 with correct value"
          - "Spanish text is natural and user-friendly"

      - file: "src/app/(auth)/register.tsx"
        verdict: pass
        changes: "Refactored handleRegister() error handling"
        checks:
          - check: "No 'throw response.error' -- removed as planned"
            status: pass
            detail: "Lines 81-94: response.error is checked as boolean, then data?.error is inspected for known codes"
          - check: "email_exists mapped to t('auth.emailExists')"
            status: pass
            detail: "Line 84-87"
          - check: "stake_ward_exists mapped to t('auth.stakeWardExists')"
            status: pass
            detail: "Line 88-91"
          - check: "Fallback for unknown non-2xx errors: t('auth.registrationFailed')"
            status: pass
            detail: "Line 92-93"
          - check: "Success path unchanged (data?.session)"
            status: pass
            detail: "Lines 97-105"
          - check: "Catch block: network errors -> t('auth.requiresConnection')"
            status: pass
            detail: "Line 108-109"
          - check: "Catch block: non-network errors -> t('auth.registrationFailed')"
            status: pass
            detail: "Line 110-111"
          - check: "Optional chaining on data?.error for null safety"
            status: pass
            detail: "Lines 84, 88 use data?.error"
          - check: "No raw error.message ever shown to user"
            status: pass
            detail: "All error paths use i18n-translated strings"

      - file: "src/components/ActorSelector.tsx"
        verdict: pass
        changes: "Hook reorder (TDZ fix) + web overlay fix"
        checks:
          - check: "useActors and mutation hooks declared before useEffect that uses filtered"
            status: pass
            detail: "Lines 69-72: useActors, useCreateActor, useUpdateActor, useDeleteActor"
          - check: "useMemo filtered declared before useEffect that references it"
            status: pass
            detail: "Lines 74-79: useMemo. Lines 82-91: useEffect uses filtered"
          - check: "No hooks removed or added, only reordered"
            status: pass
            detail: "Same hooks as before, in different order"
          - check: "Web onClick stopPropagation on inner sheet View"
            status: pass
            detail: "Line 231: {...(Platform.OS === 'web' ? { onClick: (e: any) => e.stopPropagation() } : {})}"
          - check: "onStartShouldSetResponder preserved for native"
            status: pass
            detail: "Line 230: onStartShouldSetResponder={() => true}"
          - check: "Platform already imported (no new import needed)"
            status: pass
            detail: "Line 19: Platform in import list"

      - file: "src/components/AgendaForm.tsx"
        verdict: pass
        changes: "HymnSelectorModal web overlay fix + initialNumToRender"
        checks:
          - check: "Platform imported from react-native"
            status: pass
            detail: "Line 19: Platform in import list"
          - check: "Web onClick stopPropagation on HymnSelectorModal inner View"
            status: pass
            detail: "Line 834: {...(Platform.OS === 'web' ? { onClick: (e: any) => e.stopPropagation() } : {})}"
          - check: "onStartShouldSetResponder preserved for native"
            status: pass
            detail: "Line 833: onStartShouldSetResponder={() => true}"
          - check: "FlatList has initialNumToRender={filtered.length}"
            status: pass
            detail: "Line 856: initialNumToRender={filtered.length}"

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      test_file: "src/__tests__/f053-f054-f055-web-bugfixes.test.ts"
      test_count: 57
      test_result: "57/57 pass"
      coverage_assessment: |
        Tests cover all 21 acceptance criteria across 3 features:
        - F053 (CR-263): 30 tests covering i18n keys (3 locales x 4 keys),
          error-handling decision logic (13 scenarios including edge cases),
          frontend validation (8 field validation tests)
        - F054 (CR-264): 15 tests covering filterActorsByRole for all 5 roles
          plus 'all', scroll-to-editingId findIndex behavior, TDZ-safe
          useMemo with undefined/empty/populated actors
        - F055 (CR-265): 12 tests covering initialNumToRender matching data
          length, web onClick stopPropagation pattern, onStartShouldSetResponder
          preservation, search filtering behavior, overlay close pattern,
          native no-regression

      full_suite:
        total: 1000
        passing: 999
        failing: 1
        flaky_test: |
          useSpeechCounts.integration.test.ts > "makes a single Supabase call
          per fetch (AC-046-06 bulk query)" -- pre-existing flaky test from
          Phase 5 (CR-258). Passes when run in isolation, fails in full suite
          due to shared mock state. Also fails on pre-Phase 7 code. NOT caused
          by Phase 7 changes.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-263, CR-264, CR-265: All changes are client-side. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native). No database migrations.
          No Edge Function changes. Can be bundled with the existing pending
          EAS Build from CR-262. No additional deploy steps needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/deploy-commands.yaml"
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M007.yaml"
        - "docs/plans/PLAN_P007.yaml"
        - "docs/qa/QA_PLAN_007.yaml"
        - "docs/specs/SPEC_F007.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 6
      total_commits: 6
      tests_total: 1000
      tests_passing: 999
      tests_flaky: 1
      test_files: 48

  # ===========================================================================
  # REV-010: Phase 8 - CR-266, CR-267, CR-268
  # ===========================================================================

  - id: REV-010
    scope: "Phase 8 - Fixed collection sort order (CR-266), Topic field visibility parity (CR-267), Offline NextSundaysSection (CR-268)"
    date: "2026-03-04"
    reviewer: reviewer-agent
    spec: SPEC_F008.yaml
    arch: ARCH_M008.yaml
    plan: PLAN_P008.yaml
    features: [F056, F057, F058]
    iteration: 1
    commits:
      - hash: 0160b00
        message: "fix(useCollections): replace active/inactive sort with fixed-order priority map (CR-266)"
      - hash: 6cd57b7
        message: "fix(SpeechSlot): remove !!speech guard from showTopicRow for visibility parity (CR-267)"
      - hash: c04ed86
        message: "fix(home): show NextSundaysSection when offline (CR-268)"
      - hash: 00e67f0
        message: "fix(NextSundaysSection): hide pencil buttons when offline (CR-268)"
      - hash: 6bc08ca
        message: "test(phase-8): add 20 tests for F056-F057-F058 (CR-266, CR-267, CR-268)"
      - hash: f2a888b
        message: "docs(ledger): add phase 8 commits for CR-266, CR-267, CR-268"
    verdict: approved

    summary: |
      All 3 CRs implemented correctly. Chain validation fully intact from user
      request through SPEC, ARCH, PLAN, and CODE. 4 source commits plus 1 test
      commit. All 1055 tests pass (51 test files). No P0, P1, or P2 issues found.
      All changes are client-side only; no migrations, no Edge Functions. Clean repo.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        details: |
          CR-266: User asked to fix collection order in Topics settings with specific
          sequence (Temas Especiais, FSY, Principios do Evangelho, conferences newest
          first). SPEC F056 captures this exactly with 3 ACs and 2 edge cases.
          CR-267: User reported topic and speaker field visibility mismatch. SPEC F057
          identifies the root cause (showTopicRow has !!speech guard, speaker row does not)
          and specifies the one-line fix. 2 ACs and 2 edge cases.
          CR-268: User reported can't see upcoming speeches offline on Home tab. SPEC F058
          specifies showing NextSundaysSection offline with pencil buttons hidden. 5 ACs
          and 2 edge cases. Correctly notes this overrides CR-262 F049 behavior.

      arch_vs_spec:
        status: pass
        details: |
          ARCH M008 defines 4 components (C008-01 through C008-04) mapping directly to
          the 3 features. Each component specifies the exact file, function, and code
          change. ADR-024 documents the client-side sort approach decision. Integrations
          mapped for query cache and OnlineStatusContext dependencies.

      plan_vs_arch:
        status: pass
        details: |
          PLAN P008 has 5 steps: STEP-01 (F056 sort), STEP-02 (F057 visibility),
          STEP-03 (F058 Home tab), STEP-04 (F058 pencil button), STEP-05 (verification).
          Each step references the correct files and specifies done_when criteria matching
          the ARCH components. Steps are parallelizable as specified.

      code_vs_plan:
        status: pass
        details: |
          STEP-01: FIXED_COLLECTION_ORDER constant with 9 entries, select function sorts
          by priority then name descending. Exported for testability. JSDoc updated.
          STEP-02: Line 169 changed from "!isPrayer && !!speech" to "!isPrayer".
          STEP-03: NextSundaysSection rendered without isOnline guard. Other 2 sections
          still guarded.
          STEP-04: useOnlineStatus imported, renderHeaderRight conditional on isOnline.

      chain_intact: true

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      - cr: CR-266
        title: "Fixed collection sort order in useCollections"
        verdict: pass
        verification:
          - check: "FIXED_COLLECTION_ORDER constant exported with 9 entries"
            status: pass
            file: src/hooks/useTopics.ts
            detail: "Lines 187-200: 3 pt-BR, 3 en-US, 3 es-LA entries with priorities 0, 1, 2"
          - check: "select function uses priority map for sorting"
            status: pass
            detail: "Lines 244-250: sorts by priority (0-2 named, 3 conferences), then name descending"
          - check: "Active/inactive status not referenced in sort"
            status: pass
            detail: "No reference to .active property in the select function"
          - check: "JSDoc comment updated"
            status: pass
            detail: "Lines 202-207: 'Collections are sorted in fixed order: known collections by priority (0-2), then conference collections (priority 3) by name descending (newest first).'"
          - check: "Test file created with comprehensive coverage"
            status: pass
            detail: "f056-collection-sort.test.ts: 18 tests covering all 3 ACs, 2 edge cases, plus additional edge cases"

      - cr: CR-267
        title: "Topic field visibility parity with speaker field"
        verdict: pass
        verification:
          - check: "showTopicRow guard changed to !isPrayer only"
            status: pass
            file: src/components/SpeechSlot.tsx
            detail: "Line 169: const showTopicRow = !isPrayer; (removed !!speech)"
          - check: "Topic Pressable disabled prop still has !speech check"
            status: pass
            detail: "Line 271: disabled={!canAssign || !speech} -- ensures tapping topic when speech=null is a no-op"
          - check: "Test file created"
            status: pass
            detail: "f057-topic-visibility.test.ts: 16 tests covering both ACs, both edge cases, plus regression guard"

      - cr: CR-268
        title: "Show NextSundaysSection offline on Home tab"
        verdict: pass
        verification:
          - check: "NextSundaysSection isOnline guard removed in Home tab"
            status: pass
            file: src/app/(tabs)/index.tsx
            detail: "Line 245: <NextSundaysSection /> (no isOnline guard)"
          - check: "NextAssignmentsSection still has isOnline guard"
            status: pass
            detail: "Line 246: {isOnline && <NextAssignmentsSection />}"
          - check: "InviteManagementSection still has isOnline guard"
            status: pass
            detail: "Line 247: {isOnline && <InviteManagementSection />}"
          - check: "NextSundaysSection imports useOnlineStatus"
            status: pass
            file: src/components/NextSundaysSection.tsx
            detail: "Line 18: import { useOnlineStatus } from '../contexts/OnlineStatusContext'"
          - check: "isOnline used in NextSundaysSection"
            status: pass
            detail: "Line 37: const isOnline = useOnlineStatus();"
          - check: "renderHeaderRight conditional on isOnline"
            status: pass
            detail: "Line 106: renderHeaderRight={isOnline ? () => (...pencil...) : undefined}"
          - check: "f049 test updated for CR-268 change"
            status: pass
            detail: "f049-home-offline.test.ts updated: AC-049-03 now tests NextSundaysSection always visible"
          - check: "New test file created"
            status: pass
            detail: "f058-offline-next-sundays.test.ts: 15 tests covering all 5 ACs, 2 edge cases"

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      new_test_files:
        - file: "src/__tests__/f056-collection-sort.test.ts"
          test_count: 18
          coverage: "AC-056-1, AC-056-2, AC-056-3, EC-056-1, EC-056-2, plus additional edge cases"
        - file: "src/__tests__/f057-topic-visibility.test.ts"
          test_count: 16
          coverage: "AC-057-1, AC-057-2, EC-057-1, EC-057-2, plus regression guard and display logic"
        - file: "src/__tests__/f058-offline-next-sundays.test.ts"
          test_count: 15
          coverage: "AC-058-1 through AC-058-5, EC-058-1, EC-058-2"
      updated_test_files:
        - file: "src/__tests__/f049-home-offline.test.ts"
          changes: "AC-049-03 updated to reflect CR-268 (NextSundaysSection always visible)"

      full_suite:
        total: 1055
        passing: 1055
        failing: 0
        test_files: 51

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy app update (EAS Build)"
        reason: "CR-266, CR-267, CR-268: All changes are client-side only. No migrations, no Edge Functions."
        urgency: low
        notes: |
          All changes are client-side (React Native / TypeScript). No database
          migrations. No Edge Function changes. Can be bundled with existing
          pending EAS Build from CR-262 (which is still pending for offline mode
          npm packages). No additional deploy steps needed beyond the app binary.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M008.yaml"
        - "docs/plans/PLAN_P008.yaml"
        - "docs/qa/QA_PLAN_008.yaml"
        - "docs/specs/SPEC_F008.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 5
      total_commits: 6
      tests_total: 1055
      tests_passing: 1055
      tests_flaky: 0
      test_files: 51

  # ===========================================================================
  # REV-009: Phase 9 - CR-269 (F059)
  # ===========================================================================

  - id: REV-009
    scope: "CR-269 - Designation Notification Initial Assignment Guard (F059)"
    date: "2026-03-04"
    phase: 9
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: 9bffbf3
        message: "fix(notification): add OLD.status guard to designation trigger Case 1 (CR-269)"
      - hash: 9bdaa4d
        message: "docs(ledger): add phase 9 commit for CR-269"
      - hash: 77971de
        message: "test(F059): replace fs.readFileSync tests with behavioral trigger logic tests (CR-269)"
    verdict: approved

    summary: |
      Clean, minimal DB trigger fix. Migration 028 redefines enqueue_speech_notification()
      with a strict OLD.status = 'not_assigned' guard on Case 1 (designation), ensuring
      notifications only fire on initial speaker assignment. 35 behavioral tests cover
      all 7 ACs, 3 edge cases, early-exit guard, and exhaustive status transitions.
      No client-side code changes. Full test suite passes (1090 tests, 52 files).

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        details: |
          User request: designation push notification should only fire when bishopric
          selects a speaker name in the expanded speech card (initial assignment).
          SPEC_F009 captures this correctly as F059 with 7 ACs and 3 edge cases.
          The fix scope (DB trigger only, no client changes) matches the root cause.
      arch_vs_spec:
        status: pass
        details: |
          ARCH_M009 correctly maps the fix to a single component C009-01 (migration 028).
          The strict positive equality guard (OLD.status = 'not_assigned') is justified
          by ADR-025 and is more restrictive than the negative guard used by Cases 4/5,
          which matches the different business requirement (only initial assignment vs
          prevent duplicate fires).
      plan_vs_arch:
        status: pass
        details: |
          PLAN_P009 has 2 steps: STEP-01 (create migration + tests) and STEP-02
          (verify full suite). The plan includes the complete function body copied
          from migration 013 with only Case 1 guard added. done_when criteria are
          comprehensive (9 checks).
      code_vs_plan:
        status: pass
        details: |
          Migration 028 exactly matches the planned function body. The diff between
          migrations 013 and 028 shows only: (1) header comments updated, (2) Case 1
          comment updated, (3) Case 1 guard line changed from
          "IF NEW.status = 'assigned_not_invited' THEN" to
          "IF NEW.status = 'assigned_not_invited' AND (TG_OP = 'INSERT' OR OLD.status = 'not_assigned') THEN".
          All other cases (4, 5, cancellation, early-exit) are byte-identical.
      chain_intact: true

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      migration_028:
        file: supabase/migrations/028_fix_designation_notification_guard.sql
        status: pass
        checks:
          - check: "CREATE OR REPLACE FUNCTION enqueue_speech_notification()"
            status: pass
            detail: "Line 20: correct function signature"
          - check: "SECURITY DEFINER preserved"
            status: pass
            detail: "Line 96: $$ LANGUAGE plpgsql SECURITY DEFINER;"
          - check: "Early-exit guard unchanged"
            status: pass
            detail: "Lines 24-26: IF TG_OP = 'UPDATE' AND OLD.status = NEW.status THEN RETURN NEW;"
          - check: "Case 1 guard added correctly"
            status: pass
            detail: "Lines 32-33: IF NEW.status = 'assigned_not_invited' AND (TG_OP = 'INSERT' OR OLD.status = 'not_assigned') THEN"
          - check: "Case 4 guard unchanged"
            status: pass
            detail: "Line 50: IF NEW.status = 'assigned_confirmed' AND (TG_OP = 'INSERT' OR OLD.status != 'assigned_confirmed') THEN"
          - check: "Case 5 guard unchanged"
            status: pass
            detail: "Line 67: IF NEW.status = 'gave_up' AND (TG_OP = 'INSERT' OR OLD.status != 'gave_up') THEN"
          - check: "Cancellation case unchanged"
            status: pass
            detail: "Lines 84-91: IF NEW.status = 'not_assigned' AND TG_OP = 'UPDATE' AND OLD.status != 'not_assigned' THEN ..."
          - check: "INSERT INTO notification_queue columns match original"
            status: pass
            detail: "All 3 INSERT statements (Cases 1, 4, 5) have identical column lists and value expressions as migration 013"
          - check: "ADR-025 referenced in comments"
            status: pass
            detail: "Lines 13-15 and 31"
          - check: "No trigger re-creation (CREATE OR REPLACE FUNCTION only)"
            status: pass
            detail: "File contains only function definition, no CREATE TRIGGER or DROP TRIGGER"

      test_file:
        file: src/__tests__/f059-designation-notification-guard.test.ts
        status: pass
        checks:
          - check: "Behavioral simulation approach (not fs.readFileSync)"
            status: pass
            detail: "simulateTrigger() function mirrors SQL logic, tests behavior not string content"
          - check: "All 7 ACs covered"
            status: pass
            detail: "AC-059-1 through AC-059-7 each have dedicated describe blocks"
          - check: "All 3 edge cases covered"
            status: pass
            detail: "EC-059-1 (reassign cycle), EC-059-2 (unmanaged prayer), EC-059-3 (INSERT safety)"
          - check: "Early-exit guard exhaustive test"
            status: pass
            detail: "it.each(ALL_STATUSES) tests all 5 same-status transitions"
          - check: "Exhaustive guard test"
            status: pass
            detail: "Tests all 3 non-not_assigned statuses blocked from designation + positive confirmation"
          - check: "35 tests, all passing"
            status: pass

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      new_test_files:
        - file: "src/__tests__/f059-designation-notification-guard.test.ts"
          test_count: 35
          coverage: "AC-059-1, AC-059-2, AC-059-3, AC-059-4, AC-059-5, AC-059-6, AC-059-7, EC-059-1, EC-059-2, EC-059-3, early-exit guard, exhaustive guard, Case 4/5 self-transitions, INSERT cancellation guard"
      updated_test_files: []

      full_suite:
        total: 1090
        passing: 1090
        failing: 0
        test_files: 52

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Apply migration 028 to Supabase (supabase db push or apply via dashboard)"
        reason: "CR-269: New SQL migration redefines enqueue_speech_notification() trigger function"
        urgency: medium
        instructions: |
          Run the migration against the production Supabase database:
            supabase db push
          or apply 028_fix_designation_notification_guard.sql manually via
          the Supabase SQL Editor in the dashboard.
          No downtime needed - CREATE OR REPLACE FUNCTION is atomic.
          No client-side changes are needed; the fix is entirely server-side.
        notes: |
          This migration does NOT require an app binary update (EAS Build).
          It can be deployed independently of the pending EAS Build from CR-262.
          After applying, verify in Supabase SQL Editor:
            SELECT prosrc FROM pg_proc WHERE proname = 'enqueue_speech_notification';
          Should contain "OLD.status = 'not_assigned'" in the Case 1 guard.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/tests/TEST_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M009.yaml"
        - "docs/plans/PLAN_P009.yaml"
        - "docs/qa/QA_PLAN_009.yaml"
        - "docs/specs/SPEC_F009.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 3
      total_commits: 3
      tests_total: 1090
      tests_passing: 1090
      tests_flaky: 0
      test_files: 52

  # ===========================================================================
  # REV-012: Phase 10 - Notification text, error i18n, session refresh
  #          (CR-270, CR-271, CR-272)
  # ===========================================================================

  - id: REV-012
    scope: "F060/F061/F062 Phase 10 - Prayer-Aware Designation Notifications (CR-270), User-Friendly Error Messages (CR-271), Refresh Session Before Edge Function Invoke (CR-272)"
    date: "2026-03-05"
    reviewer: reviewer-agent
    spec: SPEC_F010
    arch: ARCH_M010
    plan: PLAN_P010
    iteration: 1
    commits:
      # STEP-01: i18n keys (CR-270/CR-271/CR-272)
      - hash: 9612ef9
        step: STEP-01
        message: "feat(i18n): add 5 new error message i18n keys to all 3 locales (CR-270, CR-271, CR-272)"
      # STEP-02: Server-side prayer-aware designation text (CR-270)
      - hash: 59734d5
        step: STEP-02
        message: "feat(process-notifications): prayer-aware designation text for position 0/4 (CR-270)"
      # STEP-03: Client-side prayer-aware designation text (CR-270)
      - hash: 6749af7
        step: STEP-03
        message: "feat(notificationUtils): prayer-aware designation text for position 0/4 (CR-270)"
      # STEP-04: Invite error i18n (CR-271) - users.tsx
      - hash: cb03b90
        step: STEP-04
        message: "fix(users): replace raw error with i18n key in invite error handler (CR-271)"
      # STEP-05 (actually STEP-06 in PLAN): Settings error i18n (CR-271)
      - hash: 1f4b56b
        step: STEP-05
        message: "fix(settings): replace raw errors with i18n keys for language change and sign out (CR-271)"
      # STEP-06 (actually STEP-07 in PLAN): Members error i18n (CR-271)
      - hash: 5155acf
        step: STEP-06
        message: "fix(members): replace raw errors with i18n keys for delete warning and import error (CR-271)"
      # STEP-07 (actually STEP-05 in PLAN): Invite registration error i18n (CR-271)
      - hash: dc84aaf
        step: STEP-07
        message: "fix(invite): replace raw errors with i18n registrationFailed key (CR-271)"
      # STEP-08: refreshSession guard (CR-272) - initial attempt
      - hash: 3e29e1c
        step: STEP-08
        message: "feat(users): add refreshSession() after role change and user deletion (CR-272)"
      # STEP-08-fix: Correct refreshSession placement (CR-272)
      - hash: 98f2149
        step: STEP-08-fix
        message: "fix(users): use refreshSession() in callEdgeFunction guard (CR-272)"
    verdict: approved

    summary: |
      All 9 commits for Phase 10 (CR-270, CR-271, CR-272) correctly implement
      prayer-aware designation notification text, user-friendly error messages
      in all UI alerts, and session refresh before edge function invocation.
      Chain validation passes: SPEC -> ARCH -> PLAN -> CODE is fully aligned.
      All 1148 tests pass (53 test files, 0 failures). Repository is clean
      (no uncommitted code files). One P2 deviation noted: members.tsx import
      error discrimination uses message-content checking instead of the tag-based
      approach specified in PLAN/ARCH (ADR-027), but is functionally correct.
      No P0 or P1 issues found.

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested 3 bug fixes:
          CR-270: Designation notification uses speech text for prayers - SPEC_F010
          defines F060 with 5 ACs covering prayer-specific title/body for positions
          0 and 4, individual (not grouped) processing, and client-server consistency.
          CR-271: Raw error messages shown in UI alerts - SPEC_F010 defines F061
          with 8 ACs covering all 7 identified error points across 4 files.
          CR-272: Stale JWT when invoking edge functions - SPEC_F010 defines F062
          with 3 ACs covering refreshSession replacement.
          All user requirements are faithfully captured.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M010 maps each feature to architectural components:
          F060: C010-01 (server grouping + buildDesignationText), C010-02 (client notificationUtils)
          F061: C010-03 (users.tsx), C010-04 (invite/[token].tsx), C010-05 (settings/index.tsx),
                C010-06 (members.tsx), C010-07 (i18n keys)
          F062: C010-08 (callEdgeFunction refreshSession)
          7 integrations mapped. 3 ADRs documented (ADR-026, ADR-027, ADR-028).
          All components map correctly to SPEC features.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P010 has 8 steps covering all 8 ARCH components. Step order is
          correct: i18n keys first (STEP-01), then server notification (STEP-02),
          client notification (STEP-03), then error i18n fixes (STEP-04 to STEP-07),
          verification (STEP-08). STEP-04 combines F061+F062 users.tsx changes
          (correct since both modify same file, different functions).
          Validation matrix maps all 22 ACs and 9 edge cases to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 8 plan steps implemented in 9 commits. STEP-08 was split into
          two commits (3e29e1c initial attempt + 98f2149 fix) to correctly place
          refreshSession() in the callEdgeFunction guard rather than after
          individual mutation calls. The step ordering was slightly different
          from PLAN (invite/[token].tsx done as STEP-07 instead of STEP-05),
          but all changes are complete and correct. One implementation deviation:
          members.tsx uses message-content checking for CSV error discrimination
          instead of the isCsvValidation tag specified in PLAN/ARCH (P2 issue).

      chain_intact: true

    # =========================================================================
    # PER-CR VERIFICATION
    # =========================================================================

    cr_reviews:

      # -----------------------------------------------------------------------
      # CR-270: Prayer-Aware Designation Notification Text (F060)
      # -----------------------------------------------------------------------
      - cr: CR-270
        title: "Prayer-aware designation notification text"
        verdict: pass
        verification:

          # Server-side (process-notifications/index.ts)
          - check: "buildDesignationText accepts optional position parameter"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Line 66-70: function buildDesignationText(language, names, date, position?: number | null)"

          - check: "Prayer position (0,4) returns prayer-specific title and body"
            status: pass
            detail: |
              Lines 73-90: When getPrayerLabel returns non-null, returns prayer-specific
              text. pt-BR: 'Designacao de Oracao', en-US: 'Prayer Assignment',
              es-LA: 'Asignacion de Oracion'. Body includes prayer label (abertura/encerramento).

          - check: "Speech position (1-3, null) returns existing text unchanged"
            status: pass
            detail: "Lines 93-109: Existing speech text with nameList, verb conjugation, unchanged."

          - check: "Grouping loop splits prayer vs speech designations"
            status: pass
            detail: |
              Lines 244-258: entry.speech_position === 0 || 4 -> immediateEntries.
              Otherwise -> designationGroups map by (ward_id:sunday_date).

          - check: "immediateEntries switch has 'designation' case"
            status: pass
            detail: |
              Lines 316-325: case 'designation' calls buildDesignationText with
              [entry.speaker_name ?? ''], entry.sunday_date, entry.speech_position.

          # Client-side (notificationUtils.ts)
          - check: "pt-BR designation builder checks prayer position"
            status: pass
            file: src/lib/notificationUtils.ts
            detail: "Lines 67-83: Checks d.position != null, calls getPrayerLabel. Returns 'Designacao de Oracao' for prayer."

          - check: "en-US designation builder checks prayer position"
            status: pass
            detail: "Lines 120-135: Same pattern. Returns 'Prayer Assignment' for prayer."

          - check: "es-LA designation builder checks prayer position"
            status: pass
            detail: "Lines 173-188: Same pattern. Returns 'Asignacion de Oracion' for prayer."

          - check: "Client and server text match"
            status: pass
            detail: |
              Both use getPrayerLabel() for position detection. Both produce
              identical title strings per language. Body format matches
              (name + prayer label + date + call to action).

        ac_coverage:
          - ac: AC-060-1
            status: covered
            detail: "Position 0 -> getPrayerLabel returns 'oracao de abertura'/'opening prayer' -> prayer title+body"
          - ac: AC-060-2
            status: covered
            detail: "Position 4 -> getPrayerLabel returns 'oracao de encerramento'/'closing prayer' -> prayer title+body"
          - ac: AC-060-3
            status: covered
            detail: "Positions 1-3 -> grouped by (ward_id, sunday_date), speech text unchanged"
          - ac: AC-060-4
            status: covered
            detail: "Prayer entries (0,4) pushed to immediateEntries, not designationGroups"
          - ac: AC-060-5
            status: covered
            detail: "Client notificationUtils mirrors server behavior with same getPrayerLabel"

        ec_coverage:
          - ec: EC-060-1
            status: covered
            detail: "Both position 0 and 4 go to immediateEntries independently, each gets own notification"
          - ec: EC-060-2
            status: covered
            detail: "null position -> getPrayerLabel returns null -> falls through to speech text"

      # -----------------------------------------------------------------------
      # CR-271: User-Friendly Error Messages (F061)
      # -----------------------------------------------------------------------
      - cr: CR-271
        title: "User-friendly error messages in all alerts"
        verdict: pass
        verification:

          # P0: users.tsx invite error
          - check: "inviteMutation.onError maps auth/no-session to i18n key"
            status: pass
            file: src/app/(tabs)/settings/users.tsx
            detail: |
              Lines 110-116: if (err.message === 'auth/no-session') -> t('users.sessionExpired')
              else -> t('users.inviteFailed'). No raw err.message shown.

          # P1: invite/[token].tsx unknown error
          - check: "Unknown data.error uses t('auth.registrationFailed')"
            status: pass
            file: src/app/(auth)/invite/[token].tsx
            detail: "Line 117: setError(t('auth.registrationFailed')) after known codes check."

          # P1: invite/[token].tsx catch-all
          - check: "Non-network catch uses t('auth.registrationFailed')"
            status: pass
            detail: "Line 134: setError(t('auth.registrationFailed')) instead of raw message."

          # P1: Known codes preserved
          - check: "Known error codes (token_expired/used/invalid) still mapped correctly"
            status: pass
            detail: |
              Lines 101-114: token_expired -> t('auth.inviteExpired'),
              token_used -> t('auth.inviteUsed'), token_invalid -> t('auth.inviteInvalid').
              Unchanged from before.

          # P1: Network error preserved
          - check: "Network error still uses t('auth.requiresConnection')"
            status: pass
            detail: "Lines 131-132: message includes 'Failed to fetch' or 'Network' -> t('auth.requiresConnection')"

          # P2: settings/index.tsx language change
          - check: "handleAppLanguageSelect uses t('settings.languageChangeFailed')"
            status: pass
            file: src/app/(tabs)/settings/index.tsx
            detail: "Line 153: Alert.alert(t('common.error'), t('settings.languageChangeFailed'))"

          # P2: settings/index.tsx sign out
          - check: "handleSignOut uses t('settings.signOutFailed')"
            status: pass
            detail: "Line 195: Alert.alert(t('common.error'), t('settings.signOutFailed'))"

          # P3: members.tsx import RPC error
          - check: "Import RPC error shows t('members.importRpcError')"
            status: pass
            file: src/app/(tabs)/settings/members.tsx
            detail: |
              Lines 474-479: onError checks isCsvParseError (message-based detection),
              shows err.message for CSV errors, t('members.importRpcError') for RPC errors.

          # P3: CSV validation errors preserved
          - check: "CSV validation errors still show detailed per-line messages"
            status: pass
            detail: "CSV parse errors contain newlines from join('\\n'). Detected by includes('\\n')."

          # P4: members.tsx future speeches i18n
          - check: "Future speeches uses t('members.futureSpeechWarning')"
            status: pass
            detail: "Line 356: t('members.futureSpeechWarning', { count: futureCount })"

          # i18n keys
          - check: "All 6 new i18n keys present in all 3 locales"
            status: pass
            detail: |
              Verified in pt-BR.json, en-US.json, es-LA.json:
              - settings.languageChangeFailed: present in all 3
              - settings.signOutFailed: present in all 3
              - members.futureSpeechWarning: present in all 3 with {{count}}
              - members.importRpcError: present in all 3
              - users.sessionExpired: present in all 3
              - auth.registrationFailed: present in all 3
              Total: 18 new entries (6 per locale).

        ac_coverage:
          - ac: AC-061-1
            status: covered
            detail: "inviteMutation.onError uses t('users.inviteFailed'), not err.message"
          - ac: AC-061-2
            status: covered
            detail: "Unknown data.error shows t('auth.registrationFailed')"
          - ac: AC-061-3
            status: covered
            detail: "Non-network catch shows t('auth.registrationFailed')"
          - ac: AC-061-4
            status: covered
            detail: "handleAppLanguageSelect catch shows t('settings.languageChangeFailed')"
          - ac: AC-061-5
            status: covered
            detail: "handleSignOut catch shows t('settings.signOutFailed')"
          - ac: AC-061-6
            status: covered
            detail: "RPC import error shows t('members.importRpcError')"
          - ac: AC-061-7
            status: covered
            detail: "CSV validation errors show err.message (already i18n'd)"
          - ac: AC-061-8
            status: covered
            detail: "Future speeches uses t('members.futureSpeechWarning', { count })"

      # -----------------------------------------------------------------------
      # CR-272: Refresh Session Before Edge Function Invoke (F062)
      # -----------------------------------------------------------------------
      - cr: CR-272
        title: "Refresh session before edge function invoke"
        verdict: pass
        verification:
          - check: "callEdgeFunction uses refreshSession() not getSession()"
            status: pass
            file: src/app/(tabs)/settings/users.tsx
            detail: "Line 47: const { data: { session }, error: refreshError } = await supabase.auth.refreshSession();"

          - check: "refreshError checked in guard"
            status: pass
            detail: "Line 48: if (refreshError || !session) throw new Error('auth/no-session');"

          - check: "Comment references ADR-028"
            status: pass
            detail: "Line 46: // Guard: refresh session before calling invoke (ADR-028)"

        ac_coverage:
          - ac: AC-062-1
            status: covered
            detail: "refreshSession() returns fresh JWT; invoke() uses updated internal state"
          - ac: AC-062-2
            status: covered
            detail: "refreshError || !session -> throws 'auth/no-session'"
          - ac: AC-062-3
            status: covered
            detail: "Supabase SDK internally returns existing session if token is still valid (no unnecessary network call)"

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: P2-010-01
        severity: P2
        category: quality
        location: "src/app/(tabs)/settings/members.tsx:474-479"
        description: |
          Import error discrimination uses message-content checking (includes('\n')
          and includes(t('members.importEmpty'))) instead of the isCsvValidation tag
          approach specified in PLAN_P010 STEP-07 and ARCH_M010 ADR-027. The ARCH
          explicitly noted "Parse error message format (check for newlines): Fragile
          and breaks if error message format changes" as a rejected alternative.
        suggestion: |
          Consider tagging errors with a custom property (isCsvValidation = true)
          before throwing, as specified in the PLAN. However, this is a minor quality
          concern - the current approach works correctly for all current error formats.
          Not blocking approval.

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 1148
      total_files: 53
      all_passing: true
      new_tests:
        - file: src/__tests__/f060-f061-f062.test.ts
          covers: "All 3 CRs: prayer-aware designation text, error i18n, refreshSession guard"
      coverage_gaps:
        - cr: CR-270
          component: "Server-side process-notifications grouping"
          reason: "Edge Function (Deno runtime) cannot be imported in vitest Node environment"
          risk: low
          mitigation: "Code review verified grouping logic. Client-side equivalent is tested."
        - cr: CR-272
          component: "refreshSession actual behavior"
          reason: "Cannot mock Supabase auth.refreshSession in unit tests for real SDK behavior"
          risk: low
          mitigation: "Code review verified correct API usage. Tests verify the code path exists."

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Deploy process-notifications Edge Function"
        command: "supabase functions deploy process-notifications"
        reason: "CR-270: Prayer-aware designation grouping and text changes"
        urgency: medium
        instructions: |
          Deploy the updated process-notifications Edge Function:
            supabase functions deploy process-notifications
          No downtime needed. The change is backward-compatible:
          - Existing speech designations continue to be grouped as before
          - New prayer designations (position 0,4) will be sent individually
          No database migration needed for this change.
        notes: |
          This deployment can be done independently of the app update.
          After deploying, verify by assigning a prayer and checking that
          the notification has prayer-specific title ('Prayer Assignment')
          instead of speech title ('Speech Assignment').

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M010.yaml"
        - "docs/plans/PLAN_P010.yaml"
        - "docs/qa/QA_PLAN_010.yaml"
        - "docs/specs/SPEC_F010.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # STATS
    # =========================================================================

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 10
      total_commits: 9
      tests_total: 1148
      tests_passing: 1148
      tests_flaky: 0
      test_files: 53

  # ===========================================================================
  # REV-012: Phase 11 - CR-274 (F063)
  # Missing Topic Indicator in Invite Management
  # ===========================================================================

  - id: REV-012
    scope: "CR-274 (F063) - Missing Topic Indicator in Invite Management"
    phase: 11
    date: "2026-03-05"
    reviewer: reviewer-agent
    iteration: 1

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        details: |
          User request: Indicador "(Falta Tema)" ao lado do nome para discursos sem
          tema, destaque visual, botao WhatsApp indisponivel quando falta tema.
          SPEC_F011 covers all these: AC-01 through AC-07, i18n keys for 3 locales,
          WhatsApp button disabled via opacity+no-op, prayer positions excluded.
          The SPEC faithfully represents the user request with no scope creep.

      arch_vs_spec:
        status: pass
        details: |
          ARCH_M011 maps to SPEC correctly:
          - UI-only change (no backend/database) matches SPEC scope
          - Contracts define isSpeechPosition, isTopicMissing, isWhatsAppDisabled
          - i18n keys match SPEC exactly (3 locales, correct values)
          - ADR-029 (inline logic vs helper) is reasonable for 2-line booleans
          - Diagram shows correct component hierarchy

      plan_vs_arch:
        status: pass
        details: |
          PLAN_P011 follows ARCH_M011:
          - STEP-01: i18n keys (dependency for STEP-02) matches ARCH i18n section
          - STEP-02: Component changes follow ARCH contracts and UI changes exactly
          - STEP-03: Verification step
          - Validation matrix covers all 7 ACs + 4 edge cases

      code_vs_plan:
        status: pass
        details: |
          Code implementation matches PLAN_P011:
          - STEP-01 commits (ba87f0a): i18n keys added to all 3 locales with correct values
          - STEP-02 commit (f19942e): InviteManagementSection modified with:
            * const isSpeech, topicMissing, isWhatsAppDisabled (per ADR-029)
            * speakerNameRow View wrapper with indicator Text
            * Pressable opacity 0.4 + onPress undefined when disabled
            * styles.speakerNameRow and styles.topicMissing added
          - Tests cover all ACs and edge cases
          All matches PLAN specifications.

      chain_intact: true

    # =========================================================================
    # ACCEPTANCE CRITERIA VERIFICATION
    # =========================================================================

    acceptance_criteria_verification:
      - ac: AC-01
        status: covered
        detail: |
          Lines 230-231: isSpeech (position 1-3) + topicMissing check (!topic_title || trim).
          Lines 247-251: conditional Text with t('invite.topicMissing') and colors.error.
          Tests: 3 cases (null, empty, whitespace) verified in test file.
      - ac: AC-02
        status: covered
        detail: |
          When topic_title is non-empty, topicMissing=false, no indicator rendered.
          Tests: position 1 with 'My Topic' -> topicMissing=false.
      - ac: AC-03
        status: covered
        detail: |
          Position 0 and 4: isSpeech=false -> topicMissing=false.
          Tests: EC-02 and EC-03 verify positions 0 and 4 never trigger.
      - ac: AC-04
        status: covered
        detail: |
          Line 232: isWhatsAppDisabled = topicMissing && isNotInvited.
          Line 279: opacity 0.4 when disabled. Line 281: onPress=undefined.
          Tests: position 1 + null topic + assigned_not_invited -> disabled=true.
      - ac: AC-05
        status: covered
        detail: |
          When topic_title present, topicMissing=false, isWhatsAppDisabled=false.
          Pressable works normally with full opacity and original onPress.
          Tests: position 1 + 'My Topic' + assigned_not_invited -> disabled=false.
      - ac: AC-06
        status: covered
        detail: |
          When status=assigned_invited, isNotInvited=false, isWhatsAppDisabled=false.
          Three-dot dropdown not affected. Indicator text IS shown (topicMissing=true).
          Tests: position 2 + null topic + assigned_invited -> disabled=false, topicMissing=true.
      - ac: AC-07
        status: covered
        detail: |
          i18n keys in all 3 locales:
          pt-BR: "(Falta Tema)", en-US: "(Missing Topic)", es-LA: "(Falta Tema)"
          Tests: 3 explicit assertions in STEP-01 test block.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: P2-011-01
        severity: P2
        category: quality
        location: "src/components/InviteManagementSection.tsx:336-338,244"
        description: |
          The speakerName Text inside the new speakerNameRow flex row lacks
          flexShrink: 1 (or flex: 1). With a very long speaker name, the
          "(Falta Tema)" indicator could be pushed off-screen since the Text
          will try to take its full natural width. The numberOfLines={1} prop
          will truncate the text, but without flexShrink, the truncation
          happens at the container boundary rather than before the indicator.
        suggestion: |
          Add flexShrink: 1 to the speakerName style to ensure the name
          truncates to make room for the indicator. Not blocking approval
          because the parent details View has flex: 1 which provides some
          constraint, and the visual impact is minor (long names are rare).

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 1181
      total_files: 54
      all_passing: true
      new_tests:
        - file: src/__tests__/f063-topic-missing-indicator.test.ts
          test_count: 33
          covers: |
            STEP-01: 3 i18n key assertions
            STEP-02: 21 boolean logic tests (isSpeech, topicMissing, isWhatsAppDisabled)
            SUPPLEMENTARY: 9 integration tests with getInviteItems
      coverage_gaps: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys: []
    deploy_notes: |
      CR-274 is a purely client-side, UI-only change. No Edge Function or
      database migration needed. Changes will be included in the next
      EAS Build / app update.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/plans/PLAN_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M011.yaml"
        - "docs/plans/PLAN_P011.yaml"
        - "docs/qa/QA_PLAN_011.yaml"
        - "docs/specs/SPEC_F011.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-274 (F063) implementation is correct and complete. Chain validation
      passes at all 4 levels. All 7 ACs are covered by both code and tests.
      1181 tests passing, no regressions. One minor P2 (flexShrink on speaker
      name in flex row) noted but not blocking. No deploys needed.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 5
      total_commits: 2
      tests_total: 1181
      tests_passing: 1181
      tests_flaky: 0
      test_files: 54

  # ===========================================================================
  # REV-014: Phase 12 - Supabase Resource Optimization (CR-273)
  # ===========================================================================

  - id: REV-014
    scope: "F064 Phase 12 - Supabase Resource Optimization (CR-273)"
    date: "2026-03-05"
    reviewer: reviewer-agent
    spec: F064
    arch: M012
    plan: P012
    iteration: 1
    commits:
      - hash: f257893
        step: STEP-01
        message: "fix(send-reset-email): paginate listUsers to handle >50 users (CR-273)"
      - hash: e09ae26
        step: STEP-02
        message: "feat(migration): add role column to device_push_tokens (CR-273)"
      - hash: 536289b
        step: STEP-03
        message: "feat(useNotifications): include role in push token upsert (CR-273)"
      - hash: 734ee91
        step: STEP-04
        message: "perf(process-notifications): filter tokens by role in SQL (CR-273)"
      - hash: 64eed72
        step: STEP-05
        message: "perf(process-notifications): add ward cache Map for batch query (CR-273)"
      - hash: f87321f
        step: STEP-06
        message: "feat(process-notifications): add 7-day retention cleanup (CR-273)"
      - hash: 4886c58
        step: STEP-07
        message: "perf(useTopics): parallelize useActiveTopics queries (CR-273)"
      - hash: 1896b67
        step: STEP-08
        message: "perf(useTopics,sync): upsert for collection toggle + 10s polling (CR-273)"
      - hash: ea1352a
        step: STEP-09
        message: "test(sync): update polling interval tests from 2500 to 10000 (CR-273)"
      - hash: d0b80db
        step: STEP-docs
        message: "docs(ledger): add phase 12 commits for CR-273"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User request specified 7 optimization items at 3 priority levels:
          CRITICAL(1), HIGH(2), MEDIUM(4). SPEC_F012 maps each item to a
          sub-feature (F064-S1 through F064-S7) with acceptance criteria
          (AC-064-01 through AC-064-17). All 7 items are fully specified.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M012 defines components for all 7 sub-features. ADR-030
          (role in device_push_tokens), ADR-031 (ward cache Map), ADR-032
          (paginated listUsers), ADR-033 (Promise.all for useActiveTopics),
          ADR-034 (10s polling) are well-documented. Migration 029 adds
          role column. Contracts defined for all integration points.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P012 has 9 steps (STEP-01 through STEP-09) mapping to all
          ARCH components. Dependency graph is correct: STEP-03/04 depend
          on STEP-02 (migration before client/function changes). Steps
          01, 05, 06, 07, 08 are parallelizable. Validation matrix maps
          all 17 ACs to steps.

      code_vs_plan:
        status: pass
        detail: |
          All 9 plan steps implemented in 10 commits (9 code + 1 docs).
          STEP-08 combined useToggleCollection upsert + polling interval
          change in a single commit as planned. STEP-09 updated the 2 test
          files that referenced the old 2500ms polling value. Files match
          PLAN expectations exactly.

      chain_intact: true

    # =========================================================================
    # PER-STEP VERIFICATION
    # =========================================================================

    step_reviews:

      # --- STEP-01: send-reset-email pagination (F064-S1) ---
      - step: STEP-01
        title: "Paginate listUsers to handle >50 users"
        verdict: pass
        verification:
          - check: "listUsers called with pagination params"
            status: pass
            file: supabase/functions/send-reset-email/index.ts
            detail: "Line 126: listUsers({ page, perPage: 50 }) with page starting at 1."
          - check: "Pagination loop iterates until user found or last page"
            status: pass
            detail: "Line 124: while(!user) loop. Line 144: break when user found OR data.users.length < 50 (last page)."
          - check: "Anti-enumeration preserved"
            status: pass
            detail: "Lines 148-154: if (!user) returns { success: true } status 200, same as before."
          - check: "Error handling preserved inside loop"
            status: pass
            detail: "Lines 128-137: listError check returns 500 inside the loop."
          - check: "No standalone listUsers() without params"
            status: pass
            detail: "grep for 'listUsers()' returns 0 matches. Only 'listUsers({ page, perPage: 50 })' exists."
        covers_ac: ["AC-064-01", "AC-064-02", "AC-064-03"]

      # --- STEP-02: Migration 029 (F064-S2) ---
      - step: STEP-02
        title: "Add role column to device_push_tokens"
        verdict: pass
        verification:
          - check: "Migration file exists with correct name"
            status: pass
            file: supabase/migrations/029_add_device_push_tokens_role.sql
            detail: "File exists, 6 lines."
          - check: "ALTER TABLE adds role TEXT (nullable)"
            status: pass
            detail: "Line 6: ALTER TABLE device_push_tokens ADD COLUMN role TEXT; No NOT NULL constraint."
          - check: "Migration comment references ADR-030"
            status: pass
            detail: "Line 1: -- Migration 029: Add role column to device_push_tokens (CR-273, ADR-030)"
        covers_ac: ["AC-064-05"]

      # --- STEP-03: useNotifications role in upsert (F064-S2) ---
      - step: STEP-03
        title: "Include role in push token upsert"
        verdict: pass
        verification:
          - check: "role destructured from useAuth"
            status: pass
            file: src/hooks/useNotifications.ts
            detail: "Line 38: const { user, role, wardId } = useAuth();"
          - check: "role included in upsert payload"
            status: pass
            detail: "Line 91: role is included in the upsert object { user_id, ward_id, expo_push_token, role }."
          - check: "onConflict unchanged"
            status: pass
            detail: "Line 94: onConflict: 'user_id,expo_push_token' - same as before."
          - check: "Comment explains purpose"
            status: pass
            detail: "Line 83: // Upsert token in device_push_tokens (includes role for SQL-based filtering)"
        covers_ac: ["AC-064-05", "AC-064-06"]

      # --- STEP-04: getTargetTokens filter by role (F064-S2) ---
      - step: STEP-04
        title: "Filter tokens by role in SQL"
        verdict: pass
        verification:
          - check: "No getUserById calls remain"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "grep for 'getUserById' returns only the ADR-030 comment on line 420, not a function call."
          - check: "Roles array built correctly from targetRole"
            status: pass
            detail: "Lines 412-418: handles 'secretary', 'bishopric', and 'secretary_and_bishopric' with push logic."
          - check: "Single query with .in('role', roles)"
            status: pass
            detail: "Lines 421-425: .from('device_push_tokens').select('expo_push_token, user_id').eq('ward_id', wardId).in('role', roles)"
          - check: "Return type unchanged"
            status: pass
            detail: "Line 427: return (tokens as PushToken[] | null) ?? []; Same PushToken interface (expo_push_token, user_id)."
        covers_ac: ["AC-064-04"]

      # --- STEP-05: Ward cache Map (F064-S3) ---
      - step: STEP-05
        title: "Ward cache Map for batch query"
        verdict: pass
        verification:
          - check: "Unique ward_ids collected from entries"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Line 240: const wardIds = [...new Set(entries.map((e) => e.ward_id))];"
          - check: "Single batch query with .in('id', wardIds)"
            status: pass
            detail: "Lines 241-244: .from('wards').select('id, language, timezone').in('id', wardIds)"
          - check: "Map built from query results"
            status: pass
            detail: "Lines 246-249: Map<string, WardInfo> built with forEach. WardInfo has language and timezone."
          - check: "Cache used in grouped designations loop"
            status: pass
            detail: "Line 286: const ward = wardCache.get(wardId); Line 287: const language = ward?.language ?? 'pt-BR';"
          - check: "Cache used in immediate entries loop"
            status: pass
            detail: "Line 304: const ward = wardCache.get(entry.ward_id); Line 305: const language = ward?.language ?? 'pt-BR';"
          - check: "No inline ward queries remain"
            status: pass
            detail: "grep for '.from(\"wards\")' or '.from(''wards'')' inside processing loops returns 0. Only the batch query at lines 241-244."
          - check: "Fallback to pt-BR on cache miss"
            status: pass
            detail: "Both lines 287 and 305 use ward?.language ?? 'pt-BR'."
        covers_ac: ["AC-064-07", "AC-064-08"]

      # --- STEP-06: notification_queue retention cleanup (F064-S4) ---
      - step: STEP-06
        title: "7-day retention cleanup"
        verdict: pass
        verification:
          - check: "Cleanup code exists after invalid token removal"
            status: pass
            file: supabase/functions/process-notifications/index.ts
            detail: "Lines 376-386: Section 6 comment, try/catch block."
          - check: "7-day calculation correct"
            status: pass
            detail: "Line 378: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString()"
          - check: "Filters only sent/cancelled status"
            status: pass
            detail: "Line 382: .in('status', ['sent', 'cancelled'])"
          - check: "Uses lt for created_at comparison"
            status: pass
            detail: "Line 383: .lt('created_at', sevenDaysAgo)"
          - check: "Cleanup errors are caught and logged, not thrown"
            status: pass
            detail: "Lines 384-386: catch (cleanupErr) { console.error(...) } - does not rethrow, does not fail the response."
        covers_ac: ["AC-064-09", "AC-064-10"]

      # --- STEP-07: useActiveTopics parallelization (F064-S5) ---
      - step: STEP-07
        title: "Parallelize useActiveTopics queries"
        verdict: pass
        verification:
          - check: "Round 1: ward_topics + ward_collection_config in Promise.all"
            status: pass
            file: src/hooks/useTopics.ts
            detail: "Lines 349-359: const [wardTopicsResult, activeConfigsResult] = await Promise.all([...]);"
          - check: "Error handling for both Round 1 results"
            status: pass
            detail: "Lines 361-362: if (wardTopicsResult.error) throw; if (activeConfigsResult.error) throw;"
          - check: "Round 2: general_collections + general_topics in Promise.all"
            status: pass
            detail: "Lines 379-388: const [collectionsResult, generalTopicsResult] = await Promise.all([...]);"
          - check: "Round 2 guarded by activeCollectionIds.length > 0"
            status: pass
            detail: "Line 377: if (activeCollectionIds.length > 0) {"
          - check: "Sort order unchanged"
            status: pass
            detail: "Lines 409-412: results.sort with localeCompare on 'Collection : Title' string. Identical to previous."
          - check: "Result set unchanged (same data, same TopicWithCollection[])"
            status: pass
            detail: "Structure is identical: ward topics get type 'ward', general topics get type 'general', same collectionMap logic."
        covers_ac: ["AC-064-11", "AC-064-12", "AC-064-13"]

      # --- STEP-08: useToggleCollection UPSERT + polling interval (F064-S6, S7) ---
      - step: STEP-08
        title: "Upsert for collection toggle + 10s polling"
        verdict: pass
        verification:
          - check: "useToggleCollection uses single .upsert()"
            status: pass
            file: src/hooks/useTopics.ts
            detail: "Lines 294-299: .upsert({ ward_id, collection_id, active }, { onConflict: 'ward_id,collection_id' })"
          - check: "No SELECT + conditional INSERT/UPDATE pattern"
            status: pass
            detail: "grep for '.select(' in useToggleCollection mutationFn returns 0. Single upsert only."
          - check: "Comment references UNIQUE constraint"
            status: pass
            detail: "Line 293: // Single upsert with onConflict (UNIQUE(ward_id, collection_id) at 001_initial_schema.sql:81)"
          - check: "POLLING_INTERVAL_MS = 10000"
            status: pass
            file: src/lib/sync.ts
            detail: "Line 67: export const POLLING_INTERVAL_MS = 10000;"
        covers_ac: ["AC-064-14", "AC-064-15", "AC-064-16", "AC-064-17"]

      # --- STEP-09: Test updates ---
      - step: STEP-09
        title: "Update polling interval tests"
        verdict: pass
        verification:
          - check: "sync-utils.test.ts updated to 10000"
            status: pass
            file: src/__tests__/sync-utils.test.ts
            detail: "Line 70: expect(POLLING_INTERVAL_MS).toBe(10000);"
          - check: "sync-realtime.integration.test.ts updated to 10000"
            status: pass
            file: src/__tests__/integration/sync-realtime.integration.test.ts
            detail: "Line 110: expect(POLLING_INTERVAL_MS).toBe(10000);"

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # TEST VERIFICATION
    # =========================================================================

    test_verification:
      total_tests: 1222
      total_files: 55
      all_passing: true
      new_tests: 41
      test_details:
        - file: src/__tests__/sync-utils.test.ts
          change: "Updated POLLING_INTERVAL_MS assertion from 2500 to 10000"
        - file: src/__tests__/integration/sync-realtime.integration.test.ts
          change: "Updated POLLING_INTERVAL_MS assertion from 2500 to 10000"
      qa_verdict: "APPROVED - 41 new tests, 1222 total, all passing"
      coverage_gaps: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Apply migration 029"
        command: |
          Via Management API (HTTPS):
          SQL: ALTER TABLE device_push_tokens ADD COLUMN role TEXT;
        reason: "Adds role column to device_push_tokens (required before Edge Function and app update)"
        urgency: high
        notes: "Must be deployed BEFORE process-notifications and app update. Existing rows will have NULL role until user next opens the app."

      - step: 2
        action: "Deploy Edge Functions send-reset-email and process-notifications"
        command: |
          supabase functions deploy send-reset-email --no-verify-jwt
          supabase functions deploy process-notifications
        reason: "send-reset-email: paginated listUsers fix. process-notifications: role-based token filtering, ward cache, 7-day cleanup."
        urgency: high
        notes: "Deploy AFTER migration 029. process-notifications now expects role column in device_push_tokens."

      - step: 3
        action: "EAS Build (app update)"
        reason: "Client-side changes: role in push token upsert, useActiveTopics parallelization, useToggleCollection upsert, 10s polling."
        urgency: medium
        notes: "Deploy after migration and Edge Functions. The app upsert will populate role for existing tokens on next app open."

    deploy_notes: |
      Deploy order is critical:
      1. Migration 029 FIRST (adds role column)
      2. Edge Functions SECOND (they query the role column)
      3. App update THIRD (populates role on upsert)
      Deploying Edge Functions before migration would cause getTargetTokens
      to fail silently (no 'role' column to filter on, returning empty results).

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: false
      uncommitted_code_files: []
      uncommitted_doc_files:
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_F012.yaml"
        - "docs/arch/ARCH_M012.yaml"
        - "docs/plans/PLAN_P012.yaml"
        - "docs/qa/QA_PLAN_012.yaml"
        - ".devteam/project-status.yaml"

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-273 (F064) implementation is correct and complete. All 7 Supabase
      resource optimization items are properly addressed:
      (1) CRITICAL: send-reset-email listUsers now paginates correctly;
      (2) HIGH: N+1 getUserById eliminated via role column in device_push_tokens;
      (3) HIGH: ward queries cached with batch Map;
      (4) MEDIUM: 7-day retention cleanup for notification_queue;
      (5) MEDIUM: useActiveTopics queries parallelized (4 sequential -> 2 rounds);
      (6) MEDIUM: polling interval increased from 2.5s to 10s;
      (7) MEDIUM: useToggleCollection check-then-act replaced with UPSERT.
      Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).
      All 17 acceptance criteria are covered. 1222 tests passing across 55
      test files, no regressions. No P0, P1, or P2 issues found. Three
      pending deploys identified with correct ordering instructions.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 8
      total_commits: 10
      tests_total: 1222
      tests_passing: 1222
      tests_flaky: 0
      test_files: 55

  # ===========================================================================
  # REV-013: CR-275 (F065) + CR-276 (F066) - Phase 013
  # ===========================================================================
  - id: REV-013
    scope: "CR-275 (F065) + CR-276 (F066) - Nickname search + Secretary permissions + Review notifications"
    date: "2026-03-08"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: efae0f7
        message: "feat(useMembers): add informal_name search to filterMembers (CR-275)"
      - hash: 28867fc
        message: "feat(permissions): add speech:assign, speech:unassign, home:next_assignments to secretary (CR-276)"
      - hash: f427b6b
        message: "feat(types): add secretary_review notification type, assigned_by_role, and topic_title (CR-276)"
      - hash: 6a5287b
        message: "feat(useSpeeches): add assigned_by_role to assign/unassign mutations (CR-276)"
      - hash: e018607
        message: "feat(migration): add migration 031 for secretary review notifications (CR-276)"
      - hash: fd190d4
        message: "feat(process-notifications): add secretary_review support (CR-276)"
      - hash: 8d87b09
        message: "feat(notificationUtils): add secretary_review to client-side buildNotificationText (CR-276)"
      - hash: 1347f79
        message: "test: update legacy tests for CR-276 secretary speech permissions"
      - hash: 319fdb4
        message: "test(f065-f066): add 46 tester tests for CR-275 and CR-276"
      - hash: e5d6230
        message: "docs: update PLAN_CONSOLIDATED, TEST_CONSOLIDATED, CODE_LEDGER for P013 (CR-275, CR-276)"
      - hash: 8e61bb2
        message: "docs(tests): update TEST_CONSOLIDATED after F065+F066 tester - all 1318 tests passing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        result: pass
        notes: |
          SPEC_F013 faithfully captures both user requests:
          - CR-275: informal_name search in filterMembers() - 7 ACs, 3 ECs
          - CR-276: Two-part change (permissions + notifications) - 30 ACs, 8 ECs
          - All user-provided texts are captured verbatim (i18n strings)
          - assigned_by_role column solution explicitly requested by user
          - "Imediato, sem agrupar" requirement reflected in AC-066-20/30

      arch_vs_spec:
        result: pass
        notes: |
          ARCH_M013 correctly maps all SPEC features to components:
          - F065: filterMembers() single change point with OR condition
          - F066A: permissions.ts Set expansion (23->26) with separate objects
          - F066B: Full data flow: client->trigger->notification_queue->process-notifications
          - Trigger restructure (ADR-036) for topic detection before early-return
          - 4 ADRs (035-038) for key design decisions

      plan_vs_arch:
        result: pass
        notes: |
          PLAN_P013 has 9 steps covering all ARCH components:
          - S013-01: filterMembers + tests (F065)
          - S013-02: permissions + tests (F066A)
          - S013-03: TypeScript types (F066B)
          - S013-04: Client mutations (F066B)
          - S013-05: Migration 031 (F066B)
          - S013-06: process-notifications (F066B)
          - S013-07: notificationUtils (F066B)
          - S013-08: Documentation
          - S013-09: Final verification
          Dependencies are correct. Coverage matrix maps all ACs/ECs to steps.

      code_vs_plan:
        result: pass
        notes: |
          All code changes match the PLAN:
          - S013-01: filterMembers() has OR condition for informal_name (line 40-41)
          - S013-02: secretary Set has 26 permissions (identical to bishopric)
          - S013-03: NotificationType includes 'secretary_review', Speech has assigned_by_role, NotificationQueue has topic_title
          - S013-04: All 3 hooks (useAssignSpeaker, useAssignTopic, useRemoveAssignment) include assigned_by_role
          - S013-05: Migration 031 has schema changes + restructured trigger
          - S013-06: buildSecretaryReviewText() with speaker/topic variants for 3 languages
          - S013-07: buildNotificationText() includes secretary_review case for 3 languages
          - S013-08: ADR-035 to ADR-038 (pending doc commit by Orchestrator)

      chain_intact: true

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found

    # =========================================================================
    # DETAILED CODE REVIEW NOTES
    # =========================================================================

    code_review_notes:
      - area: "F065 - filterMembers (src/hooks/useMembers.ts)"
        finding: |
          Clean implementation. OR condition with null coalescing:
          normalizeForSearch(m.informal_name ?? '').includes(normalized)
          The ?? '' handles null informal_name. The early return on empty search
          (line 37) prevents false matches on empty string normalization.
          8 new tests cover all scenarios including accent-insensitive, null,
          empty string, and cross-member matching.

      - area: "F066A - permissions.ts"
        finding: |
          Secretary Set expanded from 23 to 26 permissions. Both bishopric and
          secretary now have identical permission sets (26 each) but remain
          separate new Set() instances (RULE-07 from SPEC). Observer unchanged (3).
          SpeechSlot.tsx comment updated. All test assertions updated (permissions.test.ts
          and integration test).

      - area: "F066B - Migration 031"
        finding: |
          Migration is well-structured:
          1. ALTER TABLE speeches ADD COLUMN assigned_by_role TEXT (nullable)
          2. ALTER TABLE notification_queue ADD COLUMN topic_title TEXT
          3. CHECK constraint dropped/recreated with 'secretary_review'
          4. Trigger restructured per ADR-036:
             - Case 6B (topic change) BEFORE early-return
             - Early-return on status unchanged
             - Existing Cases 1,4,5,Cancellation unchanged
             - Case 6A (secretary speaker assignment) AFTER existing cases
          All INSERTs use public. schema prefix. SECURITY DEFINER maintained.

      - area: "F066B - Client mutations (useSpeeches.ts)"
        finding: |
          All 3 hooks correctly destructure role from useAuth() and include
          assigned_by_role: role in the .update() payload. Line references:
          - useAssignSpeaker (line 244): role destructured, line 257 payload
          - useAssignTopic (line 280): role destructured, line 291 payload
          - useRemoveAssignment (line 358): role destructured, line 371 payload

      - area: "F066B - process-notifications Edge Function"
        finding: |
          NotificationQueueEntry interface includes topic_title: string | null.
          buildSecretaryReviewText() handles both variants (speaker/topic)
          for all 3 languages. Routing: secretary_review goes to immediateEntries
          via the else branch (line 314-316), never grouped. Switch case at
          line 398 calls buildSecretaryReviewText with correct parameters.

      - area: "F066B - notificationUtils.ts (client-side)"
        finding: |
          buildNotificationText() includes secretary_review case for all 3
          languages. data.topic_title parameter added to type. Speaker and topic
          variants match server-side texts. Uses getOrdinal() for position ordinals.

      - area: "F066B - TypeScript types (database.ts)"
        finding: |
          NotificationType union includes 'secretary_review' (line 66).
          Speech interface has assigned_by_role: string | null (line 153).
          NotificationQueue has topic_title: string | null (line 266).

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "Apply migration 031"
        command: |
          Via Supabase Management API (HTTPS) or SQL Editor:
          Run supabase/migrations/031_secretary_review_notification.sql
        reason: "Adds assigned_by_role to speeches, topic_title to notification_queue, 'secretary_review' CHECK, restructured trigger"
        urgency: high
        notes: "Must be deployed BEFORE Edge Function and app update. The trigger function is replaced in this migration."

      - step: 2
        action: "Deploy Edge Function process-notifications"
        command: |
          supabase functions deploy process-notifications
        reason: "Adds secretary_review case to buildSecretaryReviewText and switch routing"
        urgency: high
        notes: "Deploy AFTER migration 031. The function now handles the new secretary_review notification type."

      - step: 3
        action: "EAS Build (app update)"
        reason: "Client-side changes: permissions expansion, assigned_by_role in mutations, secretary_review notification text, informal_name search."
        urgency: medium
        notes: "Deploy after migration and Edge Function. Includes both CR-275 (filterMembers) and CR-276 (permissions, mutations, notification text)."

    deploy_notes: |
      Deploy order is critical:
      1. Migration 031 FIRST (adds columns, CHECK, trigger)
      2. Edge Function SECOND (handles new notification type)
      3. App update THIRD (sends assigned_by_role, expanded permissions)
      Deploying the app before migration would cause Supabase to reject
      the assigned_by_role field (column doesn't exist yet).

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: false
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/tests/TEST_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M013.yaml"
        - "docs/plans/PLAN_P013.yaml"
        - "docs/qa/QA_PLAN_013.yaml"
        - "docs/specs/SPEC_F013.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-275 (F065) and CR-276 (F066) implementation is correct and complete.
      F065: filterMembers() now searches by both full_name and informal_name
      with accent-insensitive normalization. Single-line change automatically
      benefits all consumers (MemberSelectorModal, PrayerSelector).
      F066 Part A: Secretary permissions expanded from 23 to 26 (same as
      bishopric). Sets remain separate objects. SpeechSlot canAssign/canUnassign
      now works for secretary on speech positions automatically.
      F066 Part B: Full secretary review notification pipeline implemented:
      assigned_by_role column, trigger restructured (topic change before
      early-return), secretary_review type, process-notifications with
      buildSecretaryReviewText (3 languages x 2 variants), client-side
      notificationUtils. All immediate, never grouped.
      Chain validation passes at all 4 levels. All 37 acceptance criteria
      covered (7 for F065, 30 for F066). 1318 tests passing (1317+1 flaky
      pre-existing). No P0, P1, or P2 issues found. Three pending deploys
      identified with correct ordering.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 13
      total_commits: 11
      tests_total: 1318
      tests_passing: 1317
      tests_flaky: 1
      flaky_test_note: "useSpeechCounts.integration.test.ts - pre-existing, passes in isolation, fails in full suite due to mock leakage"
      test_files: 60

  # ===========================================================================
  # REV-016: Phase 014 - F067 (CR-277)
  # ===========================================================================

  - id: REV-016
    scope: "F067 Phase 014 - Announcements list UX improvement (CR-277)"
    date: "2026-03-08"
    reviewer: reviewer-agent
    phase: 14
    feature: F067
    iteration: 1
    spec: SPEC_F014
    arch: ARCH_M014
    plan: PLAN_P014

    commits:
      - sha: ce4ff67
        message: "feat(i18n): add agenda.addAnnouncement key to all 3 locales (CR-277)"
      - sha: 5ebfc0b
        message: "feat(AnnouncementsList): add structured list component with add/delete/edit/reorder (CR-277)"
      - sha: 84c4ee1
        message: "feat(AgendaForm): replace announcements DebouncedTextInput with AnnouncementsList (CR-277)"
      - sha: 35614b3
        message: "feat(presentation): add bullet_list type to PresentationField and render as bullet points (CR-277)"
      - sha: 0487b01
        message: "docs(phase-014): add ADR-039 to ADR-043 and update consolidated docs (CR-277)"
      - sha: 6a5c8c1
        message: "test(f067): add 46 tests for AnnouncementsList and update CODE_LEDGER (CR-277)"
      - sha: 37d9e0d
        message: "test(f067): add 57 tester tests for AnnouncementsList (CR-277)"
      - sha: 986e1b3
        message: "docs(tests): update TEST_CONSOLIDATED after F067 tester - all 1421 tests passing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        result: pass
        notes: |
          SPEC_F014 faithfully captures the user request for CR-277:
          - Announcements field transformed from single multiline text to list items
          - Add (bottom input, submit/blur), Delete (X button), Inline edit (tap to edit),
            Reorder (long-press or up/down arrows per OQ-1 fallback) all specified
          - Storage format unchanged: \n-joined TEXT column, no migration
          - Disabled/observer: read-only, no controls
          - Presentation Mode: bullet_list type, each item as "bullet item"
          - i18n key agenda.addAnnouncement in 3 locales
          - 21 acceptance criteria cover all behaviors, 8 edge cases
          - User's confirmed decisions (empty state, inline edit, drag/reorder,
            offline, presentation) all reflected in ACs

      arch_vs_spec:
        result: pass
        notes: |
          ARCH_M014 correctly maps SPEC to components:
          - AnnouncementsList.tsx (NEW): encapsulates all list logic with props
            matching SPEC (value, onSave, disabled, placeholder)
          - AgendaForm.tsx (MODIFIED): replaces DebouncedTextInput with AnnouncementsList
          - usePresentationMode.ts (MODIFIED): extends PresentationField type union
            with 'bullet_list', changes announcements field type
          - presentation.tsx (MODIFIED): renders bullet_list type as bullet-prefixed lines
          - i18n (3 files): addAnnouncement key
          - 5 ADRs (039-043): storage format, bullet_list type, dedicated component,
            no debounce, up/down arrows instead of drag
          - Contracts defined for all integrations

      plan_vs_arch:
        result: pass
        notes: |
          PLAN_P014 has 6 steps covering all ARCH components:
          - S014-01: i18n keys (3 locales)
          - S014-02: AnnouncementsList component (parseItems, joinItems, full UI)
          - S014-03: AgendaForm integration (replace DebouncedTextInput)
          - S014-04: Presentation Mode (bullet_list type + rendering)
          - S014-05: Documentation (ADR-039 to ADR-043, consolidated docs)
          - S014-06: Final verification (tsc + vitest)
          Dependency graph is correct. Coverage matrix maps all 21 ACs and 8 ECs.

      code_vs_plan:
        result: pass
        notes: |
          Code implementation follows PLAN precisely:
          - S014-01: All 3 locales have agenda.addAnnouncement with correct translations
          - S014-02: AnnouncementsList.tsx created with exported parseItems/joinItems,
            props interface matching ARCH contract, all behaviors (add, delete,
            inline edit, reorder with up/down arrows, disabled state, external sync)
          - S014-03: AgendaForm imports AnnouncementsList, replaces DebouncedTextInput
            block, passes value/onSave/disabled/placeholder correctly,
            announcementsInput style preserved for welcome_new_families
          - S014-04: PresentationField type extended, announcements uses 'bullet_list',
            PresentationFieldRow handles bullet_list rendering with bullet prefix
          - S014-05: ADR-039 to ADR-043 added to ARCH_CONSOLIDATED, SPEC_CONSOLIDATED
            updated
          - S014-06: All 1421 tests pass, tsc compiles

      chain_intact: true

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      - area: "AnnouncementsList.tsx"
        finding: |
          Well-structured component. parseItems/joinItems exported for testing.
          Props interface matches ARCH contract exactly. Internal state management
          uses useState and useRef for editing guards (isEditingRef pattern from
          DebouncedTextInput). useEffect syncs external value when not editing.
          All handlers (add, delete, edit, reorder) wrapped in useCallback with
          correct dependency arrays. saveItems encapsulates state update + onSave.
          Disabled state renders bullet-prefixed text with no controls.
          Active state renders item rows with edit TextInput, ChevronUp/Down,
          XIcon delete, and add-input at bottom with dashed border.
          Styling follows existing patterns (borderRadius: 6, padding).

      - area: "AgendaForm.tsx integration"
        finding: |
          Line 31: import { AnnouncementsList } from './AnnouncementsList';
          Lines 245-252: AnnouncementsList replaces DebouncedTextInput.
          value: agenda.announcements ?? null (correctly null, not empty string).
          onSave: updateField('announcements', text) - same pattern.
          disabled: isObserver - which includes offline mode (disabled prop ORed).
          placeholder: t('agenda.addAnnouncement') - correct i18n key.
          styles.announcementsInput preserved on line 949-951, still used by
          welcome_new_families on line 235. DebouncedTextInput import kept.

      - area: "usePresentationMode.ts"
        finding: |
          Line 27: type union extended to include 'bullet_list'.
          Line 98: announcements field uses type: 'bullet_list' instead of 'multiline'.
          Value remains raw \n-joined string, unchanged.
          Other multiline fields (recognized_names, welcome_new_families,
          ward_business) remain 'multiline' - correct separation.

      - area: "presentation.tsx - PresentationFieldRow"
        finding: |
          Lines 155-188: bullet_list handling added before the default return.
          Splits value by \n, filters empty strings. Empty list renders "---".
          Non-empty list renders each item as Text with bullet prefix (\u2022).
          Font sizes follow existing pattern (fontSizes?.label, fontSizes?.value).
          Uses key={idx} for map - acceptable for this use case since items
          don't have stable IDs and the list is read-only in presentation mode.

      - area: "i18n locales (3 files)"
        finding: |
          pt-BR: "Adicionar anuncio" (with accent u on u)
          en-US: "Add announcement"
          es-LA: "Agregar anuncio"
          All at line 246 in the agenda section after "announcements" key.
          Translations are correct and natural.

      - area: "Tests (f067-announcements-list.test.ts)"
        finding: |
          46 coder tests + 57 tester tests = 103 total tests for F067.
          Tests cover: i18n keys, parseItems/joinItems, component behavior
          (add, delete, edit, reorder, disabled state, external sync,
          backward compatibility), AgendaForm integration, presentation mode
          (bullet_list type, rendering, empty state, single item, filtering).
          Tests use inline copies of parseItems/joinItems due to react-native
          import limitation in node env - acceptable pattern.
          buildPresentationCards tested with real function import.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "EAS Build (app update)"
        reason: "Client-side only changes: new AnnouncementsList component, AgendaForm integration, presentation bullet_list rendering, i18n key."
        urgency: low
        notes: |
          No database migration, no Edge Function changes. Pure UI enhancement.
          Can be bundled with other pending client-side changes (CR-262, 271, 272, 273, 274).

    deploy_notes: |
      CR-277 is a client-only change. No server-side deploys required.
      The announcements TEXT column and storage format are unchanged.
      Can be deployed alongside the existing pending EAS Build.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: false
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/CHANGE_REQUESTS.yaml"
      notes: |
        Git status shows only 2 modified doc files (project-status.yaml and
        CHANGE_REQUESTS.yaml). No uncommitted code files. All CR-277 code
        changes are committed (8 commits: ce4ff67..986e1b3).
        The doc files are expected to be committed by the Orchestrator.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-277 (F067) implementation is correct and complete. The announcements
      field in AgendaForm has been successfully transformed from a single
      multiline DebouncedTextInput into a structured list component
      (AnnouncementsList) with add, delete, inline-edit, and reorder (up/down
      arrows) capabilities. Storage format is preserved (\n-joined TEXT column,
      no migration). Presentation Mode correctly renders announcements as
      bullet-prefixed items using the new 'bullet_list' PresentationField type.
      Disabled/observer mode shows read-only bullet text without controls.
      i18n key agenda.addAnnouncement added to all 3 locales.
      Chain validation passes at all 4 levels. All 21 acceptance criteria
      and 8 edge cases are covered. 1421 tests pass (62 test files).
      No P0, P1, or P2 issues found. Only deploy needed is a client-side
      EAS Build which can be bundled with other pending deploys.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 7
      total_commits: 8
      tests_total: 1421
      tests_passing: 1421
      test_files: 62

  # ===========================================================================
  # REV-017: Phase 015 - F068 (CR-278) + F069 (CR-279) + F070 (CR-280)
  # EditableListField generalization + grip icon + zebra striping
  # ===========================================================================

  - id: REV-017
    scope: "F068+F069+F070 Phase 015 - EditableListField generalization, grip icon, zebra striping (CR-278, CR-279, CR-280)"
    date: "2026-03-08"
    reviewer: reviewer-agent
    phase: 15
    features: [F068, F069, F070]
    iteration: 1
    spec: SPEC_F015
    arch: ARCH_M015
    plan: PLAN_P015

    commits:
      - sha: 5cc099c
        message: "feat(EditableListField): rename AnnouncementsList to EditableListField and add i18n keys (CR-278)"
      - sha: 0435634
        message: "feat(AgendaForm): replace DebouncedTextInput with EditableListField for welcome/sustaining (CR-278)"
      - sha: 3e89fb3
        message: "feat(EditableListField): add auto-split for \\n in add-input and inline-edit (CR-278)"
      - sha: e210cd8
        message: "feat(EditableListField): replace arrows with GripIcon drag-to-reorder (CR-279)"
      - sha: 07da63c
        message: "feat(presentation): add zebra striping to bullet_list and change welcome/sustaining to bullet_list (CR-278, CR-280)"
      - sha: b4b7c6f
        message: "docs(phase-015): add ADR-044 to ADR-047, update consolidated docs (CR-278, CR-279, CR-280)"
      - sha: 5afb6e9
        message: "docs(tests): update TEST_CONSOLIDATED and CODE_LEDGER after F068/F069/F070 - all 1474 tests passing"
      - sha: caf0150
        message: "test(f068-f070): add 98 tester tests for EditableListField, GripIcon, zebra striping (CR-278, CR-279, CR-280)"
      - sha: 3fe5a2f
        message: "docs(tests): update TEST_CONSOLIDATED after F068/F069/F070 tester - all 1572 tests passing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        result: pass
        notes: |
          SPEC_F015 faithfully captures all 3 user requests:
          - CR-278: Rename AnnouncementsList to EditableListField, apply to
            welcome_new_families and sustaining_releasing, auto-split \n on
            paste/edit, presentation mode bullet_list for all 3 fields, new
            i18n keys (addWelcome, addWardBusiness) in 3 locales. recognized_names
            excluded (kept on ActorSelector) as requested.
          - CR-279: Replace ChevronUp/Down arrows with grip icon on LEFT side,
            word-wrap (remove numberOfLines={1}).
          - CR-280: Zebra striping font color in bullet_list presentation items.
            Font color only, not background. Subtle difference.
          19 acceptance criteria across F068 (19 ACs), F069 (7 ACs), F070 (8 ACs)
          cover all behaviors. 5 OQs resolved with defaults. 6 assumptions documented.

      arch_vs_spec:
        result: pass
        notes: |
          ARCH_M015 correctly maps SPEC to components:
          - EditableListField.tsx (RENAMED): auto-split in handleAdd + finishEdit,
            DraggableFlatList for drag-and-drop, GripIcon on left
          - AgendaForm.tsx (MODIFIED): 3 EditableListField usages (announcements,
            welcome_new_families, sustaining_releasing)
          - GripIcon (NEW): added to icons/index.tsx, 6-circle grip pattern
          - usePresentationMode.ts: welcome/sustaining changed to bullet_list
          - presentation.tsx: zebra striping via textZebraFaded color
          - theme.ts: new textZebraFaded color added
          - i18n: 2 new keys in 3 locales
          - 4 ADRs (044-047), 2 superseded (041, 043)
          - Contracts defined for all integrations

      plan_vs_arch:
        result: pass
        notes: |
          PLAN_P015 has 7 steps covering all ARCH components:
          - S015-01: i18n keys + rename component + update imports
          - S015-02: Apply to welcome/sustaining fields
          - S015-03: Auto-split \n
          - S015-04: Install draggable-flatlist + GripIcon + drag
          - S015-05: Presentation mode (bullet_list + zebra)
          - S015-06: Documentation (ADRs)
          - S015-07: Final verification
          Dependency graph is correct. Coverage matrix maps all 34 ACs and
          11 edge cases to steps. No circular dependencies.

      code_vs_plan:
        result: pass
        notes: |
          Code implementation follows PLAN accurately across 9 commits:
          - S015-01 (5cc099c): File renamed via git mv, component/props renamed,
            no references to AnnouncementsList remain in codebase. i18n keys
            addWelcome and addWardBusiness added to all 3 locales.
          - S015-02 (0435634): welcome_new_families and sustaining_releasing both
            use EditableListField with correct props (value ?? null, disabled=isObserver,
            correct placeholders). announcementsInput style removed.
          - S015-03 (3e89fb3): handleAdd and finishEdit both split by \n, filter
            empty, and trim. finishEdit handles 0 items (delete), 1 item (update),
            N items (splice). Add-input is multiline with blurOnSubmit.
          - S015-04 (e210cd8): react-native-draggable-flatlist installed,
            DraggableFlatList replaces items.map(), GripIcon on left with
            onLongPress={drag}, ScaleDecorator wraps rows, XIcon on right.
            ChevronUp/Down removed. numberOfLines removed from both active
            and disabled states. Disabled state has no bullet prefix (ADR-047).
            alignItems: 'flex-start' for text wrap alignment.
          - S015-05 (07da63c): theme.ts has textZebraFaded in ThemeColors
            interface and both palettes. usePresentationMode changes welcome and
            sustaining to bullet_list. presentation.tsx applies alternating
            color via idx % 2.
          - S015-06 (b4b7c6f): ADR-044 to ADR-047 documented, ADR-041/043
            marked superseded.
          - S015-07: Tester added 98 tests (caf0150), total 1572 passing.
          All done_when criteria satisfied.

      chain_intact: true

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:

      - area: "EditableListField.tsx (renamed)"
        finding: |
          Clean rename from AnnouncementsList. Props interface unchanged
          (value, onSave, disabled, placeholder). Auto-split logic correctly
          implemented in both handleAdd and finishEdit using split('\n').map(trim).
          filter(nonEmpty) pattern. DraggableFlatList properly integrated with
          keyExtractor, onDragEnd, scrollEnabled={false}. GripIcon positioned
          on left with onLongPress={drag}. ScaleDecorator wraps each row.
          Disabled state renders plain text without controls or bullet prefix
          (consistent with ADR-047). handleDragEnd cancels editing state.
          All callbacks wrapped in useCallback with correct dependencies.
          multiline add-input with blurOnSubmit allows pasting multi-line text.

      - area: "AgendaForm.tsx integration"
        finding: |
          Three EditableListField instances correctly configured:
          - announcements: value ?? null, placeholder addAnnouncement (unchanged)
          - welcome_new_families: value ?? null, placeholder addWelcome (new)
          - sustaining_releasing: value ?? null, placeholder addWardBusiness (new)
          All use disabled={isObserver} and updateField callback.
          DebouncedTextInput removed from both fields. announcementsInput style
          removed (no orphaned styles). recognized_names correctly unchanged
          (still uses ActorSelector).

      - area: "GripIcon (icons/index.tsx)"
        finding: |
          GripIcon follows existing icon pattern (IconProps with size/color).
          Uses 6 Circle elements from react-native-svg (already imported).
          Lucide grip-horizontal pattern: 3x2 grid at cx={5,12,19} cy={9,15}.
          fill={color} (not stroke) for solid dots. Consistent with other icons.

      - area: "usePresentationMode.ts"
        finding: |
          welcome_new_families changed from 'multiline' to 'bullet_list' (line 91).
          sustaining_releasing changed from 'multiline' to 'bullet_list' (line 117).
          announcements already used 'bullet_list' from CR-277.
          recognized_names still uses 'multiline' - correct.

      - area: "presentation.tsx (zebra striping)"
        finding: |
          PresentationFieldRow bullet_list branch correctly applies alternating
          color: idx % 2 === 0 uses colors.text (normal), idx % 2 === 1 uses
          colors.textZebraFaded (faded). Applied to all bullet items uniformly.
          Non-bullet_list fields (text, hymn, multiline) are unaffected - they
          use the default return which always uses colors.text.

      - area: "theme.ts"
        finding: |
          textZebraFaded added to ThemeColors interface after textInverse.
          lightColors: '#4A4A4A' (midpoint between text #1A1A1A and textSecondary
          #5A5A5A). darkColors: '#B8C5D4' (midpoint between text #F1F5F9 and
          textSecondary #94A3B8). Both are distinct from textTertiary.
          Subtle difference that aids line tracking without looking disabled.

      - area: "i18n locales (3 files)"
        finding: |
          pt-BR: addWelcome "Adicionar boas-vindas", addWardBusiness "Adicionar apoio ou desobrigacao"
          en-US: addWelcome "Add welcome", addWardBusiness "Add sustaining or release"
          es-LA: addWelcome "Agregar bienvenida", addWardBusiness "Agregar apoyo o relevo"
          All at line 247-248 in the agenda section. Translations are correct.

      - area: "Tests"
        finding: |
          1572 total tests passing across 63 test files. Phase 15 added 151 new
          tests (1421 -> 1572) covering all ACs and edge cases for F068/F069/F070.
          Test files updated to reference EditableListField (no AnnouncementsList
          references remaining). Tests cover: i18n keys, parseItems/joinItems,
          auto-split behavior, GripIcon presence, word-wrap, disabled state,
          zebra striping colors, presentation mode bullet_list for all 3 fields.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues: []

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "EAS Build (app update)"
        reason: |
          Client-side changes: EditableListField (renamed + auto-split + drag),
          AgendaForm (welcome/sustaining fields updated), GripIcon (new icon),
          theme (textZebraFaded), presentation (zebra striping, bullet_list for
          welcome/sustaining), i18n (2 new keys). New npm dependency:
          react-native-draggable-flatlist.
        urgency: low
        notes: |
          No database migration, no Edge Function changes. Pure UI enhancement.
          The new npm dependency (react-native-draggable-flatlist) requires a
          native rebuild (EAS Build), not just an OTA update.
          Can be bundled with other pending client-side changes.

    deploy_notes: |
      CR-278/279/280 are client-only changes. No server-side deploys required.
      The welcome_new_families and sustaining_releasing TEXT columns and storage
      format are unchanged. The new react-native-draggable-flatlist dependency
      requires a native EAS Build (not an OTA update via expo-updates).

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files: []
      notes: |
        Git status is clean. All Phase 15 code changes committed across 9
        commits (5cc099c..3fe5a2f). Working tree clean, nothing to commit.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-278, CR-279, CR-280 (F068, F069, F070) implementation is correct and
      complete. AnnouncementsList successfully renamed to EditableListField and
      applied to all 3 fields (announcements, welcome_new_families,
      sustaining_releasing). Auto-split for \n works in both add-input and
      inline edit. GripIcon with react-native-draggable-flatlist provides real
      drag-and-drop reorder, replacing ChevronUp/Down arrows. Word-wrap enabled
      (no truncation). Zebra striping via textZebraFaded theme color provides
      subtle alternating font colors in bullet_list presentation items.
      Chain validation passes at all 4 levels. All 34 acceptance criteria and
      11 edge cases covered. 1572 tests pass (63 test files). No P0, P1, or
      P2 issues found. Only deploy needed is EAS Build (client-side + new
      native dependency).

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 10
      total_commits: 9
      tests_total: 1572
      tests_passing: 1572
      test_files: 63

  # ===========================================================================
  # REV-016 — Phase 016: CR-281, CR-282, CR-283 (F068/F069 bugfixes + F071)
  # ===========================================================================

  - id: REV-016
    scope: "CR-281, CR-282, CR-283 - EditableListField bugfixes + recognized_names rework"
    date: "2026-03-08"
    reviewer: reviewer-agent
    iteration: 1
    commits:
      - hash: 687b37e
        step: S016-01
        message: "fix(EditableListField): add multiline, blurOnSubmit, paddingVertical:0 to inline edit TextInput (CR-281)"
      - hash: 538b2cc
        step: S016-02
        message: "fix(EditableListField): add activationDistance={9999} to DraggableFlatList (CR-282)"
      - hash: 5e3e952
        step: S016-03
        message: "feat(i18n+types): add agenda.addPresence i18n key + change recognized_names to string|null (CR-283)"
      - hash: 17e5a74
        step: S016-04
        message: "feat(migration): add migration 032 to convert recognized_names from TEXT[] to TEXT (CR-283)"
      - hash: d758fa5
        step: S016-05
        message: "feat(EditableListField): add onItemPress and onAddPress callback props (CR-283)"
      - hash: ccc31e5
        step: S016-06
        message: "feat(ActorSelector): add disabledNames prop for graying out selected actors (CR-283)"
      - hash: cf95cf5
        step: S016-07
        message: "feat(AgendaForm): replace recognized_names with EditableListField + ActorSelector integration (CR-283)"
      - hash: 08cca39
        step: S016-08
        message: "test(f071): add 39 tests for CR-281/282/283 and update existing tests (S016-08)"
      # Documentation and test updates
      - hash: 1278c0d
        step: S016-09
        message: "docs(phase-016): update TEST_CONSOLIDATED with F071 tests (S016-09)"
      - hash: 1113ddf
        step: S016-10
        message: "docs(phase-016): update CODE_LEDGER with CR-281/282/283 commits (S016-10)"
      - hash: e92dae0
        step: S016-10
        message: "docs(phase-016): mark PLAN_P016 as done"
      - hash: 792d5a1
        step: tester
        message: "test(f071): add 96 tester tests for CR-281/282/283 and fix flaky useSpeechCounts test"
      - hash: d19bce3
        step: tester
        message: "docs(tests): update TEST_CONSOLIDATED after F071 tester - all 1707 tests passing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        detail: |
          User requested three changes:
          1. CR-281: Bug - EditableListField inline edit becomes one line, cursor disappears.
          2. CR-282: Bug - Scroll blocked when touching list items (gesture handler captures touch).
          3. CR-283: Feature - recognized_names -> EditableListField with ActorSelector single-select.
          SPEC_F016 covers all three CRs comprehensively:
          - CR-281: Root cause identified (missing multiline, paddingVertical, autoFocus position).
            4 ACs + 2 edge cases all faithful to user's bug report.
          - CR-282: Root cause identified (activationDistance=0 captures all touches).
            4 ACs + 2 edge cases covering scroll, drag, tap behavior.
          - CR-283: Full feature specification with 12 ACs + 5 edge cases covering
            add/edit/delete via ActorSelector, disabledNames, drag reorder, presentation,
            offline mode, empty state, DB migration 032. 6 open questions resolved.
          No scope creep. All requirements match user intent.

      arch_vs_spec:
        status: pass
        detail: |
          ARCH_M016 maps every SPEC requirement to components:
          - CR-281 -> EditableListField TextInput: multiline, blurOnSubmit, editInput style
          - CR-282 -> DraggableFlatList: activationDistance={9999}
          - CR-283 -> 5 components: EditableListField (onItemPress/onAddPress), ActorSelector
            (disabledNames), AgendaForm (integration), usePresentationMode (bullet_list),
            database.ts (type change), migration 032
          Minor documentation discrepancy: ARCH diagram (line 53) mentions renderAddArea
          prop that is not in the components spec or code. This is a P2 doc-only issue
          in the diagram section; the authoritative components section correctly specifies
          only onItemPress and onAddPress.

      plan_vs_arch:
        status: pass
        detail: |
          PLAN_P016 has 10 steps that follow ARCH precisely:
          S016-01 (CR-281 bugfix) -> ARCH component 1
          S016-02 (CR-282 bugfix) -> ARCH component 2
          S016-03 (i18n + TS type) -> ARCH i18n + types
          S016-04 (migration 032) -> ARCH migration
          S016-05 (new props) -> ARCH component 3 (depends on S01+S02)
          S016-06 (disabledNames) -> ARCH component 4
          S016-07 (AgendaForm rework) -> ARCH component 5 (depends on S03+S05+S06)
          S016-08 (test updates) -> follows S07
          S016-09 (documentation) -> follows S07
          S016-10 (final verification) -> depends on all
          Dependencies are correct. File paths match.

      code_vs_plan:
        status: pass
        detail: |
          All 10 plan steps implemented correctly:
          S016-01: TextInput has multiline, blurOnSubmit, editInput style with paddingVertical:0.
            autoFocus preserved, returnKeyType="done" preserved. (lines 163-173)
          S016-02: DraggableFlatList has activationDistance={9999}, scrollEnabled={false}
            preserved. (lines 198-205)
          S016-03: i18n key agenda.addPresence added in all 3 locales with correct
            translations. SundayAgenda.recognized_names type changed to string | null.
          S016-04: Migration 032 exists with temp column strategy, array_to_string,
            NULL/empty array guards.
          S016-05: onItemPress and onAddPress props added. Backward compatible: when
            not provided, original inline edit and TextInput add area preserved.
          S016-06: disabledNames prop on ActorSelector. Disabled actors at opacity 0.4,
            onPress undefined, disabled={true}. renderItem deps include disabledNames.
          S016-07: AgendaForm uses EditableListField with onItemPress/onAddPress.
            recognizeSelector state manages add/edit mode. ActorSelector integration
            with roleFilter="recognize", disabledNames computed correctly (edit excludes
            current, add includes all). Old recognizing code removed.
          S016-08: Tests updated for new recognized_names behavior.
          S016-09/10: Documentation and verification complete.

      chain_intact: true

    # =========================================================================
    # CODE REVIEW FINDINGS
    # =========================================================================

    code_review:
      - area: "EditableListField.tsx (CR-281 + CR-282 + CR-283)"
        finding: |
          CR-281 fix is correct: TextInput at line 163-173 has multiline, blurOnSubmit,
          and styles.editInput with paddingVertical:0. autoFocus preserved for cursor.
          CR-282 fix is correct: DraggableFlatList at line 204 has activationDistance={9999}.
          Drag only activates via onLongPress={drag} on GripIcon Pressable (line 159).
          CR-283 props are correct: onItemPress/onAddPress optional, backward compatible.
          When onItemPress provided, tap calls it (line 175-181). When onAddPress
          provided, add area is Pressable (lines 206-214). Both callbacks properly
          integrated. parseItems/joinItems exported for AgendaForm import.

      - area: "ActorSelector.tsx (CR-283)"
        finding: |
          disabledNames prop added to interface (line 41) and destructured (line 51).
          In renderItem (line 172): isDisabled check uses includes(). Disabled actors
          have opacity 0.4, onPress=undefined, disabled={true} (lines 188-190).
          renderItem useCallback deps correctly include disabledNames (line 224).
          Implementation matches SPEC AC-283-05 and AC-283-12.

      - area: "AgendaForm.tsx (CR-283)"
        finding: |
          Import: EditableListField, parseItems, joinItems from EditableListField (line 31).
          State: recognizeSelector with mode/editIndex (lines 83-86).
          recognized_names FieldRow (lines 202-215): uses EditableListField with
          value, onSave, disabled, placeholder, onItemPress, onAddPress.
          ActorSelector integration (lines 582-608): visible when recognizeSelector set,
          roleFilter="recognize", onSelect handles add/edit modes correctly.
          disabledNames IIFE correctly excludes current edit target.
          Old recognizing code fully removed (no recognizingContent/Names/Name styles).
          No multiSelect usage in AgendaForm.

      - area: "usePresentationMode.ts (CR-283)"
        finding: |
          recognized_names field (lines 80-86): guard uses truthy check (not .length),
          value is raw string (not .join), type is 'bullet_list'. All correct per PLAN.

      - area: "database.ts (CR-283)"
        finding: |
          recognized_names type changed from string[] | null to string | null (line 187).
          No remaining string[] references in production code (grep verified).

      - area: "Migration 032"
        finding: |
          Correct 3-step approach: add temp column, migrate data with
          array_to_string(recognized_names, E'\n'), drop old + rename.
          WHERE clause guards NULL and empty arrays.
          Note: Migration must be deployed via SQL Editor BEFORE the
          client-side update, since the client now expects string | null.

      - area: "i18n locales (3 files)"
        finding: |
          pt-BR: "Reconhecer nova presenca" (line 247)
          en-US: "Recognize new presence" (line 247)
          es-LA: "Reconocer nueva presencia" (line 247)
          All under agenda.addPresence key. Translations are correct.

      - area: "Tests"
        finding: |
          1707 total tests passing across 65 test files. Phase 16 added 135 new
          tests (1572 -> 1707) covering all ACs and edge cases for CR-281/282/283.
          Test file f071-recognized-names.test.ts covers: inline edit props,
          DraggableFlatList props, i18n keys, type checks, migration file checks,
          EditableListField callback props, ActorSelector disabledNames,
          AgendaForm integration, presentation mode.
          Test file f071-tester.test.ts (96 tester tests) covers: behavior
          verification including parseItems/joinItems, reorder, presentation
          rendering, migration edge cases, offline/observer mode.
          Existing tests updated (f068-f069-f070-tester, f067-announcements-list,
          f067-tester) for new recognized_names behavior.

    # =========================================================================
    # ISSUES
    # =========================================================================

    issues:
      - id: R016-P2-001
        severity: P2
        category: quality
        location: "docs/arch/ARCH_M016.yaml:53"
        description: |
          ARCH_M016 diagram section (line 53) mentions "renderAddArea?: () => React.ReactNode"
          as a prop on EditableListField. This prop does not exist in the code or in the
          authoritative components section of the ARCH. The diagram is stale/inaccurate
          on this point.
        suggestion: |
          Remove renderAddArea reference from the ARCH_M016 diagram lines 53 and 58.
          No functional impact - the authoritative components section is correct.

    # =========================================================================
    # PENDING DEPLOYS
    # =========================================================================

    pending_deploys:
      - step: 1
        action: "DB Migration 032 via SQL Editor"
        reason: |
          Migration 032 converts recognized_names from TEXT[] to TEXT.
          Must be run BEFORE the client-side update because the new client
          code expects string | null, not string[] | null.
        urgency: high
        notes: |
          Deploy order:
          1. Migration 032 via SQL Editor (FIRST - breaking change for old client)
          2. EAS Build (client-side update with new EditableListField features)
          Migration is backward-compatible during transition: old client that
          expects TEXT[] will fail on string data, so migration must be
          paired with client update promptly.

      - step: 2
        action: "EAS Build (app update)"
        reason: |
          Client-side changes: EditableListField (multiline fix, activationDistance fix,
          onItemPress/onAddPress props), ActorSelector (disabledNames prop),
          AgendaForm (recognized_names EditableListField integration),
          usePresentationMode (bullet_list for recognized_names),
          database.ts (type change), i18n (addPresence key).
        urgency: high
        notes: |
          Must be deployed shortly after Migration 032. The react-native-draggable-flatlist
          native dependency (from Phase 015) already requires an EAS Build. This can
          be bundled with the Phase 015 EAS Build if not yet deployed.

    deploy_notes: |
      CR-281/CR-282 are client-only bugfixes. CR-283 requires BOTH a database
      migration (032) AND a client-side update. Deploy order is critical:
      1. Migration 032 via SQL Editor (converts TEXT[] to TEXT)
      2. EAS Build (new client code that reads/writes string | null)
      The migration is NOT backward-compatible with old client code that expects
      TEXT[], so the EAS Build should follow promptly after migration.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files: []
      notes: |
        Git status is clean. All Phase 16 code changes committed across 13
        commits (687b37e..d19bce3). Working tree clean, nothing to commit.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-281, CR-282, CR-283 (F068/F069 bugfixes + F071 recognized_names rework)
      implementation is correct and complete. Chain validation passes at all 4 levels.
      CR-281: Inline edit TextInput fixed with multiline, blurOnSubmit, paddingVertical:0.
      CR-282: Scroll blocking fixed with activationDistance={9999} on DraggableFlatList.
      CR-283: recognized_names successfully transformed from TEXT[] multi-select to
      EditableListField + single-select ActorSelector. Migration 032 converts data.
      New disabledNames prop on ActorSelector prevents duplicate selection. Presentation
      mode uses bullet_list with zebra striping. All 20 acceptance criteria and 9 edge
      cases covered. 1707 tests pass (65 test files). One P2 doc issue (stale
      renderAddArea in ARCH diagram). No P0 or P1 issues. Deploy requires Migration 032
      first, then EAS Build.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 1
      files_reviewed: 8
      total_commits: 13
      tests_total: 1707
      tests_passing: 1707
      test_files: 65

  # ===========================================================================
  # REVIEW 19 — Phase 017 (CR-284): E2E Testing with Maestro Phase 1
  # ===========================================================================
  - id: REV-019
    scope: "CR-284 — E2E Testing with Maestro Phase 1: Setup + Auth Flows"
    date: "2026-03-08"
    reviewer: reviewer-agent
    phase: 17
    feature: F017_E2E
    iteration: 1

    commits:
      - sha: e322ac6
        message: "feat(login): add testID props to 6 login screen elements (CR-284)"
      - sha: 32c8513
        message: "feat(register): add testID props to 15 register screen elements (CR-284)"
      - sha: da45ac3
        message: "feat(settings): add testID to sign-out Pressable button (CR-284)"
      - sha: aadbe28
        message: "feat(e2e): create Maestro flow 01-register for new ward registration (CR-284)"
      - sha: aea8f0d
        message: "feat(e2e): create Maestro flow 02-login-logout with login, logout, and invalid login (CR-284)"
      - sha: 2b1fb9c
        message: "docs(e2e): create README with setup, run commands, and conventions (CR-284)"
      - sha: b917459
        message: "chore(plans): mark P017 E2E testing phase 1 as implemented (CR-284)"
      - sha: 15bf8d2
        message: "chore(code-ledger): add CR-284 E2E testing commits to CODE_LEDGER v17"
      - sha: c9f359b
        message: "test(e2e): add tests for E2E testing infrastructure CR-284"
      - sha: b5b2d5b
        message: "docs(tests): update TEST_CONSOLIDATED v22 for CR-284 E2E testing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        notes: |
          SPEC_F017 fully covers the user request for E2E testing with Maestro:
          - testID props on login.tsx (6 elements), register.tsx (15 elements),
            settings/index.tsx (1 element) per the request
          - Two Maestro flow files: 01-register and 02-login-logout
          - e2e/README.md documentation
          - testID naming convention {screen}-{element}-{type} per user spec
          - All assumptions validated (production Supabase, pt-BR strings, etc.)

      arch_vs_spec:
        status: pass
        notes: |
          ARCH_M017 correctly maps all SPEC components:
          - 3 TestID Layer components for the 3 modified source files
          - 2 Maestro Flow components with correct dependencies
          - 1 E2E README component
          - Contracts define all 22 testIDs with correct component types
          - ADR-052 (testID naming) and ADR-053 (numbered flow prefix) align with SPEC

      plan_vs_arch:
        status: pass
        notes: |
          PLAN_P017 has 7 steps (S017-01 through S017-07) that follow ARCH:
          - Steps 1-3: testID additions to login, register, settings (parallel)
          - Step 4: 01-register flow (depends on S017-01, S017-02)
          - Step 5: 02-login-logout flow (depends on S017-01, S017-03)
          - Step 6: README (independent)
          - Step 7: Final verification (depends on all)
          Dependencies match ARCH component dependencies exactly.

      code_vs_plan:
        status: pass
        notes: |
          Code implements exactly what PLAN specifies:
          - login.tsx: 6 testID props at correct positions (error view, 2 inputs,
            3 touchable opacities) — verified by grep
          - register.tsx: 12 testID code lines generating 15 runtime testIDs
            (7 static inputs + 2 dynamic role radios + 3 dynamic language radios +
            2 static buttons + 1 error view) — uses template literals as planned
          - settings/index.tsx: 1 testID on sign-out Pressable — correct placement
          - 01-register.yaml: Matches PLAN flow structure (evalScript, testID targeting,
            extendedWaitUntil 15s, pt-BR assertions for 4 tabs)
          - 02-login-logout.yaml: 3 parts (login/logout/invalid login) as planned,
            uses output variables from 01-register, native Alert "Confirmar" handling
          - e2e/README.md: All 7 sections present (prerequisites, build, run,
            test data, testID convention, troubleshooting)
          - CODE_LEDGER updated with all 7 CR-284 commits

      chain_intact: true

    # =========================================================================
    # CODE REVIEW FINDINGS
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found.

    code_review_details:
      login_tsx:
        testIDs_verified:
          - "login-error-text on View (line 77)"
          - "login-email-input on TextInput (line 112)"
          - "login-password-input on TextInput (line 138)"
          - "login-submit-button on TouchableOpacity (line 151)"
          - "login-forgot-password-button on TouchableOpacity (line 166)"
          - "login-create-account-button on TouchableOpacity (line 179)"
        count: 6
        naming_convention: pass
        zero_functional_impact: true

      register_tsx:
        testIDs_verified:
          - "register-error-text on View (line 154)"
          - "register-fullname-input on TextInput (line 186)"
          - "register-email-input on TextInput (line 211)"
          - "register-password-input on TextInput (line 234)"
          - "register-confirm-password-input on TextInput (line 257)"
          - "register-stake-input on TextInput (line 278)"
          - "register-ward-input on TextInput (line 299)"
          - "register-role-${r}-radio on TouchableOpacity (line 321, dynamic)"
          - "register-language-${lang}-radio on TouchableOpacity (line 354, dynamic)"
          - "register-timezone-input on TextInput (line 387)"
          - "register-submit-button on TouchableOpacity (line 401)"
          - "register-back-button on TouchableOpacity (line 416)"
        code_lines: 12
        runtime_testIDs: 15
        naming_convention: pass
        dynamic_template_literals: correct
        zero_functional_impact: true

      settings_index_tsx:
        testIDs_verified:
          - "settings-sign-out-button on Pressable (line 326)"
        count: 1
        naming_convention: pass
        zero_functional_impact: true

      maestro_01_register:
        structure: valid_yaml
        appId: "com.alouisio.sacramentmeetingplanner"
        evalScript_email: "e2e-{Date.now()}@test.com pattern"
        element_targeting: "All via testID (id:)"
        assertions: "pt-BR text strings verified against i18n"
        timeout: "extendedWaitUntil 15000ms for registration"
        tab_assertions: ["Inicio", "Agendas", "Discursos", "Configuracoes"]
        i18n_verification: |
          All pt-BR strings match src/i18n/locales/pt-BR.json:
          - "Entrar" = auth.login
          - "Criar Conta" = auth.createAccountButton
          - Tab labels match tabs.home/agenda/speeches/settings

      maestro_02_login_logout:
        structure: valid_yaml
        parts:
          - "Part A: Login with output.TEST_EMAIL/TEST_PASSWORD"
          - "Part B: Logout via Configuracoes tab, sign-out button, Confirmar alert"
          - "Part C: Invalid login with error assertion"
        element_targeting: "All via testID (id:)"
        native_alert_handling: "tapOn 'Confirmar' for iOS Alert"
        i18n_verification: |
          All pt-BR strings match src/i18n/locales/pt-BR.json:
          - "Inicio" = tabs.home
          - "Configuracoes" = tabs.settings
          - "Confirmar" = common.confirm
          - "E-mail ou senha incorretos" = auth.loginFailed

      readme:
        sections_present: 7
        sections: ["Prerequisites", "Building the app", "Running tests",
                   "Test data management", "testID convention", "Troubleshooting"]
        accuracy: "Run commands correct, conventions documented, troubleshooting covers key scenarios"

    # =========================================================================
    # SECURITY REVIEW
    # =========================================================================

    security:
      notes: |
        - No secrets stored in Maestro flow files. Credentials generated at runtime
          via evalScript (Date.now() email pattern)
        - Test password "Test123456" is acceptable for test wards with no real data
        - Production Supabase used but RLS ensures complete data isolation
        - Test accounts identifiable by e2e-* email pattern for cleanup

    # =========================================================================
    # DEPLOY IDENTIFICATION
    # =========================================================================

    pending_deploys: []
    deploy_notes: |
      No deploys required. Changes are:
      - testID props on 3 source files (zero functional impact, client-side only)
      - 2 Maestro YAML flow files (outside app bundle, in e2e/ directory)
      - 1 README documentation file (outside app bundle)
      testID props will be included in the next regular EAS Build. No database
      migrations, Edge Function changes, or infrastructure updates needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - ".devteam/project-status.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_M017.yaml"
        - "docs/plans/PLAN_P017.yaml"
        - "docs/qa/QA_PLAN_017.yaml"
        - "docs/specs/SPEC_F017.yaml"
      notes: |
        All code changes for CR-284 are committed across 10 commits
        (e322ac6..b5b2d5b). The 7 pending documentation files are project
        artifacts (specs, arch, plans, QA) that are committed by the
        Orchestrator at the end of the phase, not by the Coder.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-284 (E2E Testing with Maestro Phase 1) implementation is correct and
      complete. Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).
      22 testID props added to 3 source files (6 login, 15 register, 1 settings)
      following the {screen}-{element}-{type} kebab-case convention (ADR-052).
      Dynamic testIDs use template literals for role/language radio buttons.
      Two Maestro flows created: 01-register (new ward registration end-to-end)
      and 02-login-logout (login, logout via native Alert, invalid login error).
      All pt-BR strings in Maestro assertions verified against i18n locale file.
      e2e/README.md covers all 7 required sections. Zero functional impact on
      the app. No deploys required. No P0, P1, or P2 issues found.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 6
      total_commits: 10

  - id: REV-020
    scope: "CR-284 — E2E Testing with Maestro Phase 2: Agenda + Presentation Flows"
    date: "2026-03-09"
    reviewer: reviewer-agent
    phase: 18
    feature: F018_E2E
    iteration: 1

    commits:
      - sha: 12ec5f2
        message: "feat(home): add testID props to 2 Home tab elements (CR-284)"
      - sha: 79ba4f3
        message: "feat(agenda): add dynamic testID props to 2 AgendaSundayCard elements (CR-284)"
      - sha: cc8551b
        message: "feat(AgendaForm): add testID to SelectorField + 7 callers + HymnSelectorModal (CR-284)"
      - sha: 201c992
        message: "feat(ActorSelector): add testID to 5 interactive elements (CR-284)"
      - sha: 8381858
        message: "feat(presentation): add testID to 5 Presentation Mode elements (CR-284)"
      - sha: 2674d81
        message: "feat(e2e): create Maestro flow 03-home-presentation for Presentation Mode (CR-284)"
      - sha: d981798
        message: "feat(e2e): create Maestro flow 04-agenda for Agenda tab assign flow (CR-284)"
      - sha: 0dc7019
        message: "chore(plans): mark P018 E2E testing phase 2 as implemented (CR-284)"
      - sha: 22950cf
        message: "chore(code-ledger): add CR-284 E2E Phase 2 commits to CODE_LEDGER v18"
      - sha: ad56beb
        message: "test(e2e): add tests for Phase 2 E2E testing infrastructure CR-284"
      - sha: 9147855
        message: "docs(tests): update TEST_CONSOLIDATED v23 for CR-284 Phase 2 E2E testing"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        notes: |
          SPEC_F018 fully covers the user request for E2E testing Phase 2:
          - testID props on index.tsx (2 elements), agenda.tsx (2 dynamic),
            AgendaForm.tsx (9 elements: 7 SelectorField + 2 HymnSelectorModal),
            ActorSelector.tsx (5 elements), presentation.tsx (5 elements)
          - Two new Maestro flow files: 03-home-presentation and 04-agenda
          - 8 sub-features (SF1-SF8) with 18 acceptance criteria and 11 edge cases
          - All 5 open questions resolved with user-confirmed defaults
          - All 6 assumptions confirmed
          - Zero inconsistencies with SPEC_CONSOLIDATED

      arch_vs_spec:
        status: pass
        notes: |
          ARCH_M018 correctly maps all SPEC components:
          - 5 TestID Layer components for the 5 modified source files
          - 2 Maestro Flow components with correct dependencies
          - Contracts define all testIDs with correct component types
          - ADR-054 (SelectorField testID passthrough) correctly specifies
            the prop interface change and Pressable forwarding
          - ADR-055 (dynamic testID with ISO date suffix) aligns with SPEC
            BR_E2E_P2_002 and DD_E2E_P2_001
          - File changes summary matches SPEC files_to_modify exactly

      plan_vs_arch:
        status: pass
        notes: |
          PLAN_P018 has 8 steps (S018-01 through S018-08) that follow ARCH:
          - Steps 1-5: testID additions (parallel, no dependencies)
          - Step 6: 03-home-presentation flow (depends on S018-01, S018-05)
          - Step 7: 04-agenda flow (depends on S018-02, S018-03, S018-04)
          - Step 8: Final verification (depends on all)
          Dependencies match ARCH component dependencies exactly.
          Coverage matrix maps all ACs and edge cases to steps.

      code_vs_plan:
        status: pass
        notes: |
          Code implements exactly what PLAN specifies:
          - index.tsx: 2 testID props (home-start-meeting-button at line 134,
            home-preview-pencil-button at line 235) — matches S018-01
          - agenda.tsx: 2 dynamic testIDs using template literals
            (`agenda-card-${date}` at line 400, `agenda-play-${date}` at
            line 540) — matches S018-02
          - AgendaForm.tsx: SelectorField interface extended with testID?: string,
            forwarded to Pressable via testID={testID}. 7 caller sites correctly
            pass semantic testIDs. HymnSelectorModal has hymn-selector-search-input
            on SearchInput and hymn-selector-close-button on Pressable — matches S018-03
          - ActorSelector.tsx: 5 testIDs (search-input, close-button, add-button,
            add-input, add-confirm) — matches S018-04
          - presentation.tsx: 5 testIDs (screen on SafeAreaView, edit-button,
            font-toggle-button, font-toggle-text, close-button) — matches S018-05
          - 03-home-presentation.yaml: 6 parts (Login, Start Presentation, Verify,
            Font Toggle, Accordion, Close) — matches S018-06
          - 04-agenda.yaml: 6 parts (Login, Navigate, Expand, Assign Actor,
            Assign Hymn, Collapse) with evalScript for dynamic date — matches S018-07
          - CODE_LEDGER updated with 8 commits for Phase 2

      chain_intact: true

    # =========================================================================
    # CODE REVIEW FINDINGS
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found.

    code_review_details:
      index_tsx:
        testIDs_verified:
          - "home-start-meeting-button on Pressable (Start Meeting CTA)"
          - "home-preview-pencil-button on Pressable (agenda preview pencil)"
        count: 2
        naming_convention: pass
        zero_functional_impact: true

      agenda_tsx:
        testIDs_verified:
          - "`agenda-card-${date}` on Pressable (card header, dynamic)"
          - "`agenda-play-${date}` on Pressable (play button, dynamic)"
        count: 2
        type: dynamic_template_literals
        naming_convention: pass
        zero_functional_impact: true

      AgendaForm_tsx:
        SelectorField_interface:
          added_prop: "testID?: string"
          forwarded_to: "outer Pressable via testID={testID}"
          pattern: "ADR-054 prop passthrough"
        testIDs_verified:
          - "agenda-presiding-selector on SelectorField caller"
          - "agenda-conducting-selector on SelectorField caller"
          - "agenda-pianist-selector on SelectorField caller"
          - "agenda-conductor-selector on SelectorField caller"
          - "agenda-opening-hymn-selector on SelectorField caller"
          - "agenda-sacrament-hymn-selector on SelectorField caller"
          - "agenda-closing-hymn-selector on SelectorField caller"
          - "hymn-selector-search-input on SearchInput (HymnSelectorModal)"
          - "hymn-selector-close-button on Pressable (HymnSelectorModal)"
        count: 9
        naming_convention: pass
        zero_functional_impact: true

      ActorSelector_tsx:
        testIDs_verified:
          - "actor-selector-search-input on SearchInput"
          - "actor-selector-close-button on Pressable"
          - "actor-selector-add-button on Pressable (dashed-border add)"
          - "actor-selector-add-input on TextInput (name input)"
          - "actor-selector-add-confirm on Pressable (confirm creation)"
        count: 5
        naming_convention: pass
        zero_functional_impact: true

      presentation_tsx:
        testIDs_verified:
          - "presentation-screen on SafeAreaView (container)"
          - "presentation-edit-button on Pressable (pencil)"
          - "presentation-font-toggle-button on Pressable (font toggle)"
          - "presentation-font-toggle-text on Text (Aa/AA label)"
          - "presentation-close-button on Pressable (close X)"
        count: 5
        naming_convention: pass
        zero_functional_impact: true

      maestro_03_home_presentation:
        structure: valid_yaml
        appId: "com.alouisio.sacramentmeetingplanner"
        parts:
          - "Part A: Login with output.TEST_EMAIL/TEST_PASSWORD"
          - "Part B: Tap home-start-meeting-button, wait for presentation-screen"
          - "Part C: Assert 'Reuniao Sacramental' title, 'Presidindo' label, font toggle"
          - "Part D: Tap presentation-font-toggle-button twice (toggle and back)"
          - "Part E: Tap 'Ultimo Discurso' collapsed card by text (EC_E2E_P2_006)"
          - "Part F: Tap presentation-close-button, assert Home tab visible"
        element_targeting: "testID for interactive elements, text for assertions"
        i18n_verification: |
          All pt-BR strings match src/i18n/locales/pt-BR.json:
          - "Inicio" = tabs.home
          - "Reuniao Sacramental" = presentation.title
          - "Presidindo" = agenda.presiding
          - "Ultimo Discurso" = speeches.lastSpeech
        edge_case_handling:
          EC_E2E_P2_006: "Accordion tap by text 'Ultimo Discurso'"
          EC_E2E_P2_008: "Flow uses field labels, not populated values"
          EC_E2E_P2_009: "Flow order independent (labels not values)"

      maestro_04_agenda:
        structure: valid_yaml
        appId: "com.alouisio.sacramentmeetingplanner"
        parts:
          - "Part A: Login with output.TEST_EMAIL/TEST_PASSWORD"
          - "Part B: Navigate to Agendas tab by text"
          - "Part C: evalScript computes NEXT_SUNDAY, tap agenda-card-{date}, wait 15s for lazy creation"
          - "Part D: scrollUntilVisible, tap agenda-presiding-selector, ActorSelector inline create 'E2E Presidente'"
          - "Part E: scrollUntilVisible, tap agenda-opening-hymn-selector, search '2', tap first result"
          - "Part F: scrollUntilVisible UP, tap card header to collapse, assertNotVisible"
        element_targeting: "testID for interactive elements, text for assertions"
        dynamic_date: "evalScript computes NEXT_SUNDAY via getDay() + toISOString()"
        i18n_verification: |
          All pt-BR strings match src/i18n/locales/pt-BR.json:
          - "Inicio" = tabs.home
          - "Agendas" = tabs.agenda
          - "Boas-Vindas, Anuncios e Reconhecimentos" = agenda.sectionWelcome
        edge_case_handling:
          EC_E2E_P2_001: "Dynamic date via evalScript"
          EC_E2E_P2_003: "15s timeout for lazy agenda creation"
          EC_E2E_P2_004: "extendedWaitUntil 5s for ActorSelector"
          EC_E2E_P2_005: "waitForAnimationToEnd for hymn search"
          EC_E2E_P2_007: "scrollUntilVisible before tapping fields"
          EC_E2E_P2_010: "scrollUntilVisible for FlatList card"

    # =========================================================================
    # TESTS
    # =========================================================================

    tests:
      file: src/__tests__/f018-e2e-phase2.test.ts
      total: 95
      status: all_passing
      coverage:
        - "AC-SF1-01, AC-SF1-02: Home tab testIDs (3 tests)"
        - "AC-SF2-01, AC-SF2-02: Agenda dynamic testIDs (3 tests)"
        - "AC-SF3-01, AC-SF3-02: AgendaForm SelectorField testIDs (7 tests)"
        - "ADR-054: SelectorField testID interface (3 tests)"
        - "AC-SF4-01, AC-SF4-02: HymnSelectorModal testIDs (2 tests)"
        - "AC-SF5-01, AC-SF5-02: ActorSelector testIDs (6 tests)"
        - "AC-SF6-01, AC-SF6-02, AC-SF6-03: Presentation testIDs (6 tests)"
        - "AC-SF7-01, AC-SF7-02, AC-SF7-03: 03-home-presentation flow (16 tests)"
        - "AC-SF8-01, AC-SF8-02, AC-SF8-03: 04-agenda flow (21 tests)"
        - "Cross-validation: flow testIDs exist in source (3 tests)"
        - "ADR-052: naming convention (4 tests)"
        - "ADR-053: flow file naming (4 tests)"
        - "pt-BR i18n strings (7 tests)"
        - "EC_E2E_P2_011: SearchInput passthrough (2 tests)"
        - "Total testID count verification (1 test)"

    # =========================================================================
    # SECURITY REVIEW
    # =========================================================================

    security:
      notes: |
        Same as Phase 1:
        - No secrets stored in Maestro flow files. Credentials from 01-register output.
        - Actor data ("E2E Presidente") scoped to test ward via RLS isolation.
        - Hymn assignment updates existing agendas table with RLS protection.
        - Test accounts identifiable by e2e-* email pattern for cleanup.

    # =========================================================================
    # DEPLOY IDENTIFICATION
    # =========================================================================

    pending_deploys: []
    deploy_notes: |
      No deploys required. Changes are:
      - testID props on 5 source files (zero functional impact, client-side only)
      - 2 Maestro YAML flow files (outside app bundle, in e2e/ directory)
      testID props will be included in the next regular EAS Build. No database
      migrations, Edge Function changes, or infrastructure updates needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - "docs/specs/SPEC_F018.yaml"
        - "docs/arch/ARCH_M018.yaml"
        - "docs/plans/PLAN_P018.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"
      notes: |
        All code changes for CR-284 Phase 2 are committed across 11 commits
        (12ec5f2..9147855). The 4 pending documentation files are project
        artifacts that are committed by the Orchestrator at the end of the
        phase, not by the Coder.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-284 (E2E Testing with Maestro Phase 2) implementation is correct and
      complete. Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).
      21 testID props added across 5 source files (2 Home, 2 Agenda dynamic,
      9 AgendaForm with SelectorField interface change, 5 ActorSelector,
      5 Presentation) following the {screen}-{element}-{type} kebab-case
      convention (ADR-052). SelectorField testID prop passthrough implemented
      per ADR-054. Dynamic testIDs for agenda cards use ISO date suffix per
      ADR-055. Two Maestro flows created: 03-home-presentation (Home tab ->
      Presentation Mode round-trip with font toggle and accordion verification)
      and 04-agenda (Agenda tab -> expand Sunday -> assign actor inline ->
      assign hymn via search -> collapse). All pt-BR strings verified against
      i18n. 95 tests passing. Zero functional impact. No deploys required.
      No P0, P1, or P2 issues found.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 9
      total_commits: 11

  # ===========================================================================
  # REVIEW 21 — Phase 019 (CR-284): E2E Testing with Maestro Phase 3
  # ===========================================================================
  - id: REV-021
    scope: "CR-284 — E2E Testing with Maestro Phase 3: Speeches + Members Flows"
    date: "2026-03-09"
    reviewer: reviewer-agent
    phase: 19
    feature: F019_E2E
    iteration: 1

    commits:
      - sha: e8fb533
        message: "test(e2e): add testID to 6 SpeechSlot elements (CR-284, S019-01)"
      - sha: cba9f8b
        message: "test(e2e): add testID to 3 MemberSelectorModal elements (CR-284, S019-02)"
      - sha: d45ad17
        message: "test(e2e): add testID to 3 StatusChangeModal elements (CR-284, S019-03)"
      - sha: f80e18c
        message: "test(e2e): add testID to 2 TopicSelectorModal elements (CR-284, S019-04)"
      - sha: 49b624a
        message: "test(e2e): add testID prop to SundayCard + pass from speeches.tsx (CR-284, S019-05)"
      - sha: 3ddf488
        message: "test(e2e): add testID to 8 Members screen elements + SettingsItem prop (CR-284, S019-06)"
      - sha: 26ef184
        message: "test(e2e): add testID to 2 SwipeableCard action buttons (CR-284, S019-07)"
      - sha: 90ba6c5
        message: "test(e2e): create Maestro flow 05-speeches.yaml (CR-284, S019-09)"
      - sha: 2adadca
        message: "test(e2e): create Maestro flow 06-members.yaml (CR-284, S019-10)"
      - sha: beed56f
        message: "test(e2e): update flow count assertion for Phase 3 flows (CR-284, S019-11)"
      - sha: d30ec26
        message: "chore(code-ledger): add CR-284 Phase 3 E2E testing commits to CODE_LEDGER v19"
      - sha: 97d7e32
        message: "test(e2e): add tests for F019_E2E Phase 3 Speeches + Members"
      - sha: 3dc6a2f
        message: "docs(tests): update TEST_CONSOLIDATED v24 for F019_E2E Phase 3"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request:
        status: pass
        notes: |
          SPEC_F019 fully covers the user request for E2E testing Phase 3:
          - testID props on SpeechSlot (6 dynamic), MemberSelectorModal (3),
            StatusChangeModal (3), TopicSelectorModal (2), SundayCard (testID prop
            + header), Members screen (8), SwipeableCard (2), SettingsItem (1)
          - Two new Maestro flows: 05-speeches and 06-members
          - 10 sub-features (SF1-SF10) with 22 acceptance criteria and 12 edge cases
          - 6 open questions resolved, 7 assumptions confirmed, 0 inconsistencies
          - Continues Phase 1+2 conventions (ADR-052, ADR-053)

      arch_vs_spec:
        status: pass
        notes: |
          ARCH_M019 correctly maps all SPEC components:
          - 8 TestID Layer components for the 9 modified source files
          - 2 Maestro Flow components with correct dependencies
          - Contracts define all testIDs with correct component types
          - ADR-056 (dynamic testID with position suffix) correctly specifies
            the SpeechSlot testID scheme using the existing position prop
          - ADR-057 (self-contained flow 05 with member pre-creation) ensures
            flow independence — each flow creates its own test data
          - Data model confirms no DB schema changes needed
          - File changes summary matches SPEC files_to_modify exactly

      plan_vs_arch:
        status: pass
        notes: |
          PLAN_P019 has 11 steps (S019-01 through S019-11) that follow ARCH:
          - Steps 1-7: testID additions (parallel group A)
          - Step 8: ADR documentation (parallel with group A)
          - Step 9: 05-speeches flow (depends on S019-01..06)
          - Step 10: 06-members flow (depends on S019-06, S019-07)
          - Step 11: Final verification (depends on all)
          Dependencies match ARCH component dependencies exactly.
          Coverage matrix maps all 22 ACs and 12 ECs to steps.
          Validation gate confirms all plan gates passed.

      code_vs_plan:
        status: pass
        notes: |
          Code implements exactly what PLAN specifies:
          - S019-01: SpeechSlot.tsx has 6 testID props (5 dynamic with position,
            1 static for speech-slot-2-toggle) — verified in source
          - S019-02: MemberSelectorModal.tsx has 3 testID props (search, close,
            dynamic member item) — verified in source
          - S019-03: StatusChangeModal.tsx has 3 testID props (current, close,
            dynamic status option) — verified in source
          - S019-04: TopicSelectorModal.tsx has 2 testID props (search, close)
            — verified in source
          - S019-05: SundayCard.tsx has testID prop in interface, forwarded to
            root View and header Pressable (derived via replace). speeches.tsx
            passes testID with date suffix — verified in both source files
          - S019-06: members.tsx has 8 testID props. settings/index.tsx has
            SettingsItem testID prop added and forwarded — verified in source
          - S019-07: SwipeableCard.tsx has 2 static testID props — verified
          - S019-09: 05-speeches.yaml has 8 parts (A-H) matching PLAN exactly
          - S019-10: 06-members.yaml has 6 parts (A-F) matching PLAN exactly
          - S019-11: Flow count assertion updated in test file
          - CODE_LEDGER v19 has all 13 commits for this phase

      chain_intact: true

    # =========================================================================
    # CODE REVIEW FINDINGS
    # =========================================================================

    issues: []
    # No P0, P1, or P2 issues found.

    code_review_details:

      speechslot_tsx:
        file: src/components/SpeechSlot.tsx
        testIDs_verified:
          - "speech-slot-${position}-speaker-button on Pressable (line 240)"
          - "speech-slot-${position}-topic-button on Pressable (line 279)"
          - "speech-slot-${position}-status-button on Pressable (line 195)"
          - "speech-slot-${position}-remove-button on Pressable (line 262)"
          - "speech-slot-${position}-clear-topic-button on Pressable (line 300)"
          - "speech-slot-2-toggle on Switch (line 181)"
        count: 6
        dynamic_count: 5
        static_count: 1
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          All 5 dynamic testIDs use template literals with the existing position
          prop. The Switch testID is static because it only appears for position 2.
          No new imports, no logic changes. ADR-056 correctly applied.

      memberselectormodal_tsx:
        file: src/components/MemberSelectorModal.tsx
        testIDs_verified:
          - "member-selector-search-input on SearchInput (line 72)"
          - "member-selector-close-button on Pressable (line 74)"
          - "member-selector-item-${item.id} on Pressable (line 92)"
        count: 3
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          SearchInput passes testID to inner TextInput via rest spread (confirmed
          in Phase 2, A-3). Dynamic member item testID uses item.id (UUID).

      statuschangemodal_tsx:
        file: src/components/StatusChangeModal.tsx
        testIDs_verified:
          - "status-modal-current on View (line 89)"
          - "status-modal-option-${item} on Pressable (line 103)"
          - "status-modal-close-button on Pressable (line 122)"
        count: 3
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          Dynamic status option testID uses the FlatList item variable which
          is the status string value (assigned_not_invited, etc.). Correct
          mapping to BR_E2E_P3_003.

      topicselectormodal_tsx:
        file: src/components/TopicSelectorModal.tsx
        testIDs_verified:
          - "topic-selector-search-input on SearchInput (line 86)"
          - "topic-selector-close-button on Pressable (line 88)"
        count: 2
        naming_convention: pass
        zero_functional_impact: true

      sundaycard_tsx:
        file: src/components/SundayCard.tsx
        testIDs_verified:
          - "testID prop added to SundayCardProps interface (line 68)"
          - "testID={testID} on root View (line 457)"
          - "testID={headerTestID} on header Pressable (line 466)"
          - "headerTestID derived via testID.replace('card', 'card-header') (line 447)"
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          testID prop is optional. Callers that do not pass testID (e.g., agenda.tsx)
          get testID=undefined (no impact). The headerTestID derivation uses
          string replace which correctly transforms "speeches-card-2026-03-15"
          to "speeches-card-header-2026-03-15". This pattern is clean and
          avoids requiring a separate prop for the header.

      speeches_tsx:
        file: src/app/(tabs)/speeches.tsx
        testIDs_verified:
          - "testID={`speeches-card-${item.date}`} on SundayCard (line 447)"
        count: 1
        naming_convention: pass
        zero_functional_impact: true

      members_tsx:
        file: src/app/(tabs)/settings/members.tsx
        testIDs_verified:
          - "members-back-button on Pressable (line 571)"
          - "members-add-button on Pressable (line 583)"
          - "members-search-input on SearchInput (line 603)"
          - "members-editor-fullname-input on TextInput (line 98)"
          - "members-editor-informal-input on TextInput (line 108)"
          - "members-editor-phone-input on TextInput (line 128)"
          - "members-editor-cancel-button on Pressable (line 138)"
          - "members-editor-save-button on Pressable (line 148)"
        count: 8
        naming_convention: pass
        zero_functional_impact: true

      swipeablecard_tsx:
        file: src/components/SwipeableCard.tsx
        testIDs_verified:
          - "swipeable-edit-button on Pressable (line 159)"
          - "swipeable-delete-button on Pressable (line 170)"
        count: 2
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          Static testIDs are correct because SwipeableCard uses single-reveal
          management (activeId in parent). Only one set of action buttons
          is visible at any time.

      settings_index_tsx:
        file: src/app/(tabs)/settings/index.tsx
        testIDs_verified:
          - "testID?: string added to SettingsItemProps (line 28)"
          - "testID={testID} on SettingsItem Pressable (line 37)"
          - "testID='settings-members-button' on members call site (line 224)"
        naming_convention: pass
        zero_functional_impact: true
        notes: |
          SettingsItem is a private component in settings/index.tsx. The testID
          prop is forwarded to the outer Pressable. Only the members item
          passes a testID value. The sign-out button already had a testID
          from Phase 1 (settings-sign-out-button).

      maestro_05_speeches:
        file: e2e/maestro/05-speeches.yaml
        structure: valid_yaml
        appId: "com.alouisio.sacramentmeetingplanner"
        parts:
          - "Part A: Login with output.TEST_EMAIL/TEST_PASSWORD"
          - "Part B: Navigate to Settings -> Members -> Add 'E2E Orador' -> Save -> Back"
          - "Part C: Navigate to Speeches tab by text 'Discursos'"
          - "Part D: evalScript NEXT_SUNDAY, tap speeches-card-header-{date}, wait 15s"
          - "Part E: Tap speech-slot-1-speaker-button, select 'E2E Orador' from modal"
          - "Part F: Tap speech-slot-1-status-button, select assigned_confirmed"
          - "Part G: Open/close TopicSelectorModal without selection"
          - "Part H: Collapse card, assert speech slots hidden"
        element_targeting: "testID for interactive elements, text for assertions"
        dynamic_date: "evalScript computes NEXT_SUNDAY via getDay() + toISOString()"
        timeouts:
          - "10000ms for login wait"
          - "5000ms for settings/members/speeches navigation waits"
          - "15000ms for lazy speech creation (extendedWaitUntil)"
        i18n_verification: |
          All pt-BR strings verified against src/i18n/locales/pt-BR.json:
          - "Início" = tabs.home
          - "Configurações" = settings.title (tapOn text for Settings tab)
          - "Discursos" = speeches.title (tapOn text for Speeches tab)
          - "E2E Orador" = test data (not i18n)
        adr_057_applied: true
        notes: |
          Flow 05 is self-contained per ADR-057: creates its own member "E2E Orador"
          in Part B before navigating to Speeches in Part C. This ensures flow
          independence from flow 06. Part G correctly tests TopicSelectorModal
          open/close without topic selection (per OQ-2 resolution, ward may have
          no configured topics).

      maestro_06_members:
        file: e2e/maestro/06-members.yaml
        structure: valid_yaml
        appId: "com.alouisio.sacramentmeetingplanner"
        parts:
          - "Part A: Login with output.TEST_EMAIL/TEST_PASSWORD"
          - "Part B: Navigate to Settings -> Members via settings-members-button"
          - "Part C: Add 'E2E Membro Teste' with informal name 'Teste'"
          - "Part D: Swipe left on member, tap swipeable-edit-button, edit to 'E2E Membro Editado'"
          - "Part E: Swipe left, tap swipeable-delete-button, confirm via 'Excluir' Alert"
          - "Part F: Navigate back to Settings via members-back-button"
        element_targeting: "testID for interactive elements, text for swipe targets"
        swipe_handling: |
          Uses Maestro swipe command with direction: LEFT on text targets.
          extendedWaitUntil after each swipe waits for action buttons to appear
          (3000ms timeout). Two swipes in flow: one for edit, one for delete.
        native_alert_handling: "tapOn 'Excluir' for iOS Alert delete confirmation"
        i18n_verification: |
          All pt-BR strings verified:
          - "Início" = tabs.home
          - "Configurações" = settings.title
          - "Excluir" = common.delete (native Alert button)
          - "E2E Membro Teste", "E2E Membro Editado" = test data
        notes: |
          Flow 06 uses unique member names ("E2E Membro Teste" / "E2E Membro Editado")
          that do not conflict with flow 05's "E2E Orador" (EC_E2E_P3_010).
          clearText on the fullname input before retyping ensures the edit test
          is clean. The flow handles the full CRUD lifecycle: create -> edit -> delete.

    # =========================================================================
    # TESTS
    # =========================================================================

    tests:
      file: src/__tests__/f019-e2e-phase3.test.ts
      total: 123
      status: all_passing
      coverage:
        - "AC-SF1-01, AC-SF1-02, AC-SF1-03: SpeechSlot 6 dynamic testIDs (9 tests)"
        - "AC-SF2-01, AC-SF2-02: MemberSelectorModal 3 testIDs (4 tests)"
        - "AC-SF3-01, AC-SF3-02: StatusChangeModal 3 testIDs (4 tests)"
        - "AC-SF4-01, AC-SF4-02: TopicSelectorModal 2 testIDs (3 tests)"
        - "AC-SF5-01, AC-SF5-02: SundayCard testID prop + forwarding (5 tests)"
        - "AC-SF6-01, AC-SF6-02, AC-SF6-03: Members screen 8 testIDs (9 tests)"
        - "AC-SF7-01, AC-SF7-02: SwipeableCard 2 testIDs (3 tests)"
        - "AC-SF8-01: SettingsItem testID prop (3 tests)"
        - "AC-SF9-01 to AC-SF9-04: 05-speeches flow structure (28 tests)"
        - "AC-SF10-01, AC-SF10-02, AC-SF10-03: 06-members flow structure (22 tests)"
        - "Cross-validation: flow testIDs exist in source (5 tests)"
        - "ADR-052 + ADR-056: naming convention (4 tests)"
        - "ADR-053: flow file naming (4 tests)"
        - "pt-BR i18n strings (5 tests)"
        - "Total testID count verification (1 test)"
        - "EC_E2E_P3_009: Conference Sunday handling (2 tests)"
        - "EC_E2E_P3_008: Keyboard covering (1 test)"

      full_suite:
        total: 2007
        passing: 2007
        failing: 0
        test_files: 67

    # =========================================================================
    # SECURITY REVIEW
    # =========================================================================

    security:
      notes: |
        Same as Phase 1 and 2:
        - No secrets stored in Maestro flow files. Credentials from 01-register output.
        - Member data ("E2E Orador", "E2E Membro Teste") scoped to test ward via RLS.
        - Speech assignments and status changes scoped to test ward.
        - Test accounts identifiable by e2e-* email pattern for cleanup.

    # =========================================================================
    # DEPLOY IDENTIFICATION
    # =========================================================================

    pending_deploys: []
    deploy_notes: |
      No deploys required. Changes are:
      - testID props on 9 source files (zero functional impact, client-side only)
      - 2 Maestro YAML flow files (outside app bundle, in e2e/ directory)
      testID props will be included in the next regular EAS Build. No database
      migrations, Edge Function changes, or infrastructure updates needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - "docs/specs/SPEC_F019.yaml"
        - "docs/arch/ARCH_M019.yaml"
        - "docs/plans/PLAN_P019.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"
      notes: |
        All code changes for CR-284 Phase 3 are committed across 13 commits
        (e8fb533..3dc6a2f). The 4 pending documentation files are project
        artifacts that are committed by the Orchestrator at the end of the
        phase, not by the Coder.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-284 (E2E Testing with Maestro Phase 3) implementation is correct and
      complete. Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).
      27 testID props added across 9 source files: SpeechSlot (6 dynamic with
      position suffix per ADR-056), MemberSelectorModal (3), StatusChangeModal (3),
      TopicSelectorModal (2), SundayCard (testID prop + headerTestID derivation),
      speeches.tsx (1 dynamic date), Members screen (8), SwipeableCard (2 static),
      SettingsItem (1 + prop forwarding). All follow the {screen}-{element}-{type}
      kebab-case convention (ADR-052). Two Maestro flows created: 05-speeches
      (self-contained per ADR-057 — creates member before speech assignment,
      tests speaker assignment, status change to assigned_confirmed, and topic
      modal open/close) and 06-members (full CRUD: add -> swipe edit -> swipe
      delete with native Alert confirmation). All pt-BR strings verified against
      i18n locale file. 123 new tests, 2007 total passing. Zero functional impact.
      No deploys required. No P0, P1, or P2 issues found.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 13
      total_commits: 13
      tests_total: 2007
      tests_passing: 2007
      test_files: 67

  # ===========================================================================
  # REV-022: CR-284 Phase 4 — E2E Settings + Invite + Cleanup
  # ===========================================================================

  - id: REV-022
    scope: "CR-284 Phase 4 — E2E Testing: Settings + Invite + Cleanup (F020_E2E)"
    date: "2026-03-09"
    reviewer: reviewer-agent
    phase: 20
    iteration: 1
    commits:
      - sha: 30758fe
        message: "test(e2e): add testID to ~10 users.tsx invite-related elements (CR-284, S020-01)"
      - sha: 98c94b5
        message: "test(e2e): add testID to 5 invite/[token].tsx elements (CR-284, S020-02)"
      - sha: 6e3bd1f
        message: "test(e2e): create Maestro flow 07-invite.yaml (CR-284, S020-03)"
      - sha: 127db33
        message: "test(e2e): create Maestro flow 08-settings.yaml (CR-284, S020-04)"
      - sha: e1bd2e9
        message: "test(e2e): create Deno cleanup script for E2E test data (CR-284, S020-05)"
      - sha: a856729
        message: "docs(e2e): update README with Phase 4 flows and cleanup instructions (CR-284, S020-06)"

    # =========================================================================
    # CHAIN VALIDATION
    # =========================================================================

    chain_validation:
      spec_vs_request: pass
      arch_vs_spec: pass
      plan_vs_arch: pass
      code_vs_plan: pass
      chain_intact: true
      notes: |
        SPEC F020 defines 24 ACs and 9 edge cases for E2E Phase 4: two new Maestro
        flows (07-invite, 08-settings), testID props on users.tsx (~10) and
        invite/[token].tsx (5), a Deno cleanup script, and README update.
        ARCH M020 maps all components, contracts (testID names, flow structure,
        cleanup algorithm), and defines ADR-058 (Deno cleanup script).
        PLAN P020 has 8 steps covering all ACs.
        CODE matches PLAN exactly: 6 commits covering 6 files (3 modified, 3 created).
        No breaks in the chain at any level.

    # =========================================================================
    # CODE REVIEW
    # =========================================================================

    code_review:
      files_reviewed:
        - path: src/app/(tabs)/settings/users.tsx
          step: S020-01
          verdict: pass
          notes: |
            10 testID props added to invite-related elements: users-invite-button (header
            Pressable), users-invite-email-input (TextInput), users-invite-role-${role}-radio
            (dynamic template in ROLES.map() generating 3 radios), users-invite-submit-button
            (Pressable), users-invite-success-text (Text), users-invite-link-text (Text),
            users-invite-copy-button (Pressable), users-invite-close-button (Pressable).
            All follow {screen}-{element}-{type} kebab-case convention (ADR-052).
            Zero functional impact — testID is inert on production.

        - path: src/app/(auth)/invite/[token].tsx
          step: S020-02
          verdict: pass
          notes: |
            5 unique testID props added (6 occurrences): invite-fullname-input (TextInput),
            invite-password-input (TextInput), invite-confirm-password-input (TextInput),
            invite-submit-button (TouchableOpacity), invite-error-text (View, 2 locations:
            pre-form error at line 162 and in-form error at line 197 — only one visible
            at a time, same testID is correct). For future test coverage per SPEC OQ-1
            Option B. Zero functional impact.

        - path: e2e/maestro/07-invite.yaml
          step: S020-03
          verdict: pass
          notes: |
            7 parts (A-G): login with 01-register credentials, navigate Settings >
            "Usuarios do App", open invite modal, generate unique email via evalScript
            (e2e-invited-{timestamp}@test.com), select secretary role, submit, verify
            success text + deep link URL visible, close modal, navigate back, logout.
            All testIDs match source. Timeouts are appropriate (10s for Users loading
            EC-008, 15s for Edge Function cold start). Uses pt-BR strings matching
            i18n locale file (Inicio, Configuracoes, Usuarios do App, Voltar, Confirmar).
            Does NOT test invite registration (SPEC OQ-1 Option B).

        - path: e2e/maestro/08-settings.yaml
          step: S020-04
          verdict: pass
          notes: |
            7 parts (A-G): login, navigate to Settings, theme changes (Claro -> Escuro),
            return to Settings, language change to en-US (verify "Settings", "Light/Dark
            Theme", "App Language"), language change back to pt-BR (verify "Configuracoes",
            "Tema Claro/Escuro", "Idioma do App"), restore theme to Automatico, logout.
            Full round-trip language test per SPEC OQ-4 Option A. Text targeting for
            theme options per Assumption A-4. All i18n strings verified against both
            pt-BR.json and en-US.json locale files. Language label "Portugues (Brasil)"
            matches LANGUAGE_LABELS constant (no accent, correct).

        - path: e2e/scripts/cleanup.ts
          step: S020-05
          verdict: pass
          notes: |
            Deno script per SPEC OQ-5 Option A and ADR-058. Imports createClient from
            esm.sh CDN. Reads SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from Deno.env.
            Paginated listUsers loop (perPage: 50, same pattern as send-reset-email
            ADR-032). Filters by /^e2e-.*@test\.com$/ regex. Handles no-users case with
            "No test users found" + Deno.exit(0) (EC-005). Per-user try/catch for partial
            state (EC-006). Deletes ward first (CASCADE) then auth user. Prints per-user
            log and summary. Includes comment documenting env vars and warning not to
            commit them. Script has correct env var validation with helpful usage message.

        - path: e2e/README.md
          step: S020-06
          verdict: pass
          notes: |
            Updated with all 8 flows in listing table. Added cleanup script section with
            deno run command and env var documentation. Troubleshooting tips for 07-invite
            (cold start, slow loading) and 08-settings (language not reflecting, theme
            text targeting). Documents running all 8 flows via "maestro test e2e/maestro/".
            Updated "Run all 8 flows" from previous "6 flows". Clean formatting.

    issues: []

    # =========================================================================
    # TEST REVIEW
    # =========================================================================

    test_review:
      new_test_file: src/__tests__/f020-e2e-phase4.test.ts
      new_tests: 128
      coverage:
        - "AC-020: users.tsx 10 invite testIDs (10 tests)"
        - "AC-021: invite/[token].tsx 5 unique testIDs (7 tests)"
        - "AC-001 to AC-006: 07-invite flow structure (20 tests)"
        - "AC-007 to AC-019: 08-settings flow structure (23 tests)"
        - "Cross-validation: flow testIDs exist in source (2 tests)"
        - "AC-022: Cleanup script structure (13 tests)"
        - "AC-023: README documentation (10 tests)"
        - "AC-024: All 8 flow files exist (10 tests)"
        - "ADR-052: testID naming convention (3 tests)"
        - "ADR-053: flow file naming (2 tests)"
        - "i18n strings (pt-BR + en-US) for Phase 4 assertions (11 tests)"
        - "Deno compatibility checks (5 tests)"
        - "EC-002: Network error handling / timeouts (2 tests)"
        - "Total testID count verification (2 tests)"
        - "8 Maestro flow file sequential order verification (2 tests)"

      full_suite:
        total: 2135
        passing: 2135
        failing: 0
        test_files: 69

    # =========================================================================
    # SECURITY REVIEW
    # =========================================================================

    security:
      notes: |
        Same as Phases 1-3:
        - No secrets stored in Maestro flow files. Credentials from 01-register output.
        - Invite email uses e2e-invited-{timestamp}@test.com pattern for easy identification.
        - Cleanup script requires SUPABASE_SERVICE_ROLE_KEY via env var only — NOT committed.
        - Script documents env vars with "DO NOT commit" warning in header comment.
        - No new Edge Functions or database changes. All existing RLS isolation applies.

    # =========================================================================
    # DEPLOY IDENTIFICATION
    # =========================================================================

    pending_deploys: []
    deploy_notes: |
      No deploys required. Changes are:
      - testID props on 2 source files (zero functional impact, client-side only)
      - 2 Maestro YAML flow files (outside app bundle, in e2e/ directory)
      - 1 Deno cleanup script (outside app bundle, in e2e/ directory)
      - 1 README update (documentation only)
      testID props will be included in the next regular EAS Build. No database
      migrations, Edge Function changes, or infrastructure updates needed.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - "docs/specs/SPEC_F020.yaml"
        - "docs/arch/ARCH_M020.yaml"
        - "docs/plans/PLAN_P020.yaml"
        - "docs/qa/QA_PLAN_020.yaml"
        - "docs/specs/SPEC_CONSOLIDATED.yaml"
        - "docs/arch/ARCH_CONSOLIDATED.yaml"
        - ".devteam/project-status.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"
      notes: |
        All code changes for CR-284 Phase 4 are committed across 6 commits
        (30758fe..a856729). The 8 pending documentation files are project
        artifacts that are committed by the Orchestrator at the end of the
        phase, not by the Coder.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-284 (E2E Testing with Maestro Phase 4) implementation is correct and
      complete. Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).
      10 testID props added to users.tsx (invite modal: button, email input,
      3 role radios via dynamic template, submit, success text, link text, copy,
      close). 5 unique testIDs added to invite/[token].tsx (fullname, password,
      confirm password, submit, error text x2 locations) for future tests.
      Two Maestro flows created: 07-invite (creation only per OQ-1 Option B,
      with unique email generation via evalScript and secretary role selection)
      and 08-settings (full theme cycle Claro->Escuro->Automatico + language
      round-trip pt-BR->en-US->pt-BR with string assertions in both languages).
      Deno cleanup script with paginated listUsers, per-user try/catch, ward
      CASCADE deletion, env var validation, and summary output. README updated
      with all 8 flows, cleanup instructions, and Phase 4 troubleshooting.
      128 new tests, 2135 total passing. Zero functional impact. No deploys
      required. No P0, P1, or P2 issues found.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 6
      total_commits: 6
      tests_total: 2135
      tests_passing: 2135
      test_files: 69

  # ===========================================================================
  # REV-023: CR-285 + CR-286 + CR-287 + CR-288 (Phase 021)
  # Topic Library Overhaul
  # ===========================================================================

  - id: REV-023
    scope: "CR-285 + CR-286 + CR-287 + CR-288 - Topic Library Overhaul (Phase 021)"
    date: "2026-03-14"
    reviewer: reviewer-agent
    commits:
      - hash: 357323e
        message: "feat(migration): delete special topics collections (CR-285)"
      - hash: 07c4d58
        message: "feat(migration): add is_default column and seed default ward topics (CR-286)"
      - hash: 734c149
        message: "feat(topics): update WardTopic type, collection order, and hooks (CR-285, CR-286)"
      - hash: f633fdb
        message: "feat(register): remove special topics from knownNames, add default topics (CR-285, CR-286)"
      - hash: 0f9b7c5
        message: "feat(i18n): add customTopics key and update description text (CR-286, CR-287)"
      - hash: 3028b7d
        message: "feat(topics): overhaul topics screen with inline add and default topic protection (CR-286, CR-288)"
      - hash: 23a5801
        message: "test: update existing tests for topic library overhaul (CR-285, CR-286)"
      - hash: 130083b
        message: "test(F021): add 77 behavioral tests for Topic Library Overhaul"

    # =========================================================================
    # CHAIN VALIDATION: SPEC -> ARCH -> PLAN -> CODE
    # =========================================================================

    chain_validation:
      spec_to_arch:
        status: pass
        notes: |
          ARCH_M021 faithfully translates all 4 CRs from SPEC_F021:
          - CR-285: Migration 033 DELETE with CASCADE, FIXED_COLLECTION_ORDER cleanup, knownNames cleanup
          - CR-286: Migration 034 ALTER+INSERT, WardTopic.is_default, useWardTopics sort, section rename, register-first-user default topics
          - CR-287: topics.description i18n update in 3 locales
          - CR-288: Remove SearchInput/search state/filteredTopics/'+' button, add inline add area below list
          All acceptance criteria mapped. ADR-059 and ADR-060 documented.

      arch_to_plan:
        status: pass
        notes: |
          PLAN_P021 breaks ARCH into 9 steps with correct dependencies:
          S021-01 (migration 033) -> S021-02 (migration 034) -> S021-03 (types+hooks) -> S021-04 (register-first-user)
          S021-05 (i18n, independent) -> S021-06 (topics.tsx, depends on S021-03+05)
          S021-07 (tests) -> S021-08 (ADR docs) -> S021-09 (final verification)
          AC coverage matrix is complete (18 ACs across 4 CRs). Edge cases documented.
          Key decision to keep normalizeForSearch per team-lead instruction documented.

      plan_to_code:
        status: pass
        notes: |
          All 9 plan steps implemented across 8 commits. Each commit maps to its plan step.
          Files changed match plan file list exactly. All ACs verifiable in code.

    # =========================================================================
    # CODE REVIEW - PER FILE
    # =========================================================================

    file_reviews:

      - file: supabase/migrations/033_delete_special_topics.sql
        cr: CR-285
        status: pass
        notes: |
          Simple, correct DELETE statement. Targets all 3 language variants.
          CASCADE verified: 001_initial_schema.sql:66 has
          "collection_id UUID NOT NULL REFERENCES general_collections(id) ON DELETE CASCADE"
          for general_topics, and line 79 has the same for ward_collection_config.
          No explicit cleanup of child tables needed. Speeches table has no FK
          to general_collections (topic_title/topic_link/topic_collection are
          snapshot fields) so they are preserved. Correct.

      - file: supabase/migrations/034_add_default_ward_topics.sql
        cr: CR-286
        status: pass
        notes: |
          ALTER TABLE adds is_default BOOLEAN NOT NULL DEFAULT false. Correct.
          3 INSERT blocks (pt-BR, en-US, es-LA) use CROSS JOIN with VALUES
          for topic titles. NOT EXISTS guard prevents duplicates per ward.
          Language filter (w.language = 'pt-BR' etc.) uses correct DB locale codes.
          All inserted rows set is_default=true. Idempotent-safe.

      - file: src/types/database.ts
        cr: CR-286
        status: pass
        notes: |
          WardTopic interface at line 104 correctly includes is_default: boolean
          between link and created_at fields. Matches migration schema.

      - file: src/hooks/useTopics.ts
        cr: "CR-285, CR-286"
        status: pass
        notes: |
          FIXED_COLLECTION_ORDER: 6 entries (2 per language), FSY=0, GP=1.
          Special topics entries removed. Unknown collections get default 3
          via ?? operator in useCollections select callback. Correct.

          useWardTopics(): No search parameter. Sort by is_default DESC then
          title ASC. No select callback. Matches ARCH exactly.

          useActiveTopics: wardTopicLabel uses t('topics.customTopics'). Correct.

          normalizeForSearch: Function kept at line 37 per team-lead instruction.
          It is dead code in this file (no callers after search removal) but
          TopicSelectorModal.tsx has its own copy. Keeping it is harmless.

      - file: supabase/functions/register-first-user/index.ts
        cr: "CR-285, CR-286"
        status: pass
        notes: |
          knownNames: 6 entries, no special topics. Correct.

          Default topic insertion (lines 147-160): Placed AFTER ward creation
          (line 137) and BEFORE user creation (line 163). Uses wardLanguage
          with ?? fallback to en-US. Inserts with is_default: true. Correct.

          If user creation fails (line 177), ward is deleted (line 179) which
          will CASCADE delete the ward_topics too (ward_topics has FK to wards).
          No error handling on the topic insert itself - acceptable since
          ward_topics are non-critical and the Supabase insert will throw
          which gets caught by the outer try/catch.

      - file: src/app/(tabs)/settings/topics.tsx
        cr: "CR-286, CR-288"
        status: pass
        notes: |
          CR-286:
          - Section title uses t('topics.customTopics') at line 375. Correct.
          - SwipeableCard disabled={!canWrite || item.is_default} at line 399. Correct.

          CR-288:
          - No SearchInput import. No search state. No filteredTopics. No '+' button.
          - All old styles removed (searchContainer, closeButtonText, addButton, addButtonText).
          - Inline add area at lines 411-433, BELOW topic list (after wardTopics.map).
          - canWrite guard wraps entire inline add area. Correct.
          - isAdding toggles between Pressable placeholder and TopicEditor. Correct.
          - Pressable onPress resets activeSwipeId and editingId before setIsAdding(true).
          - TopicEditor onSave calls handleSaveNew, onCancel sets isAdding false. Correct.
          - addTopicRow and addTopicPlaceholder styles match ARCH spec. Correct.
          - wardTopics used directly (not filteredTopics). useWardTopics() no args. Correct.

      - file: src/i18n/locales/pt-BR.json
        cr: "CR-286, CR-287"
        status: pass
        notes: |
          topics.customTopics = "Temas Personalizados". Correct.
          topics.description updated with new text matching SPEC exactly. Correct.
          topics.wardTopics key retained for backward compatibility. OK.

      - file: src/i18n/locales/en-US.json
        cr: "CR-286, CR-287"
        status: pass
        notes: |
          topics.customTopics = "Custom Topics". Correct.
          topics.description updated. Matches SPEC exactly. Correct.

      - file: src/i18n/locales/es-LA.json
        cr: "CR-286, CR-287"
        status: pass
        notes: |
          topics.customTopics = "Temas Personalizados". Correct.
          topics.description updated. Matches SPEC exactly. Correct.

      - file: src/__tests__/f021-topic-library-overhaul.test.ts
        cr: "CR-285, CR-286, CR-287, CR-288"
        status: pass
        notes: |
          77 behavioral tests covering all 18 ACs and 6 edge cases.
          Tests verify migrations, types, hooks, i18n, UI structure,
          inline add pattern, and cross-cutting concerns (no regressions).
          File-based assertions (fs.readFileSync + regex/contain) for
          structural checks. Import-based assertions for i18n values
          and FIXED_COLLECTION_ORDER. Comprehensive and correct.

    # =========================================================================
    # SPECIFIC AREAS CHECKED (per review instructions)
    # =========================================================================

    specific_checks:
      - area: "Migration 033 CASCADE"
        result: pass
        detail: |
          001_initial_schema.sql:66 - general_topics.collection_id REFERENCES general_collections(id) ON DELETE CASCADE
          001_initial_schema.sql:79 - ward_collection_config.collection_id REFERENCES general_collections(id) ON DELETE CASCADE
          Both FKs have CASCADE. Migration 033 DELETE will cascade correctly.

      - area: "Migration 034 language mapping and NOT EXISTS"
        result: pass
        detail: |
          Uses w.language = 'pt-BR'/'en-US'/'es-LA' which match the wards.language column values.
          NOT EXISTS guard prevents duplicates. CROSS JOIN with VALUES generates rows correctly.

      - area: "register-first-user default topics placement"
        result: pass
        detail: |
          Lines 147-160: After ward creation (line 137), before user creation (line 163).
          Uses correct wardLanguage variable. Falls back to en-US. is_default: true.

      - area: "topics.tsx inline add BELOW topic list"
        result: pass
        detail: |
          Lines 411-433: Inline add area appears after wardTopics.map() block (lines 387-409).
          TopicEditor at TOP of list (old isAdding && block) is removed.

      - area: "topics.tsx SwipeableCard disabled for defaults"
        result: pass
        detail: |
          Line 399: disabled={!canWrite || item.is_default}. Both conditions checked.

      - area: "FIXED_COLLECTION_ORDER priorities after special topics removal"
        result: pass
        detail: |
          6 entries: FSY collections = 0, Gospel Principles = 1.
          Unknown collections get 3 via ?? in useCollections select callback.

      - area: "i18n customTopics and description in all 3 locales"
        result: pass
        detail: |
          All 3 locales have topics.customTopics with correct values.
          All 3 locales have topics.description with updated text matching SPEC.

      - area: "normalizeForSearch NOT removed"
        result: pass
        detail: |
          Function still exists at useTopics.ts:37. Kept per team-lead instruction.
          TopicSelectorModal has its own copy at line 30.

      - area: "ward_collection_config FK CASCADE check"
        result: pass
        detail: |
          001_initial_schema.sql:79 confirms ON DELETE CASCADE on collection_id FK.
          Migration 033 does not need explicit DELETE from ward_collection_config.

    # =========================================================================
    # SECURITY REVIEW
    # =========================================================================

    security:
      notes: |
        - No new RLS policies added (uses existing ward_topics and general_collections RLS)
        - Default topic protection is UI-only (SwipeableCard disabled). No RLS enforcement
          prevents a direct API call from deleting a default topic. This is acceptable for
          the current threat model since all authenticated users with topic:write already
          have full CRUD on ward_topics via existing RLS.
        - register-first-user uses supabaseAdmin (service role) for default topic insertion.
          This is the existing pattern for ward setup operations.
        - Migration 033 CASCADE is safe: only deletes data owned by the deleted collection.
        - No new API endpoints, no new auth flows, no secrets exposed.

    # =========================================================================
    # DEPLOY IDENTIFICATION
    # =========================================================================

    pending_deploys:
      - type: migration
        file: supabase/migrations/033_delete_special_topics.sql
        target: Supabase (production)
        instructions: |
          Run migration 033 BEFORE migration 034. This deletes the special topics
          collections. CASCADE handles general_topics and ward_collection_config cleanup.
          Speeches with snapshot topics from these collections are preserved (no FK).
          One-time, irreversible data deletion. Verify with:
            SELECT count(*) FROM general_collections WHERE name IN ('Temas Especiais', 'Special Topics', 'Temas Especiales');
          Should return 0 after migration.

      - type: migration
        file: supabase/migrations/034_add_default_ward_topics.sql
        target: Supabase (production)
        instructions: |
          Run migration 034 AFTER migration 033. Adds is_default column to ward_topics
          and seeds 2 default topics per existing ward. Verify with:
            SELECT count(*) FROM ward_topics WHERE is_default = true;
          Should return 2 * (number of wards). Check per-language:
            SELECT w.language, wt.title FROM ward_topics wt JOIN wards w ON w.id = wt.ward_id WHERE wt.is_default = true ORDER BY w.language, wt.title;

      - type: edge_function
        file: supabase/functions/register-first-user/index.ts
        target: Supabase Edge Functions (production)
        instructions: |
          Deploy register-first-user after migrations 033+034 are applied.
          Changes: knownNames reduced to 6 entries, default ward topics inserted
          after ward creation. Test by registering a new ward and verifying
          2 default topics appear with is_default=true.

    deploy_notes: |
      Deploy order: migration 033 -> migration 034 -> register-first-user edge function.
      Frontend changes (topics.tsx, useTopics.ts, i18n, database.ts) will be included
      in the next EAS Build.

    # =========================================================================
    # REPOSITORY STATUS
    # =========================================================================

    repository_status:
      clean: true
      uncommitted_code_files: []
      uncommitted_doc_files:
        - "docs/specs/SPEC_F021.yaml"
        - "docs/arch/ARCH_M021.yaml"
        - "docs/plans/PLAN_P021.yaml"
        - "docs/reviews/REVIEW_CONSOLIDATED.yaml"
      notes: |
        All code changes for F021 are committed across 8 commits
        (357323e..130083b). Documentation files are pending Orchestrator commit.

    # =========================================================================
    # VERDICT & STATS
    # =========================================================================

    verdict: approved
    summary: |
      CR-285 through CR-288 (Topic Library Overhaul) implementation is correct
      and complete. Chain validation passes at all 4 levels (SPEC->ARCH->PLAN->CODE).

      Migration 033 correctly deletes special topics with CASCADE (verified FK
      definitions in 001_initial_schema.sql:66,79). Migration 034 correctly adds
      is_default column and seeds defaults with NOT EXISTS guard and language
      filtering. register-first-user correctly inserts default topics after ward
      creation with language fallback to en-US. topics.tsx correctly removes search
      infrastructure, floating '+' button, and adds inline add area BELOW topic
      list with canWrite guard and TopicEditor expansion. SwipeableCard disabled
      for is_default topics. FIXED_COLLECTION_ORDER updated to 6 entries with
      correct priorities. All 3 i18n locales updated with customTopics key and
      new description text matching SPEC exactly. normalizeForSearch kept per
      team-lead instruction. 77 new tests, 2212 total passing. Zero P0/P1/P2
      issues found.

    stats:
      p0_count: 0
      p1_count: 0
      p2_count: 0
      files_reviewed: 10
      total_commits: 8
      tests_total: 2212
      tests_passing: 2212
      test_files: 70
